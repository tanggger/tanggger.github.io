<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>My-First-Blog</title>
    <url>/2025/02/06/My-First-Blog/</url>
    <content><![CDATA[<p>Welcome to My Blog!</p>
]]></content>
  </entry>
  <entry>
    <title>make命令教程</title>
    <url>/2025/02/28/make%E5%91%BD%E4%BB%A4%E6%95%99%E7%A8%8B/</url>
    <content><![CDATA[<h2 id="make的概念"><a href="#make的概念" class="headerlink" title="make的概念"></a><code>make</code>的概念</h2><p><code>make</code>命令就是要做出某个文件。比如，要做出文件<code>a.txt</code>，就可以执行下面的指令：</p>
<div class="code-container" data-rel="Bash"><figure class="iseeu highlight bash"><table><tr><td class="code"><pre><span class="line">$ make a.txt</span><br></pre></td></tr></table></figure></div>
<p>但是，如果我们真的输入这条指令，它并不会起作用，因为<code>make</code>命令并不知道如何应该做出这个文件，需要有人告诉它，如何调用其他命令完成这个目标。</p>
<p>比如，假设文件<code>a.txt</code>依赖于<code>b.txt</code>和<code>c.txt</code>，是后面两个文件连接（<code>cat</code>命令）的产物。那么，<code>make</code>需要知道下面的规则。</p>
<div class="code-container" data-rel="Makefile"><figure class="iseeu highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">a.txt: b.txt c.txt</span></span><br><span class="line">    cat b.txt c.txt &gt; a.txt</span><br></pre></td></tr></table></figure></div>
<p>也即：<code>make a.txt</code>这条指令的背后，实际上分成两步：</p>
<ul>
<li>确认<code>b.txt</code>和<code>c.txt</code>必须已经存在；</li>
<li>使用<code>cat</code>命令 将这个两个文件合并，输出为新文件。</li>
</ul>
<p>像这样的规则，都写在一个叫做<strong><code>makefile</code></strong>的文件中，<code>make</code>命令依赖这个文件进行构建。</p>
<h2 id="Makefile文件的格式"><a href="#Makefile文件的格式" class="headerlink" title="Makefile文件的格式"></a><code>Makefile</code>文件的格式</h2><p>构建规则都写在<code>Makefile</code>文件里面，要学会如何<code>make</code>命令，就必须学会如何编写<code>Makefile</code>文件。</p>
<h3 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h3><p><code>Makefile</code>文件由一系列规则组成。每条规则的形式如下：</p>
<div class="code-container" data-rel="Makefile"><figure class="iseeu highlight makefile"><table><tr><td class="code"><pre><span class="line">&lt;target&gt; : &lt;prerequisites&gt; </span><br><span class="line">[tab]  &lt;commands&gt;</span><br></pre></td></tr></table></figure></div>
<p>上面第一行冒号前面的部分，叫做”<strong>目标</strong>“（target），冒号后面的部分叫做”<strong>前置条件</strong>“（prerequisites）；第二行必须由一个<code>tab</code>键起首，后面跟着”<strong>命令</strong>“（commands）。</p>
<h3 id="目标（target）"><a href="#目标（target）" class="headerlink" title="目标（target）"></a>目标（target）</h3><p>一个<strong>目标</strong>构成一条规则。目标通常是文件名，指明<code>make</code>指令苏要构建的对象，比如上文的<code>a.txt</code>。目标可以是一个文件名，也可以是多个文件名，之间用空格分隔。</p>
<p>除了文件名，目标还可以是某个操作的名字，这称为”<strong>伪目标</strong>“（phony target）。</p>
<div class="code-container" data-rel="Makefile"><figure class="iseeu highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">clean:</span></span><br><span class="line">	rm *.o</span><br></pre></td></tr></table></figure></div>
<p>上面代码的目标是clean，它不是文件名，而是一个操作的名字，属于”伪目标 “，作用是删除对象文件。</p>
<div class="code-container" data-rel="Bash"><figure class="iseeu highlight bash"><table><tr><td class="code"><pre><span class="line">$ make clean</span><br></pre></td></tr></table></figure></div>
<p>但是，如果当前目录中，正好有一个文件叫做<code>clean</code>，那么这个命令不会执行。因为<code>make</code>发现<code>clean</code>文件已经存在，就认为没有必要重新构建了，就不会执行指定的rm命令。</p>
<p>为了避免这种情况，可以明确声明<code>clean</code>是”<strong>伪目标</strong>“，写法如下。</p>
<div class="code-container" data-rel="Makefile"><figure class="iseeu highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="meta"><span class="keyword">.PHONY</span>: clean</span></span><br><span class="line"><span class="section">clean:</span></span><br><span class="line">        rm *.o temp</span><br></pre></td></tr></table></figure></div>
<p>特别地，如果<code>make</code>指令运行时没有指定的目标，默认会执行<code>Makefile</code>文件的第一个目标。</p>
<h3 id="前置条件（prerequisites）"><a href="#前置条件（prerequisites）" class="headerlink" title="前置条件（prerequisites）"></a>前置条件（prerequisites）</h3><p>前置条件通常是一组文件名，之间用空格分隔。它指定了”目标”是否重新构建的判断标准：只要有一个前置文件不存在，或者有过更新，”目标”就需要重新构建。</p>
<div class="code-container" data-rel="Makefile"><figure class="iseeu highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">result.txt: source.txt</span></span><br><span class="line">    cp source.txt result.txt</span><br></pre></td></tr></table></figure></div>
<p>上面代码中，构建 <code>result.txt</code> 的前置条件是 <code>source.txt</code> 。如果当前目录中，<code>source.txt</code> 已经存在，那么<code>make result.txt</code>可以正常运行，否则必须再写一条规则，来生成 <code>source.txt</code> 。</p>
 <div class="code-container" data-rel="Bash"><figure class="iseeu highlight bash"><table><tr><td class="code"><pre><span class="line">source.txt:</span><br><span class="line">    <span class="built_in">echo</span> <span class="string">&quot;this is the source&quot;</span> &gt; source.txt</span><br></pre></td></tr></table></figure></div>
<p>上面代码中，source.txt后面没有前置条件，就意味着它跟其他文件都无关，只要这个文件还不存在，每次调用<code>make source.txt</code>，它都会生成。</p>
<div class="code-container" data-rel="Bash"><figure class="iseeu highlight bash"><table><tr><td class="code"><pre><span class="line">$ make result.txt</span><br><span class="line">$ make result.txt</span><br></pre></td></tr></table></figure></div>
<p>上面命令连续执行两次<code>make result.txt</code>。第一次执行会先新建 source.txt，然后再新建 result.txt。第二次执行，Make发现 source.txt 没有变动（时间戳晚于 result.txt），就不会执行任何操作，result.txt 也不会重新生成。</p>
<p>如果需要生成多个文件，往往采用下面的写法：</p>
<div class="code-container" data-rel="Makefile"><figure class="iseeu highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">source: file1 file2 file3</span></span><br></pre></td></tr></table></figure></div>
<p>执行<code>make source</code>命令后，就会一次性生成 file1，file2，file3 三个文件。这比执行三次<code>make</code>指令要方便得多。</p>
<h3 id="命令（commands）"><a href="#命令（commands）" class="headerlink" title="命令（commands）"></a>命令（commands）</h3><p>命令（commands）表示如何更新目标文件，由一行或多行的<code>Shell</code>命令组成。它是构建”目标”的具体指令，它的运行结果通常就是生成目标文件。</p>
<blockquote>
<p>[!NOTE]</p>
<p>每行命令之前必须有一个tab键。</p>
</blockquote>
<p>需要注意的是，每行命令在一个单独的shell中执行。这些Shell之间没有继承关系。</p>
<div class="code-container" data-rel="Makefile"><figure class="iseeu highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">var-lost:</span></span><br><span class="line">    <span class="keyword">export</span> foo=bar</span><br><span class="line">    echo <span class="string">&quot;foo=[$$foo]&quot;</span></span><br></pre></td></tr></table></figure></div>
<p>执行完<code>make var-lost</code>后取不到<code>foo</code>的值。因为两行命令在两个不同的进程执行。解决方法有三：</p>
<ul>
<li>两行命令写在一行，中间用分号分隔；</li>
<li>在换行符前加反斜杠转义；</li>
<li>加上<code>.ONESHELL:</code>命令。</li>
</ul>
<h2 id="Makefile文件的语法"><a href="#Makefile文件的语法" class="headerlink" title="Makefile文件的语法"></a><code>Makefile</code>文件的语法</h2><h3 id="注释"><a href="#注释" class="headerlink" title="注释"></a>注释</h3><p><code>#</code>在 <code>Makefile</code> 中表示注释。</p>
<h3 id="回声（echoing）"><a href="#回声（echoing）" class="headerlink" title="回声（echoing）"></a>回声（echoing）</h3><p>正常情况下，<code>make</code>会打印每条命令，然后再执行。</p>
<div class="code-container" data-rel="Makefile"><figure class="iseeu highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">test:</span></span><br><span class="line">    <span class="comment"># 这是测试</span></span><br></pre></td></tr></table></figure></div>
<p>执行上面的规则,就会进行输出 <code># 这是测试</code>：</p>
<div class="code-container" data-rel="Makefile"><figure class="iseeu highlight makefile"><table><tr><td class="code"><pre><span class="line">$ make test</span><br><span class="line"><span class="comment"># 这是测试</span></span><br></pre></td></tr></table></figure></div>
<blockquote>
<p>[!NOTE]</p>
<p>在命令的前面加上@，就可以关闭回声。</p>
</blockquote>
<h3 id="通配符"><a href="#通配符" class="headerlink" title="通配符"></a>通配符</h3><p>通配符（wildcard）用来指定一组符合条件的文件名。· 的通配符与 · 一致，主要有<strong>星号</strong>（<em>）、<strong>问号</strong>（？）和 <strong>[…]</strong> 。比如， `</em>.o<code>表示所有后缀名为</code>.o`的文件。</p>
<div class="code-container" data-rel="Makefile"><figure class="iseeu highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">clean:</span></span><br><span class="line">        rm -f *.o</span><br></pre></td></tr></table></figure></div>
<h3 id="模式匹配"><a href="#模式匹配" class="headerlink" title="模式匹配"></a>模式匹配</h3><p><code>make</code>命令允许对文件名，进行类似正则运算的匹配，主要用到的匹配符是<code>%</code>。比如，假定当前目录下有 <code>f1.c</code> 和 <code>f2.c</code> 两个源码文件，需要将它们编译为对应的对象文件。</p>
<div class="code-container" data-rel="Makefile"><figure class="iseeu highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">%.o: %.c</span></span><br></pre></td></tr></table></figure></div>
<p>等同于下面的写法</p>
<div class="code-container" data-rel="Makefile"><figure class="iseeu highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="section">f1.o: f1.c</span></span><br><span class="line"><span class="section">f2.o: f2.c</span></span><br></pre></td></tr></table></figure></div>
<p>使用匹配符<code>%</code>，可以将大量同类型的文件，只用一条规则就完成构建。</p>
]]></content>
      <tags>
        <tag>os</tag>
      </tags>
  </entry>
  <entry>
    <title>oo-unit2</title>
    <url>/2025/05/14/oo-unit2/</url>
    <content><![CDATA[<h2 id="同步块设置与锁选择分析"><a href="#同步块设置与锁选择分析" class="headerlink" title="同步块设置与锁选择分析"></a>同步块设置与锁选择分析</h2><h3 id="Elevator-java-中的同步："><a href="#Elevator-java-中的同步：" class="headerlink" title="Elevator.java 中的同步："></a><strong><code>Elevator.java</code></strong> 中的同步：</h3><ul>
<li><p><strong>同步块</strong>：</p>
<ul>
<li><p>在 <code>Elevator.java</code> 类中，使用了 <code>synchronized</code> 关键字来同步对共享资源的访问，特别是在对 <code>requestTable</code> 进行操作时，这个资源在多个电梯实例间共享。</p>
</li>
<li><p>例如：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">synchronized</span> (requestTable) &#123;</span><br><span class="line">    requestTable.notifyAll();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>这确保了多个线程访问 <code>requestTable</code> 时不会发生并发问题（例如多个电梯同时尝试修改或检查 <code>requestTable</code>）。</p>
</li>
<li><p>同步机制主要用于处理共享资源的访问，确保同一时刻只有一个线程能够执行某个特定操作，避免数据竞争。</p>
</li>
</ul>
</li>
<li><p><strong>锁的选择</strong>：</p>
<ul>
<li><code>synLock</code> 作为一个静态锁对象，用于同步整个电梯实例的关键操作。它确保在某些操作期间，例如电梯调度（<code>handleSche</code>）时，不会发生多个线程对电梯对象的并发修改。</li>
<li>同时，在电梯的双重模式下（<code>DuplexMode</code>），每个电梯会等待与配对电梯的同步操作。这些同步操作通过 <code>synchronized</code> 和 <code>wait</code>/<code>notify</code> 方法实现线程间的协调，保证两个电梯在执行更新操作时能够互相等待和通知。</li>
</ul>
</li>
</ul>
<h3 id="InputThread-java-中的同步："><a href="#InputThread-java-中的同步：" class="headerlink" title="InputThread.java 中的同步："></a><strong><code>InputThread.java</code></strong> 中的同步：</h3><ul>
<li><p>在 <code>InputThread</code> 中，同样使用了 <code>synchronized</code> 来确保多个线程对 <code>mainRequestTable</code> 的修改是线程安全的。例如：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">synchronized</span> (<span class="built_in">this</span>) &#123;</span><br><span class="line">    mainRequestTable.setEnd();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>这种同步确保了当请求输入线程结束时，不会发生并发访问 <code>mainRequestTable</code> 的问题。</p>
</li>
</ul>
<h3 id="RequestTable-java-中的同步："><a href="#RequestTable-java-中的同步：" class="headerlink" title="RequestTable.java 中的同步："></a><strong><code>RequestTable.java</code></strong> 中的同步：</h3><ul>
<li><strong>同步块</strong>：<ul>
<li><code>RequestTable</code> 中的大部分方法（如 <code>addRequest</code>、<code>takeRequest</code> 等）都使用了 <code>synchronized</code>，确保多个线程对请求表的操作（添加、删除请求等）是互斥的。</li>
<li>例如，在 <code>takeRequest</code> 方法中，电梯在取出请求时会等待请求表中的内容，如果没有请求，线程会在 <code>wait()</code> 调用上阻塞，直到有新的请求加入或操作结束。</li>
<li>这通过 <code>wait</code> 和 <code>notifyAll</code> 方法协调线程间的通知和等待，避免了死锁问题。</li>
</ul>
</li>
<li><strong>锁的选择</strong>：<ul>
<li>使用 <code>synchronized</code> 来同步方法，确保在访问或修改 <code>requestTable</code> 时，只有一个线程可以执行相应的代码块。</li>
</ul>
</li>
</ul>
<h3 id="Schedule-java-中的同步："><a href="#Schedule-java-中的同步：" class="headerlink" title="Schedule.java 中的同步："></a><strong><code>Schedule.java</code></strong> 中的同步：</h3><ul>
<li><p><strong>同步块</strong>：</p>
<ul>
<li><p><code>Schedule</code> 线程在检查和处理 <code>mainRequestTable</code> 时，同样使用了 <code>synchronized</code> 来确保在多个线程操作 <code>requestTable</code> 时，避免并发修改的问题。特别是在检查请求是否结束时：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">synchronized</span> (mainRequestTable) &#123;</span><br><span class="line">    <span class="keyword">if</span> (mainRequestTable.isEnd() &amp;&amp; !isAllEmpty()) &#123;</span><br><span class="line">        mainRequestTable.wait();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><code>wait</code> 和 <code>notifyAll</code> 用于通知其他线程 <code>mainRequestTable</code> 状态的变化，并确保电梯调度任务能够继续执行。</p>
</li>
</ul>
</li>
</ul>
<h3 id="锁与同步块中处理语句之间的关系："><a href="#锁与同步块中处理语句之间的关系：" class="headerlink" title="锁与同步块中处理语句之间的关系："></a><strong>锁与同步块中处理语句之间的关系</strong>：</h3><ul>
<li>锁和同步块的选择是为了保证在多线程环境下对共享资源的安全访问。在上述代码中，所有共享资源（如 <code>requestTable</code>、电梯状态、配对电梯等）都通过 <code>synchronized</code> 关键字确保了同一时刻只有一个线程可以修改它们。</li>
<li><code>wait</code> 和 <code>notify</code> 方法在这些同步块中被用来处理线程间的协调。<code>wait</code> 会让线程在条件不满足时阻塞，直到其他线程通过 <code>notify</code> 或 <code>notifyAll</code> 通知其继续执行。例如，在电梯到达请求楼层并准备继续移动时，通过 <code>notifyAll</code> 来唤醒等待的线程。</li>
<li>通过精确控制线程的执行顺序，避免了由于并发访问共享资源而引发的数据一致性问题。</li>
</ul>
<h2 id="调度器与调度策略设计"><a href="#调度器与调度策略设计" class="headerlink" title="调度器与调度策略设计"></a>调度器与调度策略设计</h2><h3 id="1-调度器设计总结分析"><a href="#1-调度器设计总结分析" class="headerlink" title="1. 调度器设计总结分析"></a>1. <strong>调度器设计总结分析</strong></h3><p>在这次作业中，调度器（<code>Schedule</code> 类）主要负责调度电梯的运行，确保电梯按请求表中的顺序执行任务，并通过合理的调度策略分配任务。调度器的设计关注以下几个方面：</p>
<h4 id="1-1-调度器的职责："><a href="#1-1-调度器的职责：" class="headerlink" title="1.1 调度器的职责："></a>1.1 <strong>调度器的职责</strong>：</h4><ul>
<li><p><strong>任务调度</strong>：调度器从 <code>mainRequestTable</code> 中获取请求，并根据请求的优先级和楼层，决定哪个电梯处理该请求。调度器根据请求的类型（如乘客请求、调度请求等），通知合适的电梯进行处理。</p>
</li>
<li><p><strong>线程协调</strong>：调度器与多个电梯线程（<code>Elevator</code> 类）协同工作。通过 <code>wait</code> 和 <code>notifyAll</code> 等同步机制，确保各个线程之间的协调与数据一致性。例如，当调度器发现 <code>mainRequestTable</code> 为空时，它会调用 <code>wait</code> 让当前线程等待，直到有新请求进入。</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">synchronized</span> (mainRequestTable) &#123;</span><br><span class="line">    <span class="keyword">if</span> (mainRequestTable.isEmpty() &amp;&amp; !mainRequestTable.isEnd()) &#123;</span><br><span class="line">        mainRequestTable.wait(); <span class="comment">// 等待直到有请求</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>调度结束控制</strong>：调度器负责检测是否所有电梯的任务都已完成（如 <code>isEnd()</code> 方法判断请求表是否为空）。当所有任务完成时，调度器会结束整个调度过程。</p>
</li>
</ul>
<h4 id="1-2-与线程的交互："><a href="#1-2-与线程的交互：" class="headerlink" title="1.2 与线程的交互："></a>1.2 <strong>与线程的交互</strong>：</h4><ul>
<li><p><strong>电梯线程和调度器的协作</strong>：调度器通过 <code>mainRequestTable</code> 与多个电梯线程进行协作。调度器定期检查请求表，并将任务分配给电梯。电梯线程则从请求表中取出任务，并在完成后向调度器报告其状态。</p>
<p>例如：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">synchronized</span> (mainRequestTable) &#123;</span><br><span class="line">    <span class="keyword">if</span> (!mainRequestTable.isEnd()) &#123;</span><br><span class="line">        mainRequestTable.wait();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>通知机制</strong>：当调度器处理完一个任务后，会通过 <code>notifyAll</code> 通知相关电梯线程开始处理任务。同时，如果电梯需要等待新的请求，调度器会确保电梯线程在请求到来前不会被过早唤醒，避免浪费资源。</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">synchronized</span> (mainRequestTable) &#123;</span><br><span class="line">    mainRequestTable.notifyAll(); <span class="comment">// 通知电梯线程继续执行</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
</ul>
<h3 id="2-调度策略总结分析"><a href="#2-调度策略总结分析" class="headerlink" title="2. 调度策略总结分析"></a>2. <strong>调度策略总结分析</strong></h3><p>调度策略的核心目的是合理分配电梯的任务，考虑多个因素（如时间、电量、优先级等），确保系统整体效率。以下是对当前调度策略的分析：</p>
<h4 id="2-1-调度策略的设计："><a href="#2-1-调度策略的设计：" class="headerlink" title="2.1 调度策略的设计："></a>2.1 <strong>调度策略的设计</strong>：</h4><p>调度策略主要通过 <code>Strategy</code> 类进行实现，并根据不同的条件（如电梯的当前楼层、方向、是否处于双电梯模式等）决定电梯的行为。<code>Strategy</code> 类使用了不同的策略来控制电梯的行动，如：</p>
<ul>
<li><p><strong>常规模式（<code>normal</code>）</strong>：该模式根据<code>Look</code>算法来决定电梯是移动、等待还是开门。</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> Advice <span class="title function_">normal</span><span class="params">(<span class="type">int</span> nowFloor, <span class="type">int</span> nowNum, <span class="type">int</span> direction, HashMap&lt;Integer, PriorityBlockingQueue&lt;Person&gt;&gt; destMap, RequestTable requestTable)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (canOpenForOut(nowFloor, destMap) || canOpenForIn(nowFloor, nowNum, direction, requestTable)) &#123;</span><br><span class="line">        <span class="keyword">return</span> Advice.OPEN; <span class="comment">// 开门</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 其他决策（如移动、等待等）</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>双电梯模式（<code>DuplexMode</code>）</strong>：当电梯处于双重模式时，会考虑与配对电梯的协作，避免电梯之间的冲突。</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> Advice <span class="title function_">DuplexMode</span><span class="params">(<span class="type">int</span> nowFloor, <span class="type">int</span> nowNum, <span class="type">int</span> direction, HashMap&lt;Integer, PriorityBlockingQueue&lt;Person&gt;&gt; destMap, RequestTable requestTable, Elevator thisElevator, Elevator pairElevator)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (willCollide(thisElevator, pairElevator)) &#123;</span><br><span class="line">        <span class="keyword">return</span> Advice.SLEEP; <span class="comment">// 如果发生冲突，休眠</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> Advice.MOVE; <span class="comment">// 否则继续移动</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
</ul>
<h2 id="整体架构分析"><a href="#整体架构分析" class="headerlink" title="整体架构分析"></a>整体架构分析</h2><h3 id="UML类图"><a href="#UML类图" class="headerlink" title="UML类图"></a>UML类图</h3><p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/images/$%7Bfiilename%7D/image-20250417163058473.png"
                      alt="image-20250417163058473"
                ></p>
<h4 id="三次作业架构设计迭代"><a href="#三次作业架构设计迭代" class="headerlink" title="三次作业架构设计迭代"></a>三次作业架构设计迭代</h4><h5 id="第五次作业"><a href="#第五次作业" class="headerlink" title="第五次作业"></a>第五次作业</h5><p>第五次作业作为多线程单元的开始，要求我们模拟一个多线程实时电梯系统，涉及到对乘客进行输入的接收，分配以及电梯对乘客的运输。本次作业我参考往届学长学姐的博客，设计了<code>InputThread</code>、<code>Schedule</code>、<code>ElevatorThread</code>三个种类的线程，分别对应着上述的三个功能。由于本次作业强行固定了乘客乘坐的电梯序号，因此在本次作业中<code>Schedule</code>线程其实没有太大作用，主要是为了给后续迭代中需要手动给乘客分配电梯做铺垫。</p>
<p>具体到电梯的调度策略，我采用了<code>Look</code>算法，并建立了<code>Strategy</code>类，为每一个电梯线程提供策略：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Strategy</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> RequestTable requestTable;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Advice <span class="title function_">getAdvice</span><span class="params">(<span class="type">int</span> nowFloor, <span class="type">int</span> nowNum, <span class="type">int</span> direction,</span></span><br><span class="line"><span class="params">                            HashMap&lt;Integer, ArrayList&lt;Person&gt;&gt; destMap)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (canOpenForOut(nowFloor, destMap) </span><br><span class="line">            || canOpenForIn(nowFloor, nowNum, direction)) &#123; </span><br><span class="line">            <span class="keyword">return</span> Advice.OPEN;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (nowNum != <span class="number">0</span>) &#123; </span><br><span class="line">            <span class="keyword">return</span> Advice.MOVE;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;  </span><br><span class="line">            <span class="keyword">if</span> (requestTable.isEmpty()) &#123; </span><br><span class="line">                <span class="keyword">if</span> (requestTable.isEnd()) &#123; </span><br><span class="line">                    <span class="keyword">return</span> Advice.OVER; </span><br><span class="line">                &#125; <span class="keyword">else</span> &#123; </span><br><span class="line">                    <span class="keyword">return</span> Advice.WAIT; </span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (hasReqInOriginDirection(nowFloor, direction)) &#123;</span><br><span class="line">                <span class="keyword">return</span> Advice.MOVE; </span><br><span class="line">            &#125; <span class="keyword">else</span> &#123; </span><br><span class="line">                <span class="keyword">return</span> Advice.REVERSE; </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
<h5 id="第六次作业"><a href="#第六次作业" class="headerlink" title="第六次作业"></a>第六次作业</h5><p>第六次作业与第五次作业相比，取消了乘客请求指定电梯的约束，需要自行设计电梯分配策略；新增了“临时调度”的请求；新增“RECEIVE”约束；修改乘客输出指令格式等。</p>
<p>针对取消乘客请求指定电梯的约束，自行设计电梯分配策略这个问题，我采用的是简单的随机分配的策略，即采用随机的方式将每一个乘客请求分配到电梯等待队列中。经过验证，随机分配的策略与其他的分配策略相比，似乎并没有对性能产生太大的差距，因此我将随机分配的策略沿用到第七次作业。但是有一个明显的缺陷就是在<code>debug</code>的时候很难对<code>bug</code>造成复现</p>
<h5 id="第七次作业"><a href="#第七次作业" class="headerlink" title="第七次作业"></a>第七次作业</h5><p>第七次作业与第六次作业相比，增加了双轿厢电梯的改造，因此本次作业的重点在于双轿厢改造的设计。</p>
<h6 id="实现双轿厢电梯的同步开始改造"><a href="#实现双轿厢电梯的同步开始改造" class="headerlink" title="实现双轿厢电梯的同步开始改造"></a>实现双轿厢电梯的同步开始改造</h6><p>根据题意，在改造开始之前，需要让<strong>两部电梯都停下后</strong>输出<code>UPDATE-BEGIN</code>才能开始改造动作。这个部分的将两个电梯停下之后在开始改造是一个小难点。根据这个问题，我的解决思路是：设置一个<code>isStop</code>的信号，若对检测到对方已经停止了，则将对方线程唤醒，否则将自己的线程进入睡眠态，从而达到使两个电梯达到同步。</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line">isStop = <span class="literal">true</span>;</span><br><span class="line"><span class="keyword">if</span> (pairElevator.getIsStop()) &#123;</span><br><span class="line">	<span class="keyword">synchronized</span> (pairElevator) &#123;</span><br><span class="line">    	pairElevator.notify();</span><br><span class="line">	&#125;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">synchronized</span> (<span class="built_in">this</span>) &#123;</span><br><span class="line">    	<span class="built_in">this</span>.wait();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
<h6 id="防撞机制"><a href="#防撞机制" class="headerlink" title="防撞机制"></a>防撞机制</h6><p>对于轿厢运行到换乘楼层的问题，我使用的解决方案是：<code>Strategy</code>类会检查当前电梯与配对电梯的以下状态：</p>
<ul>
<li>当前楼层（<code>nowFloor</code>）</li>
<li>运行方向（<code>direction</code>）</li>
<li>当前指令（<code>advice</code>）</li>
</ul>
<p>然后对两个电梯进行判断，若两个轿厢的运行方向，当前楼层以及当前指令符合特定的判断条件，则让本电梯睡眠一定时长，等待另外一个电梯安全地离开中转楼层再进行移动操作，依据这种躲避策略达到防撞的目的。</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">boolean</span> <span class="variable">judge1</span> <span class="operator">=</span> (thisNowFloor == transFloor + <span class="number">1</span> &amp;&amp; transFloor != -<span class="number">1</span>)</span><br><span class="line">	|| (thisNowFloor == transFloor + <span class="number">2</span> &amp;&amp; transFloor == -<span class="number">1</span>);</span><br><span class="line"><span class="type">boolean</span> <span class="variable">judge2</span> <span class="operator">=</span> (pairNowFloor == transFloor - <span class="number">1</span> &amp;&amp; transFloor != <span class="number">1</span>)</span><br><span class="line">	|| (pairNowFloor == transFloor - <span class="number">2</span> &amp;&amp; transFloor == <span class="number">1</span>);</span><br><span class="line"><span class="type">boolean</span> <span class="variable">judge3</span> <span class="operator">=</span> (pairNowFloor == transFloor - <span class="number">2</span> &amp;&amp; transFloor != <span class="number">2</span>)</span><br><span class="line">    || (pairNowFloor == transFloor - <span class="number">3</span> &amp;&amp; transFloor == <span class="number">2</span>);</span><br><span class="line"><span class="keyword">if</span> (judge1 &amp;&amp; judge3 &amp;&amp;</span><br><span class="line">    thisDirection == -<span class="number">1</span> &amp;&amp; pairDirection == <span class="number">1</span> &amp;&amp; pairAdvice == Advice.MOVE) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> (judge1 &amp;&amp; (pairNowFloor == transFloor ||(judge2 &amp;&amp; pairAdvice == Advice.MOVE)) &amp;&amp; thisDirection == -<span class="number">1</span>) &#123;</span><br><span class="line">     <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> (pairNowFloor == transFloor || judge2) &amp;&amp;pairDirection == -<span class="number">1</span> &amp;&amp; pairAdvice == Advice.MOVE;</span><br></pre></td></tr></table></figure></div>
<p>此设计确保了两部电梯在共享电梯井的时候，通过严格的路劲估测和状态同步，避免物理碰撞。</p>
<h3 id="UML协作图"><a href="#UML协作图" class="headerlink" title="UML协作图"></a>UML协作图</h3><p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/images/$%7Bfiilename%7D/image-20250418093116560.png"
                      alt="image-20250418093116560"
                ></p>
<h3 id="稳定以及易变内容分析"><a href="#稳定以及易变内容分析" class="headerlink" title="稳定以及易变内容分析"></a>稳定以及易变内容分析</h3><h4 id="稳定内容"><a href="#稳定内容" class="headerlink" title="稳定内容"></a>稳定内容</h4><p>主要的结构是稳定的。三次作业中，我的架构始终是<code>InputThread</code>、<code>Schedule</code>、<code>ElevatorThread</code>三个部分，分别对应着乘客的接收、分配以及电梯对乘客的运输。针对临时调度以及电梯双轿厢部分，我采用的是特殊处理的方式，并没有将临时调度请求和双轿厢请求放入<code>requestTable</code>中，因此我的架构的整体设计并没有做很大改变。</p>
<h4 id="易变内容"><a href="#易变内容" class="headerlink" title="易变内容"></a>易变内容</h4><p>对输入的请求的处理和电梯运动策略是易变的：</p>
<ul>
<li>由于在历次迭代中，逐渐添加了一些特殊的请求，比如说临时调度请求以及双轿厢电梯请求，因此在<code>InputThread</code>接收到请求之后，需要对请求的种类进行辨别，如果是临时调度请求或者双轿厢电梯请求，则对其进行特殊处理。</li>
<li>由于正常情况、临时调度以及双轿厢三种情况电梯调度的策略都不尽相同，因此需要在<code>Strategy</code>中进行修改</li>
<li>另外，还有<code>InputThread</code>线程的终止条件的判断在迭代中也产生了变化。在第五次作业的时候，判断<code>Schedule</code>线程终止的条件是<code>mainRequestTable</code>的输入已经终止了，但是在后面迭代中，由于临时调度和双轿厢要求在改造时将本电梯中所有<code>receive</code>了的乘客重新放回主请求表，因此判断<code>Schedule</code>线程终止的时候需要将电梯线程中打回的乘客请求纳入处理。</li>
</ul>
<h2 id="bug和debug"><a href="#bug和debug" class="headerlink" title="bug和debug"></a><code>bug</code>和<code>debug</code></h2><h3 id="出现过的bug"><a href="#出现过的bug" class="headerlink" title="出现过的bug"></a>出现过的<code>bug</code></h3><h4 id="第五次作业-1"><a href="#第五次作业-1" class="headerlink" title="第五次作业"></a>第五次作业</h4><p>第五次作业强测没有出现问题，但是在<code>hack</code>部分出现问题了，主要是当输入请求中暂时没有请求，但是程序仍然没有结束时，我没有将线程进入等待，导致我线程中一直处于轮询的状态，导致了<code>ctle</code>的问题。</p>
<h4 id="第六次作业-1"><a href="#第六次作业-1" class="headerlink" title="第六次作业"></a>第六次作业</h4><p>第六次作业主要碰到的问题是电梯在<code>SCHE-BEGIN</code>之后，几乎在同时输出了<code>RECEIVE</code>操作。这个是线程不安全的原因，因为不能确定在同一时间内线程中的指令执行的先后，针对这个问题，我设置了一个缓冲池，将乘客请求先放在缓冲池中，再在<code>Elevator</code>线程中从缓冲池中进行提取请求，这样，乘客的<code>RECEIVE</code>与<code>SCHE-BEGIN</code>属于串行的指令，也就自然消除了输出先后的问题。</p>
<h4 id="第七次问题"><a href="#第七次问题" class="headerlink" title="第七次问题"></a>第七次问题</h4><p>第七次问题主要问题是电梯防撞功能的优化和改进。由于在线程中访问其他线程的属性是不安全的，会产生刚读取完属性值就产生改变的情况。针对这个问题，我对双轿厢电梯的策略的获取加了锁，即每一次只能有一个轿厢取得策略，这样能很好地避免防撞判断时状态获取不准确的问题发生。</p>
<p>多线程程序的<code>debug</code>方法</p>
<h3 id="debug方法"><a href="#debug方法" class="headerlink" title="debug方法"></a><code>debug</code>方法</h3><h4 id="断点-线程监视器"><a href="#断点-线程监视器" class="headerlink" title="断点 + 线程监视器"></a>断点 + 线程监视器</h4><ul>
<li>使用 IntelliJ IDEA 的 <code>Thread Debugger</code>，在可疑代码处设置断点，右键选择 <code>Suspend: Thread</code>。<ul>
<li>这种方法只能在程序里面步进，只能简单的检查自己是否有逻辑上的错误，并不能清楚的看出进程之间的关系，可能不能找出线程之间的潜在的问题。所以这个<code>debug</code>方法只能进行初步的检测。</li>
</ul>
</li>
</ul>
<h4 id="Print大法"><a href="#Print大法" class="headerlink" title="Print大法"></a><code>Print</code>大法</h4><p>在关键路径处插入需要监测的属性，并以 <code>TimableOutput.println</code>的形式输出，这样可以很好的找出线程中的问题，但是有缺陷就是便利性不足，程序流程透明。</p>
<h2 id="心得体会"><a href="#心得体会" class="headerlink" title="心得体会"></a>心得体会</h2><h3 id="线程安全"><a href="#线程安全" class="headerlink" title="线程安全"></a>线程安全</h3><p>由于电梯系统天然是一个多线程协同调度的问题，需要多个电梯线程与输入、调度线程并发运行，如何避免竞态条件和线程阻塞成为核心挑战。例如在 <code>RequestTable</code> 中通过 <code>synchronized</code> 同步方法与 <code>wait/notifyAll()</code> 实现线程安全的生产者-消费者模型，确保请求的有序添加与处理。而在 <code>Elevator</code> 类中，对于共享资源如电梯当前楼层、请求队列、状态标志等，也通过细粒度的锁保证状态更新的一致性与原子性，避免了调度混乱与死锁风险。此外，<code>InputThread</code> 与 <code>Schedule</code> 的解耦设计，也体现了对输入处理与请求调度之间同步性的合理控制。</p>
<h3 id="层次化设计"><a href="#层次化设计" class="headerlink" title="层次化设计"></a>层次化设计</h3><p>在<strong>层次化设计</strong>方面，系统采用了清晰的职责划分架构。例如，<code>MainClass</code> 负责系统初始化，<code>InputThread</code> 负责请求接收与解析，<code>Schedule</code> 统一管理调度策略，而每个 <code>Elevator</code> 实例则自主管理自身状态与行为响应。各模块之间通过 <code>RequestTable</code> 和 <code>Person</code> 类传递数据，确保了数据逻辑的模块内封装、模块间低耦合。在电梯运行策略层面，进一步引入了 <code>Strategy</code> 模块封装调度决策逻辑，通过 <code>Advice</code> 枚举类实现电梯行为控制流的抽象，增强了代码的可维护性与可拓展性。</p>
]]></content>
      <tags>
        <tag>oo</tag>
      </tags>
  </entry>
  <entry>
    <title>oo-unit1</title>
    <url>/2025/03/19/oo-unit1/</url>
    <content><![CDATA[<h2 id="程序结构分析"><a href="#程序结构分析" class="headerlink" title="程序结构分析"></a>程序结构分析</h2><h3 id="度量分析"><a href="#度量分析" class="headerlink" title="度量分析"></a>度量分析</h3><h4 id="基于statistic插件进行代码规模分析"><a href="#基于statistic插件进行代码规模分析" class="headerlink" title="基于statistic插件进行代码规模分析"></a>基于<code>statistic</code>插件进行代码规模分析</h4><p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/images/image-20250318082718491.png"
                      alt="image-20250318082718491"
                ></p>
<p>从分析结果我们可以清楚地看到，代码行数最多的几个类分别是<code>Lexer</code>，<code>Func</code>，<code>Poly</code>以及<code>Parser</code>是代码量最大的几个类。<code>Lexer</code>类主要功能是词法解析器，即将输入的<code>String</code>处理成一个个<code>token</code>的形式，此外，我还在这个类里面设计了替换自定义递归函数，自定义普通函数，以及<code>dx</code>算子中的表达式三大功能，可以看到，本人代码的<code>Lexer</code>类功能设计较多，因此代码量最大是有一定合理性的。其次，<code>Func</code>，<code>Poly</code>两个类功能性也非常强，分别负责处理递归函数从而返回最终<code>Lexer</code>，以及实现对两个<code>Poly</code>进行运算操作的功能；最后是<code>Parser</code>类，功能是文法解析器，主要包含对<code>Expr</code>，<code>Term</code>以及<code>Factor</code>进行解析，由于上述类负责的要求比较繁杂，因此造成了代码量较大的情况。</p>
<h4 id="基于MetricsReloaded插件进行复杂度分析"><a href="#基于MetricsReloaded插件进行复杂度分析" class="headerlink" title="基于MetricsReloaded插件进行复杂度分析"></a>基于<code>MetricsReloaded</code>插件进行复杂度分析</h4><h5 id="方法复杂度分析"><a href="#方法复杂度分析" class="headerlink" title="方法复杂度分析"></a>方法复杂度分析</h5><p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/images/image-20250318090827244.png"
                      alt="image-20250318090827244"
                ></p>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/images/image-20250318090835344.png"
                      alt="image-20250318090835344"
                ></p>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/images/image-20250318090840436.png"
                      alt="image-20250318090840436"
                ></p>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/images/image-20250318090845527.png"
                      alt="image-20250318090845527"
                ></p>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/images/image-20250318090850210.png"
                      alt="image-20250318090850210"
                ></p>
<h6 id="名词解释"><a href="#名词解释" class="headerlink" title="名词解释"></a>名词解释</h6><ul>
<li><code>CogC</code>：认知复杂度，衡量理解代码逻辑所需的认知努力。它关注代码的控制流（如条件、循环、嵌套等）对开发者理解代码的难度。</li>
<li><code>ev(G)</code>：基本复杂度，衡量代码中无法通过重构简化的复杂度。它反映了问题本身的复杂性。</li>
<li><code>iv(G)</code>：设计复杂度，用于衡量模块之间的调用关系。复杂度越高，模块之间的耦合性越高，越难以隔离、维护和复用。</li>
<li><code>v(G)</code>：衡量代码中独立路径的数量，反映代码的测试难度和维护难度。圈复杂度越高，代码越难测试和维护。</li>
</ul>
<h6 id="参数分析"><a href="#参数分析" class="headerlink" title="参数分析"></a>参数分析</h6><p>在这个方法复杂度的列表中，能够很明显的看到，各个方法中用红色标注出来的方法绝大多数都是存在于上述的几个代码量较大的类中，由于他们负责一些目的性较强的功能，比如说合并三角函数同类项、解析表达式、替换递归函数等等，因此这些方法的代码复杂度相对较高，函数、循环嵌套相对复杂，因此导致了这些类里面的某些方法的<code>CogC</code>和<code>v(G)</code>值较高。</p>
<h5 id="类复杂度分析"><a href="#类复杂度分析" class="headerlink" title="类复杂度分析"></a>类复杂度分析</h5><p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/images/image-20250318105905143.png"
                      alt="image-20250318105905143"
                ></p>
<h6 id="名词解释-1"><a href="#名词解释-1" class="headerlink" title="名词解释"></a>名词解释</h6><ul>
<li><code>OCavg</code>: 平均操作复杂度</li>
<li><code>OCmax</code>: 最大操作复杂度</li>
<li><code>WMC</code>: 加权方法复杂度</li>
</ul>
<h6 id="参数分析-1"><a href="#参数分析-1" class="headerlink" title="参数分析"></a>参数分析</h6><p>分析大致与上一致，需要另外说明的是，<code>Main</code>类由于需要读入各个种类的函数，以及引用各个类，所以<code>OCavg</code>值会偏高，<code>Mono</code>类由于有判断<code>Mono</code>相等，以及对<code>Mono</code>求导等一些比较复杂的操作，其<code>WMC</code>值会偏高一些。</p>
<h3 id="unit1作业类图展示"><a href="#unit1作业类图展示" class="headerlink" title="unit1作业类图展示"></a>unit1作业类图展示</h3><p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/images/bcbee409445ba3e263ff9e3fbbb7c4f.png"
                      alt="bcbee409445ba3e263ff9e3fbbb7c4f"
                ></p>
<h4 id="类图介绍"><a href="#类图介绍" class="headerlink" title="类图介绍"></a>类图介绍</h4><ul>
<li><code>MainClass</code>：主类作用主要是接收自定义普通表达式、自定义递归表达式以及最后的计算表达式输入，以及调用一系列处理输入的核心方法。</li>
<li><code>Token</code>：<code>Token</code>类主要负责将输入的每一个字符对应成特定的词元，以便后续的词法分析以及语法分析。</li>
<li><p><code>Lexer</code>：<code>Lexer</code>类是词法分析器，它实现了词法分析功能，并且将分析得到的信息存储到<code>token</code>流中。在<code>hw1</code>的开发中，<code>Lexer</code>的功能仅限于遍历输入，将每一个元素转换成<code>token</code>；但是在后续<code>hw2</code>和<code>hw3</code>的迭代中，<code>Lexer</code>还承担了替换自定义函数递归表达式、自定义函数普通表达式、以及求导算子结果的功能，因此在我的架构中，<code>Lexer</code>类应该属于核心部分之一。</p>
</li>
<li><p><code>Func</code>：<code>Func</code>类定义的是自定义递归函数，主要功能是存储自定义递归函数的关键要素，如序号，形参因子，函数<code>Lexer</code>等，以及一个核心功能，即得出自定义递归表达式的最终结果。</p>
</li>
<li><p><code>NormalFunc</code>：<code>NormalFunc</code>类定义的是自定义普通函数，同样存储自定义普通函数的序号，形参因子，函数<code>Lexer</code>等，以及代入可能出现的普通函数调用。</p>
</li>
<li><p><code>Parser</code>：<code>Parser</code>同样是核心部分之一，主要功能是将在<code>Lexer</code>部分处理好的<code>token</code>流做处理，建立语法树，从而清晰表达表达式的语法结构。<code>Parser</code>类中，采取了自上而下，逐步拆分表达式、项以及因子的做法，因子如果是表达式的话再重新返回解析表达式的步骤，若是一些特殊种类的因子，则建立特殊的类，一直解析到最后形成一棵语法树。</p>
</li>
<li><p><code>Expr</code>，<code>Term</code>，<code>Factor</code>：这三个类定义了<code>Parser</code>中要解析的三个部分，即表达式、项、以及因子。</p>
</li>
<li><p><code>NumFactor</code>，<code>VarFactor</code>，<code>SinFactor</code>，<code>CosFactor</code>：这四个类是特殊的因子，并且都继承<code>Factor</code>类。</p>
</li>
<li><p><code>Mono</code>：<code>unit1</code>中，最终输出的表达式其实是一个</p>
</li>
</ul>
<script type="math/tex; mode=display">
Expr = \sum (coe_i * x_{i}^{exp_i} * \prod sin_j(Expr)^{exp_j} * \prod cos_j(Expr)^{exp_j})</script><p>的形式。因此我们定义了一个<code>Mono</code>类来存储一个最小单元的内容，包括系数、<code>x</code>指数以及三角函数项。</p>
<ul>
<li><code>Poly</code>：<code>Poly</code>类其实就是一个<code>Mono</code>的列表，就是表达式的最终形态。</li>
</ul>
<h2 id="架构设计体验"><a href="#架构设计体验" class="headerlink" title="架构设计体验"></a>架构设计体验</h2><h3 id="hw1"><a href="#hw1" class="headerlink" title="hw1"></a><code>hw1</code></h3><p>在第一次作业中，我的大体架构已经确定，基本上就是<code>String -&gt; Lexer -&gt; Parser -&gt; Poly -&gt; String</code>的步骤，在后续的迭代之中，<code>hw1</code>的架构都没有进行改变。</p>
<h3 id="hw2"><a href="#hw2" class="headerlink" title="hw2"></a><code>hw2</code></h3><p>第二次作业是本次<code>unit</code>最难的一个部分，添加了三角函数部分以及自定义递归函数部分，针对这两个难题，我分别对<code>hw1</code>的代码进行了如下的迭代：</p>
<h4 id="三角函数部分"><a href="#三角函数部分" class="headerlink" title="三角函数部分"></a>三角函数部分</h4><p>针对三角函数部分，我意识到<code>hw1</code>的<code>Mono</code>部分需要作出重大的改变，原先只是系数与自变量（及其指数）的乘积，本次作业则需要添加三角函数项，因此我首先在<code>Mono</code>类中添加<code>sin</code>和<code>cos</code>的<code>HashMap</code>，其中<code>key</code>存储的是三角函数括号内部的因子，<code>value</code>存储的是三角函数的指数。然后，添加三角函数之后，我们需要对<code>Poly</code>部分的运算也进行迭代，这个部分难度最大的是如何将相同的<code>Mono</code>合并，由于三角函数内部其实是一个因子，所以还会有出现三角函数的可能性。于是，针对这一问题，我采取了递归的比较思路，即若三角函数因子存在的话，就对三角函数里面的因子进行比较，通过逐步比较系数，自变量指数，三角函数相等，从而一直递归到三角函数的因子内部没有三角函数为止，从而结束递归比较，得出最终的比较结果。</p>
<h4 id="自定义递归函数部分"><a href="#自定义递归函数部分" class="headerlink" title="自定义递归函数部分"></a>自定义递归函数部分</h4><p>针对自定义递归函数部分，我采用的是函数替换的方法。简要的来说，就是在字符串输入的时候，将<code>Func</code>的关键信息存进类中，比如说形参表达式、序号、<code>token</code>流等。再对这个<code>token</code>流进行遍历，若遍历到<code>f</code>，则进行递归，用<code>f&#123;n-1&#125;</code>的<code>token</code>流对<code>f</code>进行替换，一直替换到<code>f&#123;0&#125;</code>和<code>f&#123;1&#125;</code>。这样进行架构设计的优点是可以及其直观地观察<code>token</code>流生成以及替换的流程，在<code>debug</code>过程中也会比较轻松等方面，缺点则可能是代码量可能较大，替换的<code>token</code>流易错。</p>
<h3 id="hw3"><a href="#hw3" class="headerlink" title="hw3"></a><code>hw3</code></h3><p>第三次作业的难度则要比第二次作业要简单很多，无非就是多了一个自定义普通函数以及求导。而这两个新增的要求也比较简单：</p>
<h4 id="自定义普通函数"><a href="#自定义普通函数" class="headerlink" title="自定义普通函数"></a>自定义普通函数</h4><p>针对自定义普通函数，我考虑的是先按照正常的<code>token</code>流进行读入，由于自定义普通函数可能会调用之前定义过的普通函数，则需要考虑当读到<code>h</code>或读到<code>g</code>的时候对普通函数进行<code>lexer</code>进行替换。</p>
<h4 id="求导"><a href="#求导" class="headerlink" title="求导"></a>求导</h4><p>针对求导的操作，我决定在<code>Poly</code>类中对求导算子中的表达式进行求导。一个<code>Poly</code>类的求导我将其拆分为每一个<code>Mono</code>的求导。因此，具体的求导方法我放在了<code>Mono</code>类。由于<code>Mono</code>类是系数、自变量以及三角函数的乘积，所以对<code>Mono</code>求导要进行链式法则，即对<code>Mono</code>里面的每一个子项进行求导，其他子项不变，再进行相加。很自然地，我又分别添加了单独对自变量以及三角函数的求导法则。这样的架构使我的求导计算十分简洁、可观性强。</p>
<h4 id="自定新的迭代场景"><a href="#自定新的迭代场景" class="headerlink" title="自定新的迭代场景"></a>自定新的迭代场景</h4><p>我自定义一个新的要求：</p>
<p>在前三次作业中，自定义递推函数中递推表达式只能由<code>f&#123;n-1&#125;</code>和<code>f&#123;n-2&#125;</code>生成，并且两个函数前面只能由一个系数链接，<code>f</code>也只能是一次方。因此新增的要求中，解除上述的两个限制，即递推表达式中<code>f&#123;n-1&#125;</code>和<code>f&#123;n-2&#125;</code>前面的系数可以是一个表达式，<code>f</code>的指数也不局限于<code>1</code>，可以有正常的指数。<br>在我的架构中，采用的核心方法是<code>Lexer</code>的替换，即碰到<code>f</code>的时候就将其替换成其表达的具体的表达式，因此，我的架构丝毫不会受到除了<code>f</code>以外的任何格式的影响，包括<code>f</code>的系数的形式，以及其是否存在不同的指数。因此我的代码架构对自定义递推函数格式的可拓展性上较高。</p>
<h2 id="Bug分析"><a href="#Bug分析" class="headerlink" title="Bug分析"></a>Bug分析</h2><h3 id="hw1-1"><a href="#hw1-1" class="headerlink" title="hw1"></a><code>hw1</code></h3><p><code>hw1</code>开发过程中，我大体上没有经历<code>debug</code>的过程。</p>
<h3 id="hw2-1"><a href="#hw2-1" class="headerlink" title="hw2"></a><code>hw2</code></h3><p><code>hw2</code>迭代过程中，我出现了两大<code>bug</code>：</p>
<ul>
<li>第一个判断两个<code>Mono</code>是否相等的方法判断出现<code>bug</code>。判断<code>Mono</code>相等的核心问题就是解决三角函数的递归比较问题。这个问题的特征是需要对两个<code>Mono</code>的三角函数内部的<code>Poly</code>进行逐一的检查，检查是否它们是不是一一对应的，而检查的方法又涉及到了对两个三角函数内部<code>Poly</code>的比较。因此在这个部分递归很容易错，由于递归比较是难免的，因此避免这方面<code>bug</code>的方法只能通过构造一些数据来进行验证正确性。</li>
<li>第二个是<code>Lexer</code>部分替换出现失误。在我的设计架构中，需要将递推表达式中的<code>f</code>替换成相应的<code>Lexer</code>。又由于地推函数具有实参，因此需要将<code>Lexer</code>中的形参转换为实参。因此我在这个地方出现了很多意想不到的问题，导致了<code>bug</code>的产生。由于这个替换<code>Lexer</code>的方法功能性比较强，因此代码量较多，导致我在<code>debug</code>的过程中产生了阻碍，进度被拖慢。可以改进的措施是对这个方法进行简化，将可以提取的方法提取出来，再对每一个方法针对性<code>debug</code>，可能效率会高一些，也能有效地降低<code>bug</code>的产生。</li>
</ul>
<h3 id="hw3-1"><a href="#hw3-1" class="headerlink" title="hw3"></a><code>hw3</code></h3><p>在<code>hw3</code>的迭代中，我主要就是一个<code>bug</code>，即在强测的时候出现了一个<code>bug</code>，就是由于三角函数内部需要是因子，因此内部是表达式形式的时候，需要将其转换为表达式因子，即套上一层括号。这个地方我错误的原因是由于我追求输出串的长度尽量短，仅仅将三角函数里面是表达式因子的情况作特判并添加括号，就是在特判的地方，我没有考虑到内部仅含多个<code>sin</code>或者<code>cos</code>的情况，导致特判出现<code>bug</code>，输出串中少了一对括号。改善的方法就是在构造测试样例的时候，尽量出一些边界样例，尽量把每一种情况都测试到。</p>
<h2 id="分析自己进行的优化"><a href="#分析自己进行的优化" class="headerlink" title="分析自己进行的优化"></a>分析自己进行的优化</h2><p>本次单元的优化主要有两点：</p>
<ul>
<li><code>hw1</code>我将多项式的输出顺序按照指数从小到大的顺序进行排列，但是这样的优化仅仅能方便<code>debug</code>，在一些最高项符号为负的情况时，输出结果会比最优结果多一个符号，因此并没有达到优化长度的效果。</li>
<li><code>hw2</code>我仅仅对<code>sin(0)=0</code>以及<code>cos(0)=1</code>这两种情况进行了优化，当三角函数内部系数为零时将此<code>Mono</code>进行替换。更进一步的简化策略在本次单元并没有实现，因为考虑到三角函数的判断过于复杂，涉及到递归，因此只能放弃进一步的优化了。</li>
</ul>
<h2 id="心得体会"><a href="#心得体会" class="headerlink" title="心得体会"></a>心得体会</h2><p>在本次的作业中，我最开始进行构建的时候真的一头雾水，在看完<code>oolens</code>中对递归下降的深度解析后才开始对这个思想有了一个初步的认识。我认为本次作业<strong>递归</strong>是一个关键词，其实考虑递归的写法不用考虑到这个函数时怎么一层一层递归下去的，只需要注意到最多两（三）层递归时怎么进行的，就可以应对任何层次的递归处理。</p>
<p>另外一个重要的体会就是我对代码的简洁性和可扩展性有了比较深刻的认识。在<code>hw1</code>写的时候，我其实并没有考虑到可扩展性，想到只是把这一次的要求完成即可，但是这样的结果就是我在<code>hw2</code>和<code>hw3</code>的时候对很多方法进行了重写和改进。在真正的实践过程中，新增的要求可能是自己无法想象的，因此代码在最开始的时候就需要对其的可扩展性进行深刻地考虑。还有代码的简洁性，代码构建过程中需要尽量增强代码的复用性，减少<code>debug</code>难度。</p>
<p>还有极其重要的就是本次作业我的代码中需要着重注意深浅拷贝问题。由于我的核心思想是<code>Lexer</code>的替换，因此在替换的过程中会时常出现更改参数的操作，而有些时候在退出递归的时候我并不需要这些参数进行改变，因此在深浅拷贝上我出现了比较多的<code>bug</code>，也进行了很多考虑，我认为这个部分也是我的架构在具体实现上的难点。</p>
]]></content>
      <tags>
        <tag>oo</tag>
      </tags>
  </entry>
  <entry>
    <title>oo-unit3</title>
    <url>/2025/05/14/oo-unit3/</url>
    <content><![CDATA[<h2 id="本单元测试过程"><a href="#本单元测试过程" class="headerlink" title="本单元测试过程"></a>本单元测试过程</h2><h3 id="对各类测试的理解与应用"><a href="#对各类测试的理解与应用" class="headerlink" title="对各类测试的理解与应用"></a>对各类测试的理解与应用</h3><p>在本单元的社交网络项目中，我们可以应用多种测试方法来保证代码的质量：</p>
<h4 id="单元测试"><a href="#单元测试" class="headerlink" title="单元测试"></a>单元测试</h4><p>这是最基础也是最重要的测试环节。它针对程序中的最小可测试单元（通常是类中的一个方法）进行验证，确保其行为符合JML规格的描述。JML的 <code>requires</code>, <code>ensures</code>, <code>assignable</code>, <code>signals</code> 等子句为单元测试用例的设计提供了直接依据。</p>
<ul>
<li><p><code>requires</code> 子句：帮助我们构造合法的输入参数和前置条件。</p>
</li>
<li><p><code>ensures</code> 子句：定义了方法执行后期望的返回结果和对象状态，是断言的主要依据。</p>
</li>
<li><p><code>assignable</code> 子句：指明了方法执行过程中哪些对象的状态可以被修改，测试时需要验证未在 <code>assignable</code> 中列出的对象状态是否保持不变 (即副作用控制)。</p>
</li>
<li><p><code>signals</code> 子句：明确了在何种条件下应抛出何种异常。测试时需要构造触发这些条件的情况，并验证是否正确抛出了预期的异常。</p>
</li>
</ul>
<h4 id="功能测试"><a href="#功能测试" class="headerlink" title="功能测试"></a>功能测试</h4><p>功能测试关注的是软件作为一个整体或特定功能模块是否满足需求规格说明中定义的功能。在本单元中，这意味着验证整个社交网络系统是否能正确处理指令文件中的各种操作，并产生预期的输出或状态变化。</p>
<ul>
<li>构造一系列指令，模拟用户在社交网络中的典型操作序列，例如：添加用户 -&gt; 建立关系 -&gt; 发送消息 -&gt; 查询信息 -&gt; 删除表情等。</li>
<li>从输入指令到最终输出（或通过查询方法验证内部状态）进行完整路径的测试。</li>
<li>确保所有JML定义的正常流程和异常流程都能按预期工作。</li>
</ul>
<h4 id="集成测试"><a href="#集成测试" class="headerlink" title="集成测试"></a>集成测试</h4><p>当多个单元（类或模块）组合在一起工作时，集成测试用于发现它们之间接口和交互的问题。在我们的项目中，<code>Network</code> 类是核心，它集成了 <code>Person</code>, <code>Tag</code>, <code>Message</code> 等多个组件。</p>
<ul>
<li><p><strong><code>Network</code> 作为集成点</strong>：大部分集成测试可以围绕 <code>Network</code> 类展开。</p>
</li>
<li><p><strong>跨类交互</strong>：</p>
<ul>
<li><p><code>sendMessage</code> (类型0): <code>Message</code> 对象创建 -&gt; <code>Person</code> (sender) <code>isLinked</code> <code>Person</code> (receiver) -&gt; <code>Person</code> 对象的 <code>socialValue</code>, <code>money</code>, <code>messages</code> 列表更新。</p>
</li>
<li><p><code>sendMessage</code> (类型1): <code>Message</code> 对象创建 -&gt; <code>Person</code> (sender) <code>containsTag</code> -&gt; <code>Tag</code> 对象 <code>getPersons</code> -&gt; 多个 <code>Person</code> 对象的 <code>socialValue</code>, <code>money</code>, <code>messages</code> 更新。</p>
</li>
</ul>
</li>
</ul>
<h4 id="压力测试"><a href="#压力测试" class="headerlink" title="压力测试"></a>压力测试</h4><p>压力测试评估系统在极端负载（如大量数据、高并发请求——本单元主要是大量数据和指令）下的稳定性和性能表现。目的是找到系统的瓶颈和失效点。</p>
<ul>
<li><strong>大数据量</strong>：构造包含大量用户（如JML中允许的最大数量）、大量关系、大量消息、大量标签的网络。</li>
<li><p><strong>高频复杂指令</strong>：执行大量查询指令（如 <code>qts</code>, <code>qcs</code>, <code>qbs</code>, <code>qsv</code>, <code>dce</code> 等），特别是那些时间复杂度较高的指令。</p>
</li>
<li><p><strong>性能瓶颈识别</strong>：通过压力测试，可以发现哪些操作（如 <code>isCircle</code> 的朴素实现、未优化的 <code>queryTripleSum</code>、大量成员的 <code>Tag</code> 的 <code>valueSum</code> 计算等）在数据量大时会超时。</p>
</li>
</ul>
<h4 id="回归测试"><a href="#回归测试" class="headerlink" title="回归测试"></a>回归测试</h4><p>当代码发生修改（如bug修复、功能增强、性能优化）后，重新运行之前的测试用例，以确保新的改动没有引入新的错误，或导致原有功能失效。</p>
<h3 id="数据构造策略"><a href="#数据构造策略" class="headerlink" title="数据构造策略"></a>数据构造策略</h3><h4 id="基于JML规格的构造"><a href="#基于JML规格的构造" class="headerlink" title="基于JML规格的构造"></a>基于JML规格的构造</h4><ul>
<li><p>边界值分析</p>
<ul>
<li>容器大小</li>
<li>ID范围</li>
<li>数值范围</li>
<li><code>limit</code> 参数</li>
</ul>
</li>
<li><p>等价类划分：将输入数据划分为若干个等价类，从每个类中选取代表性数据作为测试用例</p>
</li>
<li><p>异常条件构造：严格按照JML的 <code>signals</code> 子句构造触发异常的数据，如为<code>PersonIdNotFoundException</code> 构造一个不存在的 <code>personId</code> 作为输入，为<code>EqualRelationException</code> 构造两个已存在关系的 <code>id</code>。</p>
</li>
</ul>
<h4 id="基于社交网络特性的构造"><a href="#基于社交网络特性的构造" class="headerlink" title="基于社交网络特性的构造"></a>基于社交网络特性的构造</h4><p>图的结构，特殊状态，指令序列的特定组合等等。</p>
<h2 id="如何引导大模型"><a href="#如何引导大模型" class="headerlink" title="如何引导大模型"></a>如何引导大模型</h2><p>利用大模型辅助JML规格设计与代码实现时，关键在于<strong>有效引导</strong>。</p>
<h3 id="核心策略"><a href="#核心策略" class="headerlink" title="核心策略"></a>核心策略</h3><ul>
<li>精准提问与充足上下文：向模型清晰描述你的目标，提供所有相关的JML规格、接口定义、已有的类结构和代码片段。例如，要实现<code>Network.addRelation</code>，需给出<code>PersonInterface</code>、<code>TagInterface</code>以及该方法的完整JML。</li>
<li>任务分解：将复杂任务（如实现整个<code>Network</code>类）分解为小步骤（如实现单个方法，或方法中的某个逻辑分支）。</li>
<li>明确关键约束：在提问时，突出JML中的核心约束（如<code>pure</code>函数不能有副作用，<code>assignable</code>的精确范围）和性能要求。</li>
</ul>
<h3 id="JML特定场景引导技巧"><a href="#JML特定场景引导技巧" class="headerlink" title="JML特定场景引导技巧"></a>JML特定场景引导技巧</h3><ul>
<li>精确到子句提问，如“<code>\forall</code> 和 <code>\exists</code> 在这里如何理解？”</li>
<li>重点关注JML的副作用（<code>assignable</code>）和契约（<code>ensures</code>, <code>signals</code>），以及<code>\old()</code>表达式的正确处理。</li>
<li>明确指出期望的算法（如DFS/BFS），并提供项目中相关的数据结构和方法（如<code>Person</code>的邻接点获取方式）。</li>
<li>要求模型基于JML的各个条件分支（正常、边界、异常）提出测试场景。</li>
</ul>
<h2 id="架构设计"><a href="#架构设计" class="headerlink" title="架构设计"></a>架构设计</h2><p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/images/ae0dafd62c46cf1c6c93df1d7c782fec.png"
                      alt="image-20250514164659061"
                ></p>
<h3 id="整体架构设计"><a href="#整体架构设计" class="headerlink" title="整体架构设计"></a>整体架构设计</h3><h4 id="接口层"><a href="#接口层" class="headerlink" title="接口层"></a>接口层</h4><h4 id="实体层"><a href="#实体层" class="headerlink" title="实体层"></a>实体层</h4><ul>
<li><strong><code>Person</code></strong>: 代表社交网络中的个体用户。核心数据包括ID、姓名、年龄，以及与其他用户的关系（熟人及其关系值）、拥有的标签、消息列表、社交活跃度、金钱等。</li>
<li><strong><code>Message</code> (及其子类)</strong>: 代表用户间的交互信息。<code>Message</code>作为基类，封装了消息ID、类型（个人消息/标签消息）、发送者、社交价值等基本信息。子类如 <code>EmojiMessage</code>, <code>ForwardMessage</code>, <code>RedEnvelopeMessage</code> 则在其基础上增加了特定属性（表情ID、文章ID、红包金额）。这是一个典型的<strong>继承</strong>应用，不同消息类型共享通用属性和行为，同时具有各自特性。</li>
<li><p><strong><code>Tag</code></strong>: 代表用户标签，可以包含一组用户。核心数据包括标签ID、标签内的用户集合，以及一些统计值。</p>
</li>
<li><p><strong><code>OfficialAccount</code></strong>: 代表公众号实体。核心数据包括所有者ID、公众号ID、名称、关注者列表、发布的文章ID列表以及贡献度信息。</p>
</li>
</ul>
<h4 id="网络管理层"><a href="#网络管理层" class="headerlink" title="网络管理层"></a>网络管理层</h4><ul>
<li>这是整个社交网络的核心控制器和数据中心。它负责管理所有的实体对象（<code>Person</code>, <code>Message</code>, <code>Tag</code>, <code>OfficialAccount</code>），并处理它们之间的复杂交互和查询。</li>
</ul>
<h3 id="图模型构建与数据结构选择"><a href="#图模型构建与数据结构选择" class="headerlink" title="图模型构建与数据结构选择"></a>图模型构建与数据结构选择</h3><p>社交网络本质上是一个图，其中用户是节点 (Node/Vertex)，用户间的关系是边 (Edge)。在本次实验中，我的图模型主要体现在 <code>Person</code> 类和 <code>Network</code> 类中对这些节点和边的管理。</p>
<ul>
<li><p>节点 (Person) 的存储：在 <code>Network</code> 类中，使用了 <code>private final HashMap&lt;Integer, Person&gt; people = new HashMap&lt;&gt;();</code> 来存储所有的 <code>Person</code> 对象。</p>
<ul>
<li><code>HashMap</code> 提供了 O(1) 的平均时间复杂度来通过用户ID (<code>Integer</code>) 检索 <code>Person</code> 对象。</li>
</ul>
</li>
<li><p>边 (Relation) 的存储与表示：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> ArrayList&lt;PersonInterface&gt; acquaintance = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(); <span class="comment">// 存储熟人对象</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> ArrayList&lt;Integer&gt; value = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(); <span class="comment">// 存储对应熟人的关系值</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> HashMap&lt;Integer, Integer&gt; acquaintanceMap = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;(); <span class="comment">// 存储熟人ID到其在acquaintance列表中的索引，用于快速查找和删除</span></span><br></pre></td></tr></table></figure></div>
<ul>
<li><code>acquaintance</code> 和 <code>value</code> 两个 <code>ArrayList</code> 通过索引一一对应，可以方便地获取一个人的所有熟人及其对应的关系值。遍历所有熟人时效率尚可。</li>
<li>引入 <code>acquaintanceMap</code> 是一个关键优化。如果没有它，当需要查询特定熟人的 <code>value</code> (如 <code>queryValue(person)</code>) 或者修改/删除特定关系时，需要在 <code>acquaintance</code> 列表中进行线性搜索，时间复杂度为 O(k)（k为熟人数量）。有了 <code>acquaintanceMap</code>，可以通过熟人ID在 O(1) 时间内定位到其在列表中的索引，从而快速访问或修改其关系值，或者在删除时高效地维护列表（尽管删除本身在 <code>ArrayList</code> 中可能涉及元素移动，但定位是快速的）。</li>
</ul>
</li>
<li><p>标签 (<code>Tag</code>) 与人的关系：</p>
<ul>
<li><code>Person</code> 类中: <code>private final HashMap&lt;Integer, TagInterface&gt; tags = new HashMap&lt;&gt;();</code> 存储该用户拥有的所有标签，通过标签ID快速查找。</li>
<li><code>Tag</code> 类中: <code>private final HashSet&lt;PersonInterface&gt; persons = new HashSet&lt;&gt;();</code> 存储该标签下的所有用户。</li>
</ul>
</li>
<li><p>消息 (<code>Message</code>) 的管理</p>
</li>
<li><p>公众号 (<code>OfficialAccount</code>) 的管理：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> ArrayList&lt;PersonInterface&gt; followers = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> ArrayList&lt;Integer&gt; contributions = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> HashMap&lt;Integer, Integer&gt; followerIndexMap = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br></pre></td></tr></table></figure></div>
<ul>
<li>这与 <code>Person</code> 中管理熟人的方式类似，<code>followerIndexMap</code> 用于快速定位关注者在列表中的位置，便于更新贡献度或检查关注状态。</li>
</ul>
</li>
</ul>
<h3 id="图模型维护策略与性能优化"><a href="#图模型维护策略与性能优化" class="headerlink" title="图模型维护策略与性能优化"></a>图模型维护策略与性能优化</h3><p>动态维护图结构及其相关属性是本单元的难点，特别是在需要保证JML规格正确性的同时追求性能。</p>
<ul>
<li><p>添加/修改关系 (<code>Network.addRelation</code>, <code>Network.modifyRelation</code>)：</p>
<ul>
<li><code>Person.bestAcquaintanceDirty = true;</code>：当关系发生变化时，标记“最佳熟人”缓存失效，下次查询时重新计算，这个方法采用的是懒更新。</li>
<li><p><code>Tag.valueSumCache</code> 的维护：当 <code>addRelation</code> 或 <code>modifyRelation</code> 发生时，涉及到这两个人的标签的 <code>valueSumCache</code> 可能需要更新。我采用的策略是在<code>addRelation</code> 和 <code>modifyRelation</code>时，找出这两个人共同所属的标签，并直接调整这些标签的<code>valueSumCache</code>。</p>
</li>
<li><p><code>Network.tripleSum</code> 的维护：<code>queryTripleSum</code> 是一个典型的需要高效维护的全局状态。</p>
<ul>
<li>JML中的<code>qts</code>指令要求O(N)的复杂度，这意味着不能在查询时实时计算。</li>
<li>在 <code>addRelation(id1, id2, value)</code> 时，遍历 <code>id1</code> 的熟人 <code>p</code>，如果 <code>p</code> 同时也是 <code>id2</code> 的熟人，则形成了一个新的三元组 (<code>id1</code>, <code>id2</code>, <code>p</code>)，<code>tripleSum</code> 增加。<code>modifyRelation</code> 中删除关系时则相反。</li>
</ul>
</li>
</ul>
</li>
<li><p>查询连通性 (<code>Network.isCircle</code>)：</p>
<ul>
<li>我使用了 DFS (深度优先搜索)，从一个节点开始DFS，看是否能到达另一个节点。使用 <code>HashSet&lt;Integer&gt; visited</code> 防止重复访问和死循环。</li>
</ul>
</li>
<li><p>最短路径 (<code>Network.queryShortestPath</code>)：</p>
<ul>
<li>我使用了 BFS (广度优先搜索)，从起点开始BFS，层层扩展，最先到达终点时的层数即为最短路径长度。使用队列和 <code>visited</code> 集合。</li>
</ul>
</li>
</ul>
<h2 id="性能瓶颈剖析与优化实践"><a href="#性能瓶颈剖析与优化实践" class="headerlink" title="性能瓶颈剖析与优化实践"></a>性能瓶颈剖析与优化实践</h2><h3 id="出现过的性能问题"><a href="#出现过的性能问题" class="headerlink" title="出现过的性能问题"></a>出现过的性能问题</h3><h4 id="queryTripleSum-查询效率低下"><a href="#queryTripleSum-查询效率低下" class="headerlink" title="queryTripleSum 查询效率低下"></a><code>queryTripleSum</code> 查询效率低下</h4><ul>
<li>如果 <code>queryTripleSum</code> 采用暴力遍历所有用户三元组的方式，其复杂度将是 O(N^3) 或 O(N⋅K^2)（N为总人数，K为平均度数），在用户和关系数量较多时极易超时。JML中 <code>qts</code> 指令通常有 O(N) 或类似的性能要求。</li>
<li>修复策略：在 <code>Network</code> 类中维护一个全局变量 <code>tripleSum</code>，当 <code>addRelation(id1, id2, value)</code> 时：遍历 <code>id1</code> 的所有熟人 <code>p</code>，如果 <code>p</code> 也是 <code>id2</code> 的熟人（即 <code>p.isLinked(people.get(id2))</code>），则说明形成了一个新的三元组 (<code>id1</code>, <code>id2</code>, <code>p</code>)，此时 <code>tripleSum</code> 增加。</li>
<li><code>queryTripleSum</code> 变为 O(1) 操作。</li>
</ul>
<h4 id="Tag-getValueSum-计算效率低下"><a href="#Tag-getValueSum-计算效率低下" class="headerlink" title="Tag.getValueSum() 计算效率低下"></a><code>Tag.getValueSum()</code> 计算效率低下</h4><ul>
<li>如果每次调用 <code>getValueSum</code> 都遍历标签内所有用户对 (P_i,P_j)，并调用 <code>P_i.queryValue(P_j)</code>，复杂度将是 O(M^2⋅K)。</li>
<li>修复策略：在 <code>Tag</code> 类中引入 <code>private int valueSumCache = 0</code>，在<code>addPerson</code>，<code>delPerson</code>，<code>addRelation</code>，<code>modefyRelation</code>方法中进行动态维护。</li>
</ul>
<h4 id="Person-getBestAcquaintance-效率"><a href="#Person-getBestAcquaintance-效率" class="headerlink" title="Person.getBestAcquaintance() 效率"></a><code>Person.getBestAcquaintance()</code> 效率</h4><ul>
<li>如果每次调用都遍历所有熟人比较<code>value</code>，复杂度为 O(K)。如果此方法被高频调用，会累积不少耗时。</li>
<li>修复策略：缓存与脏位机制。</li>
</ul>
<h3 id="对规格与实现分离的理解"><a href="#对规格与实现分离的理解" class="headerlink" title="对规格与实现分离的理解"></a>对规格与实现分离的理解</h3><p>JML在本单元中扮演了“规格”的角色，而我们的Java代码则是“实现”。我对这两者分离的理解如下：</p>
<ul>
<li>契约的明确性与稳定性：规格定义了一个方法或一个类的”做什么“，实现关注的是“如何做”。</li>
<li>规格与实现分离的好处：<ul>
<li>设计与实现分离：可以先专注于通过JML定义清晰、无歧义的系统行为，而不必过早陷入实现细节。</li>
<li>并行开发与团队协作：一旦规格确定，不同的开发者可以依据规格独立实现不同的模块，或者对同一模块进行不同的实现尝试。</li>
<li>可替换性与可维护性：只要实现严格遵守规格，我们就可以在不影响系统其他部分的前提下，替换掉某个模块的旧实现（比如为了性能优化），或者修改其内部实现细节。</li>
</ul>
</li>
</ul>
<h2 id="JML规格驱动的JUnit测试设计与一致性检验"><a href="#JML规格驱动的JUnit测试设计与一致性检验" class="headerlink" title="JML规格驱动的JUnit测试设计与一致性检验"></a>JML规格驱动的JUnit测试设计与一致性检验</h2><h3 id="如何利用JML规格信息设计JUnit测试"><a href="#如何利用JML规格信息设计JUnit测试" class="headerlink" title="如何利用JML规格信息设计JUnit测试"></a>如何利用JML规格信息设计JUnit测试</h3><ul>
<li><strong>前置条件 (<code>requires</code>) → 构造测试输入</strong>：<code>requires</code>定义了方法调用前必须满足的状态。用它来准备合法的测试数据和场景。</li>
<li><p><strong>后置条件 (<code>ensures</code>) → 验证测试结果</strong>：</p>
<ul>
<li><code>ensures</code>描述了方法成功执行后的预期结果（返回值 <code>\result</code>）和对象状态变化。这是JUnit <code>assert</code>语句的依据。</li>
<li><code>\old(expr)</code>：表示<code>expr</code>在方法执行前的值。测试时需记录旧值，调用方法后用旧值验证<code>ensures</code>中断言。</li>
</ul>
</li>
<li><p><strong>副作用约束 (<code>assignable</code> / <code>pure</code>) → 检查状态修改范围</strong>：</p>
<ul>
<li><code>assignable</code>列出了方法能修改的状态。测试要验证这些状态按预期改变，且其他无关状态不变。</li>
<li><code>pure</code>方法（即<code>assignable \nothing</code>）不应修改任何状态。测试需验证调用前后对象状态完全一致。</li>
</ul>
</li>
</ul>
<h3 id="效果"><a href="#效果" class="headerlink" title="效果"></a>效果</h3><ul>
<li>测试直指JML定义的每个细节。</li>
<li>JML帮助考虑边界、正常、异常情况，测试更全面。</li>
</ul>
<h2 id="学习体会"><a href="#学习体会" class="headerlink" title="学习体会"></a>学习体会</h2><p>本单元的学习让我深刻体会到JML规格在软件开发中的重要性。通过精确的形式化语言定义接口行为，JML为代码实现提供了清晰的“契约”，减少了理解偏差。初期，阅读和理解复杂的JML表达式，如<code>\forall</code>和<code>\old</code>，确有挑战，但一旦掌握，便能准确把握方法的功能边界和状态变化。</p>
<p>最大的收获在于理解“规格与实现分离”。JML仅定义“做什么”，给予实现“如何做”的极大自由。这促使我不仅要保证功能正确，更要思考数据结构和算法选择以优化性能，如通过增量维护<code>tripleSum</code>和<code>valueSumCache</code>来避免超时。同时，JML也直接指导了JUnit测试用例的设计，特别是针对边界条件和异常情况的测试，有效提升了代码的健壮性。这次经历让我认识到，<strong>严谨的规格是构建高质量、可维护软件系统的坚实基础</strong>。</p>
]]></content>
      <tags>
        <tag>oo</tag>
      </tags>
  </entry>
  <entry>
    <title>oo-unit4</title>
    <url>/2025/06/11/oo-unit4/</url>
    <content><![CDATA[<h2 id="正向建模与开发"><a href="#正向建模与开发" class="headerlink" title="正向建模与开发"></a>正向建模与开发</h2><blockquote>
<p>正向建模指的是从高层设计模型开始，逐步细化并生成或指导代码实现的过程。</p>
</blockquote>
<p>这个单元的正向建模主要体现在以下几个方面：</p>
<h3 id="需求分析与抽象"><a href="#需求分析与抽象" class="headerlink" title="需求分析与抽象"></a>需求分析与抽象</h3><p>首先需要从图书馆管理系统的需求描述开始，分析出系统中的主要实体（如图书、用户、书架、图书馆各部门）及其之间的关系。这对应了面向对象思想中的<strong>抽象</strong>过程，将现实世界的复杂问题简化为模型中的概念。</p>
<h3 id="UML建模"><a href="#UML建模" class="headerlink" title="UML建模"></a>UML建模</h3><p>这个单元指导我们在代码实现之前，通过绘制UML图来指导设计。</p>
<ul>
<li><p>类图</p>
<ul>
<li><p>类图是通过设计类、属性、方法以及类之间的关系（如关联、继承、实现），来构建系统的静态结构。</p>
</li>
<li><p><strong>类图是正向建模的核心</strong>。</p>
</li>
</ul>
</li>
</ul>
<ul>
<li>状态图<ul>
<li>描述了单一对象在其生命周期中可能经历的所有状态以及导致状态转换的事件和条件。</li>
</ul>
</li>
</ul>
<ul>
<li>顺序图<ul>
<li>展示了对象之间如何通过消息传递或者方法调用来完成特定功能或用例的时序关系。</li>
</ul>
</li>
</ul>
<h3 id="代码实现以及一致性检查"><a href="#代码实现以及一致性检查" class="headerlink" title="代码实现以及一致性检查"></a>代码实现以及一致性检查</h3><ul>
<li>依据UML图的设计，逐步编写Java代码。</li>
<li>在编写代码过程中，肯定会对最开始设计的UML图进行必要的调整和更新，来保持模型与自己实现的代码同步</li>
</ul>
<h2 id="架构设计以及代码设计与UML设计的追踪关系"><a href="#架构设计以及代码设计与UML设计的追踪关系" class="headerlink" title="架构设计以及代码设计与UML设计的追踪关系"></a>架构设计以及代码设计与UML设计的追踪关系</h2><h3 id="架构设计总结"><a href="#架构设计总结" class="headerlink" title="架构设计总结"></a>架构设计总结</h3><h4 id="核心实体"><a href="#核心实体" class="headerlink" title="核心实体"></a>核心实体</h4><ul>
<li><p><code>BookDetails</code>：表示图书的抽象信息如ISBN、类别、是否热门等。</p>
</li>
<li><p><code>BookCopy</code>：表示具体的图书副本，封装了其唯一的书号、当前位置、借阅/预约状态、日期信息等。</p>
</li>
<li><code>Student</code>：表示用户，管理其信用分、当前借阅/阅读/预约状态以及处理各类请求的入口方法。</li>
</ul>
<h4 id="物理位置"><a href="#物理位置" class="headerlink" title="物理位置"></a>物理位置</h4><ul>
<li><code>Shelf</code> 接口：定义了书架的基本操作（添加、移除、查找、获取所有副本）。其中<code>NormalBookshelf</code> 和 <code>HotBookshelf</code>分别实现了 <code>Shelf</code> 接口，分别管理普通图书和热门图书。</li>
<li><code>BorrowReturnOffice</code>：模拟借还处，负责暂时存放待归还或待整理的图书。</li>
<li><code>AppointmentOffice</code>：模拟预约处，管理被预约并送达的图书副本，并处理逾期预约书。</li>
<li><code>ReadingRoom</code>：模拟阅览室，管理正在被用户阅读的图书。</li>
</ul>
<h4 id="业务逻辑"><a href="#业务逻辑" class="headerlink" title="业务逻辑"></a>业务逻辑</h4><ul>
<li><code>BorrowLibrarian</code>：处理借书和还书请求，包括各种借阅限制和信用分计算。</li>
<li><code>OrderLibrarian</code>：处理预约和取书请求，包括预约限制和预约书的流转。</li>
<li><code>ReadingLibrarian</code>：处理阅读和阅读归还请求，包括阅读限制。</li>
<li><code>ArrangeLibrarian</code>：负责图书馆的整理工作，包括每日开馆/闭馆时的图书整理和信用分计算。</li>
</ul>
<h4 id="协调"><a href="#协调" class="headerlink" title="协调"></a>协调</h4><ul>
<li><code>SelfMachine</code>：作为学生与图书馆各项业务处理的代理，将学生的请求分发给相应的 <code>Librarian</code> 类进行处理。这种设计降低了 <code>Student</code> 类与具体 <code>Librarian</code> 类之间的耦合。</li>
<li><code>StudentTable</code>：管理所有学生对象，提供根据ID获取或注册学生的功能。</li>
<li><code>LibrarySystem</code>：整个图书馆系统的入口，负责初始化图书、处理所有输入命令，并协调各个 <code>Librarian</code> 和物理位置对象的工作。</li>
</ul>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/images/image-20250611112902897.png"
                      alt="image-20250611112902897"
                ></p>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="/images/image-20250611113013658.png"
                      alt="image-20250611113013658"
                ></p>
<h3 id="代码设计与UML模型设计之间的追踪关系"><a href="#代码设计与UML模型设计之间的追踪关系" class="headerlink" title="代码设计与UML模型设计之间的追踪关系"></a>代码设计与UML模型设计之间的追踪关系</h3><h4 id="类与对象映射"><a href="#类与对象映射" class="headerlink" title="类与对象映射"></a>类与对象映射</h4><ul>
<li>UML类图中的“类”，属性，方法，接口均与代码中的一一对应</li>
</ul>
<h4 id="关系映射"><a href="#关系映射" class="headerlink" title="关系映射"></a>关系映射</h4><ul>
<li>关联：<ul>
<li><strong>单向关联</strong>: 例如 <code>Student</code> 类持有 <code>SelfMachine</code> 对象的引用 (<code>private SelfMachine selfMachine;</code>)，这在UML类图中会表现为从 <code>Student</code> 到 <code>SelfMachine</code> 的单向关联。</li>
<li><strong>聚合</strong>: 表示整体与部分的关系，但部分可以独立存在。</li>
<li><strong>组合</strong>: 体现在构造函数中对部分对象的创建和生命周期管理上。</li>
</ul>
</li>
<li>继承：UML中会用实线带空心三角箭头表示，代码中则通过 <code>extends</code> 关键字实现。</li>
</ul>
<h4 id="行为与状态映射"><a href="#行为与状态映射" class="headerlink" title="行为与状态映射"></a>行为与状态映射</h4><ul>
<li>这一点主要体现在状态图和顺序图。这两类图的概念和作用在上面已经介绍过，这里就不过多赘述。</li>
</ul>
<h2 id="大模型辅助正向建模"><a href="#大模型辅助正向建模" class="headerlink" title="大模型辅助正向建模"></a>大模型辅助正向建模</h2><ul>
<li><p>在本次的单元中，我通过使用大模型了解怎么画UML类图，理解关联、聚合、组合等具体的概念以及如何判断类和类之间的具体关系。在生成UML类图的参考代码的时候，大模型没有给出我<code>.mdj</code>的文件代码，而是给了我<code>.puml</code>代码，也就是plantUML的代码。<br>个人感觉<code>.puml</code>代码会比<code>.mdj</code>代码更能专注到系统的结构上，而不是一个一个添加属性与方法，而是通过代码的方式来进行类图的绘制。于是，我本单元采取了大模型的建议，先使用plantUML进行初步建模，再根据这个类图进行代码的书写，最后反向优化类图之后才把类图绘制到<code>.mdj</code>文件里面的。</p>
</li>
<li><p>还有就是大模型最后不要用来进行代码整体架构的建立，因为大模型生成架构的时候，经常会不能分清楚用户的需求，会根据用户的一些要求添加一些冗余的一些类，而更难做到在原有给的架构上进行优化，也就是大模型善于“加”东西，而不是“减”东西，最终导致架构的臃肿。</p>
</li>
</ul>
<h2 id="思维演进"><a href="#思维演进" class="headerlink" title="思维演进"></a>思维演进</h2><h3 id="架构设计思维的演进"><a href="#架构设计思维的演进" class="headerlink" title="架构设计思维的演进"></a>架构设计思维的演进</h3><h4 id="第一单元——层次化设计与初步抽象"><a href="#第一单元——层次化设计与初步抽象" class="headerlink" title="第一单元——层次化设计与初步抽象"></a>第一单元——层次化设计与初步抽象</h4><h5 id="设计思路"><a href="#设计思路" class="headerlink" title="设计思路"></a>设计思路</h5><p>本单元我的核心思路是<code>String -&gt; Lexer -&gt; Parser -&gt; Poly -&gt; String</code>。但 <code>Poly</code> 类内部（尤其是 <code>Mono</code>）最初可能更多地是基于字符串或简单数据类型（如 <code>HashMap&lt;String, Integer&gt;</code> 表示 <code>x</code> 的幂来存储和操作表达式的不同部分。自定义递归函数部分，我采用的是 <code>token</code> 流的替换，这是一种在文本层面解决递归调用的策略，而非在对象层面进行递归的构建和求值。</p>
<h5 id="设计局限"><a href="#设计局限" class="headerlink" title="设计局限"></a>设计局限</h5><ul>
<li><p><strong>功能扩展困难</strong>：当 <code>Mono</code> 内部的结构（三角函数的参数）本身也是一个“因子”时，原有的简单 <code>HashMap</code> 结构难以直接应对这种递归嵌套。为了合并相同的 <code>Mono</code>，我采取了“递归的比较思路，即若三角函数因子存在的话，就对三角函数里面的因子进行比较，通过逐步比较系数，自变量指数，三角函数相等，从而一直递归到三角函数的因子内部没有三角函数为止，从而结束递归比较”。 这种复杂且需要大量递归比较的逻辑，正是由于内部结构没有被彻底对象化所致。</p>
</li>
<li><p><strong>“大类”与职责混淆</strong>： <code>Lexer</code> 部分的替换功能性强、代码量较多，导致调试困难。 这表明我的 <code>Lexer</code> 不仅负责词法分析，还承担了复杂的“宏展开”或“替换”逻辑，使得单个方法的职责过于庞大，难以维护。</p>
</li>
</ul>
<h5 id="思维演进-1"><a href="#思维演进-1" class="headerlink" title="思维演进"></a>思维演进</h5><ul>
<li><strong>理解了抽象的深化</strong>：认识到“因子”内部可以包含更复杂的“表达式”结构。例如，<code>sin(Poly)</code> 中的 <code>Poly</code> 本身就是一个完整的表达式。这意味着 <code>Factor</code> 不再仅仅是 <code>x</code> 或简单的三角函数项，而是一个可以包含其他 <code>Poly</code> 对象的抽象概念。这可以促使思考如何用对象来表示表达式的<strong>递归嵌套</strong>特性。</li>
<li><strong>组合构建树形结构</strong>：自然而然地思考如何通过对象之间的<strong>组合关系</strong>来表示表达式的层次结构，即构建<strong>表达式树</strong>。</li>
<li><strong>多态统一行为</strong>：理解到对于表达式的各种统一操作（如求导、化简或求值），可以通过<strong>多态</strong>机制来极大地简化和统一处理逻辑。</li>
</ul>
<h4 id="第二单元——并发控制与职责分离"><a href="#第二单元——并发控制与职责分离" class="headerlink" title="第二单元——并发控制与职责分离"></a>第二单元——并发控制与职责分离</h4><h5 id="设计思路-1"><a href="#设计思路-1" class="headerlink" title="设计思路"></a>设计思路</h5><ul>
<li><p>设计了 <code>InputThread</code> (请求接收)、<code>Schedule</code> (请求分配)、<code>ElevatorThread</code> (电梯运输) 三种线程。</p>
</li>
<li><p>对于涉及共享数据（如请求表 <code>RequestTable</code>）的访问，运用 <code>synchronized</code> 关键字进行同步，对线程安全有了初步的认识，并尝试通过同步机制来管理共享资源的访问，应对多线程环境下的潜在问题。</p>
</li>
</ul>
<h5 id="设计局限-1"><a href="#设计局限-1" class="headerlink" title="设计局限"></a>设计局限</h5><ul>
<li><strong>并发安全问题与数据一致性挑战</strong>：对共享状态（如乘客请求的接收状态）的并发访问可能会导致不确定的行为。</li>
<li><strong>调试复杂性</strong>：多线程环境下非确定性行为对问题定位和复现带来的巨大挑战。</li>
</ul>
<h5 id="思维演进-2"><a href="#思维演进-2" class="headerlink" title="思维演进"></a>思维演进</h5><ul>
<li><strong>线程安全的核心性与深度理解</strong>：在多线程环境中，对任何<strong>共享资源</strong>（如 <code>RequestTable</code>、电梯内部乘客列表、电梯状态、配对电梯状态）的访问都必须通过<strong>精确的同步机制</strong>来保护，以确保数据的一致性和操作的原子性。</li>
</ul>
<h4 id="第三单元——契约式编程与高效数据结构选择"><a href="#第三单元——契约式编程与高效数据结构选择" class="headerlink" title="第三单元——契约式编程与高效数据结构选择"></a>第三单元——契约式编程与高效数据结构选择</h4><h5 id="设计思路-2"><a href="#设计思路-2" class="headerlink" title="设计思路"></a>设计思路</h5><ul>
<li>在第一次接触JML规格时，倾向于直接将JML的描述“翻译”成Java代码。对于对象状态的维护，可能更多地是在方法执行时进行实时计算，而非考虑增量维护或缓存机制。</li>
</ul>
<h5 id="设计局限-2"><a href="#设计局限-2" class="headerlink" title="设计局限"></a>设计局限</h5><ul>
<li><strong>性能瓶颈</strong>：仅仅根据JML的功能描述进行直译式的实现，往往无法满足实际的性能要求（如 <code>qts</code> 指令的 <code>O(N)</code> 复杂度）。</li>
</ul>
<h5 id="思维演进-3"><a href="#思维演进-3" class="headerlink" title="思维演进"></a>思维演进</h5><ul>
<li><p><strong>契约式编程 </strong>：通过JML规格，您深刻理解了软件模块之间（或方法与调用者之间）的契约关系。<code>requires</code> 定义了调用方法的“前提”，<code>ensures</code> 定义了方法执行后的“结果保证”，<code>assignable</code> 明确了方法可以修改的“范围”，<code>signals</code> 则规定了在特定异常条件下的“行为”。</p>
</li>
<li><p><strong>规格与实现分离</strong>：JML只定义了“做什么”（功能和行为），而没有规定“怎么做”（具体实现细节）</p>
</li>
</ul>
<h4 id="第四单元——程序架构的设计与抽象以及UML"><a href="#第四单元——程序架构的设计与抽象以及UML" class="headerlink" title="第四单元——程序架构的设计与抽象以及UML"></a>第四单元——程序架构的设计与抽象以及UML</h4><p>这一部分上面讲的比较清楚了，就不再赘述了。</p>
<h3 id="测试思维的演进"><a href="#测试思维的演进" class="headerlink" title="测试思维的演进"></a>测试思维的演进</h3><h5 id="第一单元"><a href="#第一单元" class="headerlink" title="第一单元"></a>第一单元</h5><p>手动构造少量包含各种操作符和嵌套层数的表达式字符串作为输入，然后人工比对程序的输出结果。</p>
<h5 id="第二单元"><a href="#第二单元" class="headerlink" title="第二单元"></a>第二单元</h5><p>多线程引入了极大的复杂性，要考虑并发安全和运行时行为，这个单元我使用了评测机来构造极端并发场景（如大量请求瞬间涌入、请求类型高度重复、临界点请求）来进行压力测试。</p>
<h5 id="第三单元"><a href="#第三单元" class="headerlink" title="第三单元"></a>第三单元</h5><p>开始掌握规格驱动测试，严格依据JML定义设计单元测试和异常测试，并利用 <code>JUnit</code> 等工具实现自动化。</p>
<h5 id="第四单元"><a href="#第四单元" class="headerlink" title="第四单元"></a>第四单元</h5><p>设计能触发复杂规则的数据，并通过日志分析等方法，确保系统在长期运行中的鲁棒性。</p>
<h2 id="课程收获"><a href="#课程收获" class="headerlink" title="课程收获"></a>课程收获</h2><p>这个学期的面向对象课终于结束了……就我现在的感觉来说，只有深深的解脱。我认为oo绝对是我到现在以来花时间最长的一门课，我相信后面可能也不会再有来者了。</p>
<p>回想我每周因为oo坐在电脑面前苦思冥想bug到底在哪里，一坐就是五六个小时，意识到的时候已经腰酸背痛的时光；<br>回想弱测的时候出现<code>WA</code>，点进去看具体样例，发现是一片空白，只能心里暗骂，却又无可奈何，对着代码干瞪眼，不知道从何入手的时光；<br>回想我第3次作业bug直到周六下午都没有找出来，坐在三号楼手足无措，只能一遍遍点着调试的“下一步”，时不时点开属性页面看能不能对上正确值的时光；<br>回想我每一次互测拿着评测机，苦思冥想造点造不出来，自己却被人刀了十多刀的时光……我绝对不会说这门课是一门很轻松很有趣的课。</p>
<p>但是，每当我沉浸其中，一步步把要求从抽象的想法变成结构清晰的代码的时候；<br>每当我检查代码，发现bug，运行样例，惊喜的发现运行出来正确答案的时候；<br>每当我紧张地点击提交，页面里出现了十个<code>accept</code>的时候；<br>每当我周日清晨打开互测，不是“很抱歉，你未能成功入围本次互测。我们鼓励你先掌握基础内容，为下一次的互测做准备”，而是显示互测页面，而且看到自己在A房的时候；<br>每当研讨课上，我不再像以前一样跟别人讨论的时候没有观点，而是能自信的分享自己代码架构，集思广益的时候，我总是觉得，oo这门课也总会给人带来一些小确幸的。</p>
<p>oo，实话说，你真的让我成长了很多，一次次迭代让我构建起面向对象的思想，让我有从容和信心从零开始面对一个项目，一次次研讨课让我也能敢于发表自己的观点，一次次<code>WA</code>磨平了我的锐气和棱角（bushi)。</p>
<blockquote>
<p>谢谢你们，oo课的老师们，谢谢你们，oo的助教们，谢谢你，oo。</p>
</blockquote>
]]></content>
      <tags>
        <tag>oo</tag>
      </tags>
  </entry>
  <entry>
    <title>lab6-shell</title>
    <url>/2025/06/28/lab6-shell/</url>
    <content><![CDATA[<h2 id="任务内容"><a href="#任务内容" class="headerlink" title="任务内容"></a>任务内容</h2><h3 id="相对路径-新增指令"><a href="#相对路径-新增指令" class="headerlink" title="相对路径+新增指令"></a>相对路径+新增指令</h3><p><strong>实现思路</strong>：<br>内建指令，如 <code>cd</code> 和 <code>exit</code>，因其需要直接修改 Shell 进程自身的状态而被内建；外部指令，如 <code>touch</code>、<code>mkdir</code> 和 <code>rm</code>，则作为独立的可执行程序存在，由 Shell 创建子进程来运行。</p>
<h4 id="1-内建指令-Built-in-Commands"><a href="#1-内建指令-Built-in-Commands" class="headerlink" title="1. 内建指令 (Built-in Commands)"></a>1. 内建指令 (Built-in Commands)</h4><p><strong><code>cd</code> 与 <code>pwd</code>：当前工作目录管理</strong></p>
<p>这两个指令是相对路径功能的核心。其实现依赖于在内核中为每个进程（<code>struct Env</code>）增加一个指向当前目录文件结构（<code>struct File</code>）的指针 <code>env_cwd</code>。</p>
<ul>
<li><p><strong><code>cd</code> 的实现核心</strong>：<code>cd</code> 命令的主要工作是解析用户传入的路径参数，并调用一个特定的系统调用（如 <code>chdir</code>）来请求内核更新当前进程的 <code>env_cwd</code> 指针。</p>
<div class="code-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span> <span class="title function_">builtin_cd</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="comment">// ... (省略参数解析和路径处理的代码) ...</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 核心：调用系统调用，请求内核改变当前工作目录</span></span><br><span class="line">    <span class="keyword">if</span> (chdir(resolved_path) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;cd: directory not found: %s\n&quot;</span>, path);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>; <span class="comment">// 失败，返回非零值</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>; <span class="comment">// 成功</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong><code>pwd</code> 的实现核心</strong>：<code>pwd</code> 命令则通过一个系统调用（如 <code>getcwd</code>）来从内核获取当前进程 <code>env_cwd</code> 所对应的完整路径字符串，然后将其打印到标准输出。</p>
<div class="code-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span> <span class="title function_">builtin_pwd</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="type">char</span> current_path_buf[MAXPATHLEN];</span><br><span class="line">    <span class="keyword">if</span> (getcwd(current_path_buf, <span class="keyword">sizeof</span>(current_path_buf)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, current_path_buf);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
</ul>
<p><strong><code>exit</code>：退出 Shell</strong></p>
<p><code>exit</code> 必须是内建指令，因为它需要终止的是 Shell 进程本身。其实现非常直接，就是调用 <code>exit()</code> 库函数，该函数会触发系统调用来销毁当前的进程环境。</p>
<h4 id="2-外部命令-External-Commands"><a href="#2-外部命令-External-Commands" class="headerlink" title="2. 外部命令 (External Commands)"></a>2. 外部命令 (External Commands)</h4><p>这些命令作为独立的可执行文件存放在文件系统中。Shell 通过 <code>fork()</code> 和 <code>spawn()</code> 来创建子进程运行它们。这些命令实现的关键在于正确地使用文件系统相关的系统调用，并在执行失败时，从其 <code>main</code> 函数<strong>返回一个非零的退出码</strong>，以便 Shell 的 <code>&amp;&amp;</code> 和 <code>||</code> 能够正确工作。</p>
<ul>
<li><p><strong><code>touch</code> 的实现核心</strong>：使用 <code>open()</code> 系统调用，并组合使用 <code>O_CREAT</code>（不存在则创建）和 <code>O_EXCL</code>（已存在则报错）标志位，来实现“若文件不存在则创建”的逻辑。如果 <code>open</code> 因为文件已存在而失败（返回 <code>-E_FILE_EXISTS</code>），则将其视为成功（返回 <code>0</code>）。</p>
</li>
<li><p><strong><code>mkdir</code> 的实现核心</strong>：</p>
<ul>
<li>不带 <code>-p</code> 时，直接使用 <code>open()</code> 和 <code>O_MKDIR</code> 标志创建目录。</li>
<li>带 <code>-p</code> 时，需要一个额外的辅助函数。该函数会逐级解析路径字符串（例如，将 <code>/a/b/c</code> 分解为 <code>/a</code>, <code>/a/b</code>, <code>/a/b/c</code>），并对每一级路径尝试调用 <code>open(path, O_MKDIR)</code>，同时忽略“文件已存在”（<code>-E_FILE_EXISTS</code>）的错误，从而实现递归创建。</li>
</ul>
</li>
<li><p><strong><code>rm</code> 的实现核心</strong>：</p>
<ul>
<li>首先使用 <code>stat()</code> 系统调用来判断目标路径是一个文件还是一个目录。</li>
<li>如果目标是文件，直接调用 <code>remove()</code> 系统调用删除。</li>
<li>如果目标是目录，则检查是否提供了 <code>-r</code> 选项。若没有，则报错“Is a directory”；若有，则进入递归删除流程：打开目录，读取目录下的所有条目，并对每一个条目（除了 <code>.</code> 和 <code>..</code>）递归地调用 <code>rm -r</code>。</li>
</ul>
</li>
</ul>
<h3 id="变量和注释"><a href="#变量和注释" class="headerlink" title="变量和注释"></a>变量和注释</h3><h4 id="1-变量的设置、展开与传递"><a href="#1-变量的设置、展开与传递" class="headerlink" title="1. 变量的设置、展开与传递"></a>1. 变量的设置、展开与传递</h4><p>这套机制能够区分对当前 Shell 生效的“局部变量”和能够被子进程继承的“环境变量”，并通过 <code>declare</code> 和 <code>unset</code> 指令进行管理。</p>
<ul>
<li><p><strong>变量的存储与管理</strong></p>
<p>所有变量都存储在主 Shell 进程内存中的一个 <code>struct Variable</code> 数组里。这个结构体不仅记录了变量的键和值，还通过一个 <code>flags</code> 字段来标记其属性（环境变量或只读）。</p>
<div class="code-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Variable</span> &#123;</span></span><br><span class="line">    <span class="type">char</span> name[<span class="number">17</span>];</span><br><span class="line">    <span class="type">char</span> value[<span class="number">17</span>];</span><br><span class="line">    <span class="type">int</span> flags; <span class="comment">// 用位掩码来存储属性</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用宏定义来表示不同的属性</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> VAR_FLAG_EXPORT   0x1  <span class="comment">// 标志该变量是一个环境变量</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> VAR_FLAG_READONLY 0x2  <span class="comment">// 标志该变量是只读的</span></span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong><code>declare</code> 和 <code>unset</code> 指令的实现</strong></p>
<p><code>declare</code> 指令用于创建和修改变量，它会解析 <code>-x</code> 和 <code>-r</code> 选项来设置变量的 <code>flags</code>。<code>unset</code> 指令则负责从内存数组中安全地（检查只读属性）移除变量。</p>
</li>
<li><p><strong><code>$</code> 变量展开与环境变量传递</strong></p>
<p>在命令执行前，<code>expand_variables</code> 函数会负责将命令中的 <code>$VAR</code> 字符串替换为其实际的值。而在创建子进程时，<code>runcmd</code> 函数会精心挑选出所有被标记为环境变量的变量，并将它们传递给 <code>spawn</code> 系统调用，从而实现环境变量的继承。</p>
<div class="code-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">runcmd</span><span class="params">(<span class="type">char</span> *s)</span> &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="type">static</span> <span class="class"><span class="keyword">struct</span> <span class="title">Variable</span> *<span class="title">exported_vars</span>[<span class="title">MAX_VARIABLES</span> + 1];</span></span><br><span class="line">    <span class="type">int</span> export_count = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 遍历内存中的所有变量</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; num_variables; i++) &#123;</span><br><span class="line">        <span class="comment">// 2. 如果变量有 VAR_FLAG_EXPORT 标志</span></span><br><span class="line">        <span class="keyword">if</span> (variable_pool[i].flags &amp; VAR_FLAG_EXPORT) &#123;</span><br><span class="line">            <span class="comment">// 3. 将其加入到待传递的数组中</span></span><br><span class="line">            exported_vars[export_count++] = &amp;variable_pool[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    exported_vars[export_count] = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4. 将只包含环境变量的数组传递给 spawn</span></span><br><span class="line">    <span class="type">int</span> child = spawn(argv[<span class="number">0</span>], argv, (<span class="type">char</span> **)exported_vars);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
</ul>
<h4 id="2-注释功能"><a href="#2-注释功能" class="headerlink" title="2. 注释功能 (#)"></a>2. 注释功能 (<code>#</code>)</h4><p><strong>实现思路</strong>：<br>注释功能允许用户在命令后添加笔记，Shell 在执行时会自动忽略注释部分。我的实现非常直接，就是在命令执行前的最后一刻，对命令字符串进行处理。</p>
<ul>
<li><p><strong>注释的实现核心</strong>：<br>在 Shell 的主执行循环中，当获取到一条完整的、即将要被送去执行的命令字符串后（此时变量和反引号等已经处理完毕），我会使用 <code>strchr</code> 函数来查找字符串中第一次出现 <code>#</code> 的位置。</p>
<div class="code-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ... (前续处理) ...</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 假设 &#x27;p&#x27; 是指向当前待执行命令的字符串指针</span></span><br><span class="line"><span class="type">char</span> *comment_start = <span class="built_in">strchr</span>(p, <span class="string">&#x27;#&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果找到了 &#x27;#&#x27;</span></span><br><span class="line"><span class="keyword">if</span> (comment_start != <span class="literal">NULL</span>) &#123;</span><br><span class="line">    <span class="comment">// 直接在 &#x27;#&#x27; 的位置插入一个字符串结束符 &#x27;\0&#x27;</span></span><br><span class="line">    <span class="comment">// 这样，后续的所有函数都会认为字符串在这里就结束了，</span></span><br><span class="line">    <span class="comment">// # 后面的所有内容自然就被忽略了。</span></span><br><span class="line">    *comment_start = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 执行被截断后的命令 p</span></span><br><span class="line">exec_line(p);</span><br></pre></td></tr></table></figure></div>
</li>
</ul>
<h3 id="指令输入优化和历史指令"><a href="#指令输入优化和历史指令" class="headerlink" title="指令输入优化和历史指令"></a>指令输入优化和历史指令</h3><h4 id="1-指令自由输入与快捷键"><a href="#1-指令自由输入与快捷键" class="headerlink" title="1. 指令自由输入与快捷键"></a>1. 指令自由输入与快捷键</h4><p>这个功能的核心，是将 <code>readline</code> 函数从一个简单的字符读取器，改造成一个迷你的行编辑器。</p>
<ul>
<li><p><strong>实现原理</strong>：<br>我将 <code>readline</code> 设计成一个无限循环，在循环中通过 <code>read(0, &amp;ch, 1)</code> 一次只读取一个字符。我使用一个字符数组 <code>buf[]</code> 来存储当前正在编辑的命令行，一个整型变量 <code>len</code> 记录其长度，另一个整型变量 <code>pos</code> 则作为光标，追踪当前的编辑位置。</p>
<p><code>readline</code> 内部通过一个大的 <code>switch (ch)</code> 结构，来判断用户的按键并执行相应操作：</p>
<ul>
<li><strong>普通字符</strong>：通过 <code>memmove</code> 将光标位置（<code>pos</code>）后的所有字符向后移动一位，为新字符腾出空间，然后将新字符插入，并更新 <code>len</code> 和 <code>pos</code>。</li>
<li><strong>退格键 (<code>Backspace</code>)</strong>：同样使用 <code>memmove</code>，将光标位置前的字符覆盖掉，实现删除。</li>
<li><strong>快捷键</strong>：对于 <code>Ctrl-A</code> (光标到行首) 或 <code>Ctrl-E</code> (光标到行尾) 等快捷键，实现起来相对简单，只需直接修改 <code>pos</code> 的值即可。对于 <code>Ctrl-K</code> (删除到行末) 或 <code>Ctrl-U</code> (删除到行首) 等，则通过字符串操作和 <code>memmove</code> 来实现。</li>
<li><strong>光标移动 (方向键)</strong>：方向键会产生特殊的转义序列（如 <code>\x1b[C</code> 代表右箭头）。我的 <code>switch</code> 会先捕捉 <code>\x1b</code>，然后再继续读后续的字符来判断具体是哪个方向键，最终通过增减 <code>pos</code> 的值来实现光标的移动。</li>
</ul>
</li>
<li><p><strong>屏幕刷新</strong>：<br>每次按键导致 <code>buf</code> 或 <code>pos</code> 发生变化后，都需要刷新屏幕上的显示。我是通过打印一系列 ANSI 转义序列来完成的：先用 <code>\r</code> 回到行首，用 <code>\x1b[J</code> 清除当前行，然后打印新的 <code>buf</code> 内容，最后再用 <code>\r</code> 和一系列 <code>\x1b[C</code> (光标右移) 将光标精确定位到 <code>pos</code> 所在的新位置。</p>
</li>
</ul>
<h4 id="2-命令历史-history-与上下导航"><a href="#2-命令历史-history-与上下导航" class="headerlink" title="2. 命令历史 (history 与上下导航)"></a>2. 命令历史 (<code>history</code> 与上下导航)</h4><ul>
<li><p><strong>实现原理</strong>：<br>命令历史的实现分为内存、文件和用户交互三个部分。</p>
<ul>
<li><p><strong>内存存储</strong>：我在 Shell 中设置了一个二维字符数组 <code>history_lines[MAX_HISTORY][MAX_CMD_LEN]</code> 作为一个环形缓冲区。<code>history_head</code> 指针用于标记下一次新命令应该插入的位置，<code>history_count</code> 则记录当前有效的历史数量。</p>
</li>
<li><p><strong>持久化存储</strong>：Shell 启动时，<code>history_load</code> 函数会尝试打开根目录下的 <code>.mos_history</code> 文件，读取其中的内容来预加载历史记录。当用户执行一条新命令后，<code>history_add</code> 函数会先将其存入内存的环形缓冲区，然后立即将内存中全部的有效历史记录覆盖写入到 <code>.mos_history</code> 文件中，从而实现历史记录的持久化。</p>
<div class="code-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">history_add</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *cmd)</span> &#123;</span><br><span class="line">    <span class="comment">// ... (省略空命令和重复命令的判断) ...</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 将新命令存入内存中的环形数组</span></span><br><span class="line">    <span class="built_in">strcpy</span>(history_lines[history_head], cmd);</span><br><span class="line">    history_head = (history_head + <span class="number">1</span>) % MAX_HISTORY;</span><br><span class="line">    <span class="keyword">if</span> (history_count &lt; MAX_HISTORY) &#123;</span><br><span class="line">        history_count++;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 2. 将内存中全部的历史记录，覆盖写入到磁盘文件中</span></span><br><span class="line">    <span class="type">int</span> fd = open(<span class="string">&quot;/.mos_history&quot;</span>, O_WRONLY | O_CREAT | O_TRUNC);</span><br><span class="line">    <span class="comment">// ... (省略写入文件的循环代码) ...</span></span><br><span class="line">    close(fd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>上下箭头导航</strong>：这部分逻辑实现在 <code>readline</code> 函数处理方向键的 <code>case</code> 中。当检测到上/下箭头时，程序会修改一个用于导航的索引 <code>history_nav_idx</code>，然后从环形缓冲区中取出对应的历史命令字符串，用它覆盖当前的输入缓冲区 <code>buf</code>，并将光标 <code>pos</code> 置于行末。我还特别处理了对用户当前正在输入、但尚未提交的命令的保存和恢复。</p>
</li>
</ul>
</li>
</ul>
<h4 id="3-不带-b-后缀执行指令"><a href="#3-不带-b-后缀执行指令" class="headerlink" title="3. 不带 .b 后缀执行指令"></a>3. 不带 <code>.b</code> 后缀执行指令</h4><ul>
<li><p><strong>实现原理</strong>： 此功能的核心逻辑被直接实现在了<code>spawn</code> 中。这样一来，所有通过 <code>spawn</code> 启动的程序都能享受到此便利。<code>spawn</code> 的执行逻辑如下：</p>
<ol>
<li>首先，它会尝试按用户输入的原样（如 <code>&quot;ls&quot;</code>）去调用 <code>open</code> 打开程序文件。</li>
<li>如果第一次 <code>open</code> 失败（很可能是因为文件不存在），它并不会立即放弃。</li>
<li>而是会在一个临时缓冲区中，将原始的程序名和 <code>.b</code> 后缀拼接起来（得到 <code>&quot;ls.b&quot;</code>），然后再次尝试 <code>open</code> 这个新路径。</li>
<li>如果第二次 <code>open</code> 成功，<code>spawn</code> 就会继续执行这个程序；如果两次尝试都失败了，<code>spawn</code> 才会向上层返回“文件未找到”的错误。</li>
</ol>
<div class="code-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">spawn</span><span class="params">(<span class="type">char</span> *prog, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> fd;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 尝试按原样打开程序</span></span><br><span class="line">    <span class="keyword">if</span> ((fd = open(prog, O_RDONLY)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 2. 如果失败，尝试拼接上 &quot;.b&quot; 后再次打开</span></span><br><span class="line">        <span class="type">char</span> prog_with_b[<span class="number">256</span>];</span><br><span class="line">        <span class="built_in">strcpy</span>(prog_with_b, prog);</span><br><span class="line">        <span class="built_in">strcat</span>(prog_with_b, <span class="string">&quot;.b&quot;</span>);</span><br><span class="line">        fd = open(prog_with_b, O_RDONLY);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 如果两次都失败，才真正返回错误</span></span><br><span class="line">    <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> fd;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ... 后续创建子进程和加载程序代码的逻辑 ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
</ul>
<h3 id="追加重定向、条件执行和反引号"><a href="#追加重定向、条件执行和反引号" class="headerlink" title="追加重定向、条件执行和反引号"></a>追加重定向、条件执行和反引号</h3><h4 id="1-条件执行-amp-amp-与"><a href="#1-条件执行-amp-amp-与" class="headerlink" title="1. 条件执行 (&amp;&amp; 与 ||)"></a>1. 条件执行 (<code>&amp;&amp;</code> 与 <code>||</code>)</h4><p>此功能允许用户根据上一条命令的成功或失败，来决定是否执行下一条命令。</p>
<ul>
<li><p><strong>实现原理</strong>：<br>这个功能的实现，完全建立在<strong>“基于 IPC 的进程退出状态传递”</strong>这一核心机制之上。</p>
<ol>
<li><strong>解析</strong>：首先，<code>split_conditional</code> 函数会以 <code>&amp;&amp;</code> 和 <code>||</code> 为分隔符，将命令行切割成一个 <code>struct Cond</code> 的数组。数组的每个元素都包含一段命令和它后面的连接符类型 (<code>TOKEN_AND</code> 或 <code>TOKEN_OR</code>)。</li>
<li><strong>循环与短路</strong>：在 <code>exec_line</code> 函数中，一个 <code>for</code> 循环负责遍历这个 <code>Cond</code> 数组。在每次循环的开始，它都会检查上一个命令的执行结果 <code>prev_res</code>，并根据连接符类型执行“短路”判断：<ul>
<li>如果上一个连接符是 <code>||</code> 且上一条命令成功了 (<code>prev_res == 0</code>)，则 <code>continue</code> 跳过当前命令。</li>
<li>如果上一个连接符是 <code>&amp;&amp;</code> 且上一条命令失败了 (<code>prev_res != 0</code>)，则 <code>continue</code> 跳过当前命令。</li>
</ul>
</li>
<li><strong>获取结果</strong>：对于需要执行的外部命令，父进程（Shell）会 <code>fork</code> 一个子进程，然后通过 <code>syscall_ipc_recv()</code> 和 <code>wait()</code> 等待子进程结束，并从 <code>env-&gt;env_ipc_value</code> 中获取其退出状态，存入 <code>prev_res</code>，以供下一次循环判断使用。</li>
</ol>
<div class="code-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">exec_line</span><span class="params">(<span class="type">char</span> *cmd)</span> &#123;</span><br><span class="line">    <span class="comment">// ... (省略 split_conditional 解析部分) ...</span></span><br><span class="line">    <span class="type">int</span> prev_res = <span class="number">0</span>; <span class="comment">// 上一个命令的执行结果</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; cond_count; i++) &#123;</span><br><span class="line">        <span class="comment">// --- 短路逻辑判断 ---</span></span><br><span class="line">        <span class="keyword">if</span> (i &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (conds[i - <span class="number">1</span>].type == TOKEN_OR &amp;&amp; prev_res == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (conds[i - <span class="number">1</span>].type == TOKEN_AND &amp;&amp; prev_res != <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// ---------------------</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// ... (执行 conds[i].cmd 命令，并将其结果存入 prev_res) ...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
</ul>
<h4 id="2-命令替换-反引号"><a href="#2-命令替换-反引号" class="headerlink" title="2. 命令替换 (反引号 `` )"></a>2. 命令替换 (反引号 <code>`` </code>)</h4><p>此功能允许将一个命令的输出，作为另一个命令的参数。</p>
<ul>
<li><strong>实现原理</strong>：<br>它的核心是利用了操作系统提供的管道（Pipe）和 <code>fork</code> 机制。在 <code>expand_backticks</code> 函数中调用的 <code>capture_command_output</code> 函数是实现此功能的关键，其步骤如下：<ol>
<li><strong>创建管道</strong>：父进程（Shell）首先调用 <code>pipe()</code> 创建一个管道，得到一个读取端和写入端。</li>
<li><strong>创建子进程</strong>：父进程 <code>fork()</code> 一个子进程来执行反引号中的命令。</li>
<li><strong>子进程重定向输出</strong>：子进程通过 <code>dup()</code> 系统调用，将自己的标准输出（文件描述符 <code>1</code>）重定向到管道的写入端。然后，它正常执行命令，此时所有本应打印到屏幕的输出，都被写入了管道。</li>
<li><strong>父进程读取结果</strong>：父进程则从管道的读取端不断地 <code>read()</code> 数据，直到管道被关闭，从而将子命令的输出捕获到一个缓冲区中。</li>
<li><strong>清理</strong>：父进程最后还必须调用 <code>syscall_ipc_recv()</code> 和 <code>wait()</code> 来正确地回收子进程，并消费掉子进程附带的 IPC 退出消息，以避免干扰后续主命令的执行。</li>
</ol>
</li>
</ul>
<h4 id="3-追加重定向-gt-gt"><a href="#3-追加重定向-gt-gt" class="headerlink" title="3. 追加重定向 (&gt;&gt;)"></a>3. 追加重定向 (<code>&gt;&gt;</code>)</h4><p>此功能用于将命令的输出追加到文件末尾，而非覆盖。</p>
<ul>
<li><p><strong>实现原理</strong>：</p>
<ol>
<li><strong>识别符号</strong>：首先，我修改了 <code>_gettoken</code> 分词器，使其能够将 <code>&gt;&gt;</code> 识别为一个不同于 <code>&gt;</code> 的、独立的新 Token (<code>TOKEN_APPEND</code>)。</li>
<li><strong>处理 Token</strong>：在 <code>parsecmd</code> 函数的 <code>switch</code> 语句中，我为 <code>TOKEN_APPEND</code> 增加了一个新的 <code>case</code>。</li>
<li><strong>关键实现</strong>：其核心技巧分为两步：<ul>
<li>调用 <code>open()</code> 打开文件时，使用的标志位是 <code>O_WRONLY | O_CREAT</code>，<strong>刻意不使用 <code>O_TRUNC</code></strong>。这保证了如果文件已存在，其内容不会被清空。</li>
<li>文件打开后，在进行 <code>dup()</code> 重定向之前，<strong>手动修改文件描述符结构体中的 <code>fd_offset</code> 成员，将其值设置为文件的当前大小 <code>f_file.f_size</code></strong>。这就像手动将“写入光标”移动到了文件的末尾。</li>
</ul>
</li>
</ol>
<div class="code-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> TOKEN_APPEND:</span><br><span class="line">    <span class="keyword">if</span> (gettoken(<span class="number">0</span>, &amp;t) != <span class="string">&#x27;w&#x27;</span>) &#123; <span class="comment">/* 错误处理 */</span> &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. open 时不使用 O_TRUNC</span></span><br><span class="line">    fd = open(t, O_WRONLY | O_CREAT);</span><br><span class="line">    <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123; <span class="comment">/* 错误处理 */</span> &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 手动将写入指针移动到文件末尾</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Fd</span> *<span class="title">fd_struct</span>;</span></span><br><span class="line">    fd_lookup(fd, &amp;fd_struct);</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Filefd</span> *<span class="title">ffd</span> =</span> (<span class="keyword">struct</span> Filefd *)fd_struct;</span><br><span class="line">    fd_struct-&gt;fd_offset = ffd-&gt;f_file.f_size;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 重定向标准输出</span></span><br><span class="line">    dup(fd, <span class="number">1</span>);</span><br><span class="line">    close(fd);</span><br><span class="line">    <span class="keyword">break</span>;</span><br></pre></td></tr></table></figure></div>
</li>
</ul>
<h2 id="挑战性任务难点"><a href="#挑战性任务难点" class="headerlink" title="挑战性任务难点"></a>挑战性任务难点</h2><p><strong>1. 复杂的状态管理</strong></p>
<ul>
<li><strong>进程级状态（CWD）</strong>：为实现相对路径，需要深入到内核，修改进程控制块 <code>struct Env</code>，为每个进程增加“当前工作目录”这一状态。这要求在内核与用户态之间建立清晰的界限和交互（通过 <code>cd</code> 和 <code>pwd</code> 的系统调用）。</li>
<li><strong>Shell 级状态（变量）</strong>：为了实现环境变量和局部变量，需要在 Shell 进程的内存中设计一套完整的变量管理系统。难点在于区分不同变量的作用域（局部 vs 全局）和属性（只读），并设计将环境变量正确地“遗传”给子进程的机制。</li>
<li><strong>持久化状态（历史记录）</strong>：<code>history</code> 功能不仅要在内存中实现一个高效的环形缓冲区来管理命令，还要处理与磁盘文件 <code>.mos_history</code> 的同步读写，以实现历史记录的持久化。</li>
</ul>
<p><strong>2. 底层系统交互与协议设计</strong></p>
<ul>
<li><strong>IPC 退出码协议</strong>：这是实现 <code>&amp;&amp;</code> 和 <code>||</code> 的基石。由于不能修改内核 <code>wait</code> 机制，我们从零设计了一套基于 IPC 的、可跨越多级进程的退出状态传递协议。难点在于设计 <code>runcmd</code> 作为“中继站”的角色，来保证状态码能从真正的执行者（孙子进程）可靠地传递回主 Shell。</li>
<li><strong>文件描述符（FD）精细操作</strong>：实现 <code>&gt;&gt;</code> 追加重定向和 <code>`` </code> 命令替换，都要求对文件描述符有深刻的理解。前者需要通过直接修改 <code>fd_struct-&gt;fd_offset</code> 来实现对文件写入位置的精确控制；后者则需要熟练运用 <code>pipe()</code> 创建管道、<code>dup()</code> 复制文件描述符来实现 I/O 流的重定向。</li>
</ul>
<p><strong>3. 并发与时序问题调试</strong></p>
<ul>
<li><strong><code>wait()</code> 与 <code>recv()</code> 的竞态条件</strong>：这是导致反复死锁的罪魁祸首。我们最终发现，必须先调用 <code>recv()</code> 等待 IPC 消息，再调用 <code>wait()</code> 回收子进程，否则子进程被 <code>wait</code> 清理后，它发出的消息就可能被内核丢弃，导致父进程永久等待。</li>
<li><strong>IPC 消息污染</strong>：在实现反引号功能时，其子进程发出的 IPC 消息如果没有被及时“消费”，就会残留在主 Shell 的消息队列里，被下一个完全不相关的命令错误地接收，导致后续的 <code>&amp;&amp;</code> <code>||</code> 逻辑全盘崩溃。</li>
</ul>
<h2 id="实验感想"><a href="#实验感想" class="headerlink" title="实验感想"></a>实验感想</h2><p>如果说之前的实验是让我们学会如何制造锤子、螺丝刀等“零件”，那么这次的 Shell 项目就是给了我们一张图纸，要求我们用所有这些零件组装出一台能正常运转的“机器”。这个过程让我对操作系统这门课的认识，从“学习理论知识”真正转向了“应用理论解决工程问题”。感谢挑战性任务，感谢os，本学期操作系统课程正式完结！</p>
]]></content>
      <tags>
        <tag>os</tag>
      </tags>
  </entry>
  <entry>
    <title>BUAA_Compiler 优化文章</title>
    <url>/2025/12/27/compiler-optimization-report/</url>
    <content><![CDATA[<h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ol>
<li><a href="#1-优化概述与整体架构">优化概述与整体架构</a></li>
<li><a href="#2-控制流图构建-cfgbuilder">控制流图构建 (CfgBuilder)</a></li>
<li><a href="#3-不可达代码消除-removeunreachcode">不可达代码消除 (RemoveUnReachCode)</a></li>
<li><a href="#4-死代码消除-removedeadcode">死代码消除 (RemoveDeadCode)</a></li>
<li><a href="#5-mem2reg-与-ssa-构造">Mem2Reg 与 SSA 构造</a></li>
<li><a href="#6-phi-指令插入-insertphi">Phi 指令插入 (InsertPhi)</a></li>
<li><a href="#7-phi-指令消除-removephi">Phi 指令消除 (RemovePhi)</a></li>
<li><a href="#8-全局值编号与代数化简-lvngvn">全局值编号与代数化简 (Lvn/GVN)</a></li>
<li><a href="#9-全局代码移动-gcm-与循环不变量外提-licm">全局代码移动 (GCM) 与循环不变量外提 (LICM)</a></li>
<li><a href="#10-函数内联-funcinline">函数内联 (FuncInline)</a></li>
<li><a href="#11-活跃变量分析-activeanalysis">活跃变量分析 (ActiveAnalysis)</a></li>
<li><a href="#12-寄存器分配-registerallocator">寄存器分配 (RegisterAllocator)</a></li>
<li><a href="#13-窥孔优化-peephole">窥孔优化 (PeepHole)</a></li>
<li><a href="#14-后端强度削减">后端强度削减</a></li>
</ol>
<hr>
<h2 id="1-优化概述与整体架构"><a href="#1-优化概述与整体架构" class="headerlink" title="1. 优化概述与整体架构"></a>1. 优化概述与整体架构</h2><h3 id="1-1-优化管理器-OptimizeManager"><a href="#1-1-优化管理器-OptimizeManager" class="headerlink" title="1.1 优化管理器 (OptimizeManager)"></a>1.1 优化管理器 (OptimizeManager)</h3><p>所有优化遍均由 <code>OptimizeManager</code> 统一管理和调度。优化执行顺序如下：</p>
<div class="code-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">1. RemoveUnReachCode + CfgBuilder + RemoveDeadCode + CfgBuilder (初始清理)</span><br><span class="line">2. FuncInline + 清理遍 (函数内联)</span><br><span class="line">3. MemToReg + CfgBuilder (栈变量提升至寄存器)</span><br><span class="line">4. 清理遍</span><br><span class="line">5. 循环 5 次:</span><br><span class="line">   - Lvn (GVN + 代数化简)</span><br><span class="line">   - Gcm (全局代码移动)</span><br><span class="line">   - 清理遍</span><br><span class="line">6. ActiveAnalysis + AllocateRegister (活跃分析与寄存器分配)</span><br><span class="line">7. RemovePhi (Phi 消除，在 MIPS 生成前)</span><br></pre></td></tr></table></figure></div>
<h3 id="1-2-设计原则"><a href="#1-2-设计原则" class="headerlink" title="1.2 设计原则"></a>1.2 设计原则</h3><ul>
<li><strong>迭代优化</strong>：许多优化会暴露新的优化机会，因此采用多轮迭代（如 Lvn + Gcm 循环 5 次）。</li>
<li><strong>清理遍协同</strong>：每个主要优化后都跟随 <code>RemoveUnReachCode</code>、<code>CfgBuilder</code>、<code>RemoveDeadCode</code>，确保 CFG 和 IR 的健康状态。</li>
<li><strong>Pass 解耦</strong>：每个优化遍只负责单一职责，通过 <code>Optimizer</code> 抽象基类统一接口。</li>
</ul>
<hr>
<h2 id="2-控制流图构建-CfgBuilder"><a href="#2-控制流图构建-CfgBuilder" class="headerlink" title="2. 控制流图构建 (CfgBuilder)"></a>2. 控制流图构建 (CfgBuilder)</h2><h3 id="2-1-功能描述"><a href="#2-1-功能描述" class="headerlink" title="2.1 功能描述"></a>2.1 功能描述</h3><p><code>CfgBuilder</code> 负责根据基本块之间的跳转关系构建控制流图（CFG），主要维护以下信息：</p>
<ul>
<li><strong>前驱 (Predecessors)</strong> 和 <strong>后继 (Successors)</strong> 列表</li>
<li><strong>支配关系 (Dominators)</strong>：直接支配者、支配边界</li>
<li><strong>支配树 (Dominator Tree)</strong>：用于 GVN 按支配序遍历</li>
</ul>
<h3 id="2-2-实现要点"><a href="#2-2-实现要点" class="headerlink" title="2.2 实现要点"></a>2.2 实现要点</h3><ol>
<li><p><strong>后继关系推导</strong>：遍历每个基本块的最后一条指令：</p>
<ul>
<li><code>JumpInstr</code>：单后继</li>
<li><code>BranchInstr</code>：双后继（True/False 分支）</li>
<li><code>ReturnInstr</code>：无后继</li>
</ul>
</li>
<li><p><strong>支配关系计算</strong>：采用数据流迭代算法：</p>
<ul>
<li>初始化：每个块的支配者集合为所有块</li>
<li>迭代：<mjx-container class="MathJax" jax="SVG"><svg style="vertical-align: -1.044ex;" xmlns="http://www.w3.org/2000/svg" width="36.43ex" height="2.741ex" role="img" focusable="false" viewBox="0 -750 16102 1211.6"><g stroke="currentColor" fill="currentColor" stroke-width="0" transform="scale(1,-1)"><g data-mml-node="math"><g data-mml-node="TeXAtom" data-mjx-texclass="ORD"><g data-mml-node="mi"><path data-c="44" d="M130 622Q123 629 119 631T103 634T60 637H27V683H228Q399 682 419 682T461 676Q504 667 546 641T626 573T685 470T708 336Q708 210 634 116T442 3Q429 1 228 0H27V46H60Q102 47 111 49T130 61V622ZM593 338Q593 439 571 501T493 602Q439 637 355 637H322H294Q238 637 234 628Q231 624 231 344Q231 62 232 59Q233 49 248 48T339 46H350Q456 46 515 95Q561 133 577 191T593 338Z"></path><path data-c="6F" d="M28 214Q28 309 93 378T250 448Q340 448 405 380T471 215Q471 120 407 55T250 -10Q153 -10 91 57T28 214ZM250 30Q372 30 372 193V225V250Q372 272 371 288T364 326T348 362T317 390T268 410Q263 411 252 411Q222 411 195 399Q152 377 139 338T126 246V226Q126 130 145 91Q177 30 250 30Z" transform="translate(764,0)"></path><path data-c="6D" d="M41 46H55Q94 46 102 60V68Q102 77 102 91T102 122T103 161T103 203Q103 234 103 269T102 328V351Q99 370 88 376T43 385H25V408Q25 431 27 431L37 432Q47 433 65 434T102 436Q119 437 138 438T167 441T178 442H181V402Q181 364 182 364T187 369T199 384T218 402T247 421T285 437Q305 442 336 442Q351 442 364 440T387 434T406 426T421 417T432 406T441 395T448 384T452 374T455 366L457 361L460 365Q463 369 466 373T475 384T488 397T503 410T523 422T546 432T572 439T603 442Q729 442 740 329Q741 322 741 190V104Q741 66 743 59T754 49Q775 46 803 46H819V0H811L788 1Q764 2 737 2T699 3Q596 3 587 0H579V46H595Q656 46 656 62Q657 64 657 200Q656 335 655 343Q649 371 635 385T611 402T585 404Q540 404 506 370Q479 343 472 315T464 232V168V108Q464 78 465 68T468 55T477 49Q498 46 526 46H542V0H534L510 1Q487 2 460 2T422 3Q319 3 310 0H302V46H318Q379 46 379 62Q380 64 380 200Q379 335 378 343Q372 371 358 385T334 402T308 404Q263 404 229 370Q202 343 195 315T187 232V168V108Q187 78 188 68T191 55T200 49Q221 46 249 46H265V0H257L234 1Q210 2 183 2T145 3Q42 3 33 0H25V46H41Z" transform="translate(1264,0)"></path></g></g><g data-mml-node="mo" transform="translate(2097,0)"><path data-c="28" d="M94 250Q94 319 104 381T127 488T164 576T202 643T244 695T277 729T302 750H315H319Q333 750 333 741Q333 738 316 720T275 667T226 581T184 443T167 250T184 58T225 -81T274 -167T316 -220T333 -241Q333 -250 318 -250H315H302L274 -226Q180 -141 137 -14T94 250Z"></path></g><g data-mml-node="mi" transform="translate(2486,0)"><path data-c="1D435" d="M231 637Q204 637 199 638T194 649Q194 676 205 682Q206 683 335 683Q594 683 608 681Q671 671 713 636T756 544Q756 480 698 429T565 360L555 357Q619 348 660 311T702 219Q702 146 630 78T453 1Q446 0 242 0Q42 0 39 2Q35 5 35 10Q35 17 37 24Q42 43 47 45Q51 46 62 46H68Q95 46 128 49Q142 52 147 61Q150 65 219 339T288 628Q288 635 231 637ZM649 544Q649 574 634 600T585 634Q578 636 493 637Q473 637 451 637T416 636H403Q388 635 384 626Q382 622 352 506Q352 503 351 500L320 374H401Q482 374 494 376Q554 386 601 434T649 544ZM595 229Q595 273 572 302T512 336Q506 337 429 337Q311 337 310 336Q310 334 293 263T258 122L240 52Q240 48 252 48T333 46Q422 46 429 47Q491 54 543 105T595 229Z"></path></g><g data-mml-node="mo" transform="translate(3245,0)"><path data-c="29" d="M60 749L64 750Q69 750 74 750H86L114 726Q208 641 251 514T294 250Q294 182 284 119T261 12T224 -76T186 -143T145 -194T113 -227T90 -246Q87 -249 86 -250H74Q66 -250 63 -250T58 -247T55 -238Q56 -237 66 -225Q221 -64 221 250T66 725Q56 737 55 738Q55 746 60 749Z"></path></g><g data-mml-node="mo" transform="translate(3911.8,0)"><path data-c="3D" d="M56 347Q56 360 70 367H707Q722 359 722 347Q722 336 708 328L390 327H72Q56 332 56 347ZM56 153Q56 168 72 173H708Q722 163 722 153Q722 140 707 133H70Q56 140 56 153Z"></path></g><g data-mml-node="mo" transform="translate(4967.6,0)"><path data-c="7B" d="M434 -231Q434 -244 428 -250H410Q281 -250 230 -184Q225 -177 222 -172T217 -161T213 -148T211 -133T210 -111T209 -84T209 -47T209 0Q209 21 209 53Q208 142 204 153Q203 154 203 155Q189 191 153 211T82 231Q71 231 68 234T65 250T68 266T82 269Q116 269 152 289T203 345Q208 356 208 377T209 529V579Q209 634 215 656T244 698Q270 724 324 740Q361 748 377 749Q379 749 390 749T408 750H428Q434 744 434 732Q434 719 431 716Q429 713 415 713Q362 710 332 689T296 647Q291 634 291 499V417Q291 370 288 353T271 314Q240 271 184 255L170 250L184 245Q202 239 220 230T262 196T290 137Q291 131 291 1Q291 -134 296 -147Q306 -174 339 -192T415 -213Q429 -213 431 -216Q434 -219 434 -231Z"></path></g><g data-mml-node="mi" transform="translate(5467.6,0)"><path data-c="1D435" d="M231 637Q204 637 199 638T194 649Q194 676 205 682Q206 683 335 683Q594 683 608 681Q671 671 713 636T756 544Q756 480 698 429T565 360L555 357Q619 348 660 311T702 219Q702 146 630 78T453 1Q446 0 242 0Q42 0 39 2Q35 5 35 10Q35 17 37 24Q42 43 47 45Q51 46 62 46H68Q95 46 128 49Q142 52 147 61Q150 65 219 339T288 628Q288 635 231 637ZM649 544Q649 574 634 600T585 634Q578 636 493 637Q473 637 451 637T416 636H403Q388 635 384 626Q382 622 352 506Q352 503 351 500L320 374H401Q482 374 494 376Q554 386 601 434T649 544ZM595 229Q595 273 572 302T512 336Q506 337 429 337Q311 337 310 336Q310 334 293 263T258 122L240 52Q240 48 252 48T333 46Q422 46 429 47Q491 54 543 105T595 229Z"></path></g><g data-mml-node="mo" transform="translate(6226.6,0)"><path data-c="7D" d="M65 731Q65 745 68 747T88 750Q171 750 216 725T279 670Q288 649 289 635T291 501Q292 362 293 357Q306 312 345 291T417 269Q428 269 431 266T434 250T431 234T417 231Q380 231 345 210T298 157Q293 143 292 121T291 -28V-79Q291 -134 285 -156T256 -198Q202 -250 89 -250Q71 -250 68 -247T65 -230Q65 -224 65 -223T66 -218T69 -214T77 -213Q91 -213 108 -210T146 -200T183 -177T207 -139Q208 -134 209 3L210 139Q223 196 280 230Q315 247 330 250Q305 257 280 270Q225 304 212 352L210 362L209 498Q208 635 207 640Q195 680 154 696T77 713Q68 713 67 716T65 731Z"></path></g><g data-mml-node="mo" transform="translate(6948.8,0)"><path data-c="222A" d="M591 598H592Q604 598 611 583V376Q611 345 611 296Q610 162 606 148Q605 146 605 145Q586 68 507 23T333 -22Q268 -22 209 -1T106 66T56 173Q55 180 55 384L56 585Q66 598 75 598Q85 598 95 585V378L96 172L98 162Q112 95 181 57T332 18Q415 18 487 58T570 175Q571 180 571 383V583Q579 598 591 598Z"></path></g><g data-mml-node="munder" transform="translate(7838,0)"><g data-mml-node="mo" transform="translate(0 -0.5)"><path data-c="22C2" d="M139 -217Q127 -241 114 -246Q106 -249 97 -249Q67 -249 57 -220Q55 -214 55 102Q55 152 55 221T54 312Q54 422 60 464T91 554Q120 612 165 654T257 714T337 741T392 749Q393 750 402 750Q414 750 422 749Q557 749 660 659T776 430Q777 422 777 102Q777 -214 775 -220Q765 -249 735 -249Q716 -249 708 -241T694 -217L692 428L690 441Q674 540 597 603T416 666H409Q388 666 364 662T294 638T212 581Q156 523 142 441L140 428L139 105V-217Z"></path></g><g data-mml-node="TeXAtom" transform="translate(866,-284.9) scale(0.707)" data-mjx-texclass="ORD"><g data-mml-node="mi"><path data-c="1D443" d="M287 628Q287 635 230 637Q206 637 199 638T192 648Q192 649 194 659Q200 679 203 681T397 683Q587 682 600 680Q664 669 707 631T751 530Q751 453 685 389Q616 321 507 303Q500 302 402 301H307L277 182Q247 66 247 59Q247 55 248 54T255 50T272 48T305 46H336Q342 37 342 35Q342 19 335 5Q330 0 319 0Q316 0 282 1T182 2Q120 2 87 2T51 1Q33 1 33 11Q33 13 36 25Q40 41 44 43T67 46Q94 46 127 49Q141 52 146 61Q149 65 218 339T287 628ZM645 554Q645 567 643 575T634 597T609 619T560 635Q553 636 480 637Q463 637 445 637T416 636T404 636Q391 635 386 627Q384 621 367 550T332 412T314 344Q314 342 395 342H407H430Q542 342 590 392Q617 419 631 471T645 554Z"></path></g><g data-mml-node="mo" transform="translate(751,0)"><path data-c="2208" d="M84 250Q84 372 166 450T360 539Q361 539 377 539T419 540T469 540H568Q583 532 583 520Q583 511 570 501L466 500Q355 499 329 494Q280 482 242 458T183 409T147 354T129 306T124 272V270H568Q583 262 583 250T568 230H124V228Q124 207 134 177T167 112T231 48T328 7Q355 1 466 0H570Q583 -10 583 -20Q583 -32 568 -40H471Q464 -40 446 -40T417 -41Q262 -41 172 45Q84 127 84 250Z"></path></g><g data-mml-node="TeXAtom" data-mjx-texclass="ORD" transform="translate(1418,0)"><g data-mml-node="mi"><path data-c="50" d="M130 622Q123 629 119 631T103 634T60 637H27V683H214Q237 683 276 683T331 684Q419 684 471 671T567 616Q624 563 624 489Q624 421 573 372T451 307Q429 302 328 301H234V181Q234 62 237 58Q245 47 304 46H337V0H326Q305 3 182 3Q47 3 38 0H27V46H60Q102 47 111 49T130 61V622ZM507 488Q507 514 506 528T500 564T483 597T450 620T397 635Q385 637 307 637H286Q237 637 234 628Q231 624 231 483V342H302H339Q390 342 423 349T481 382Q507 411 507 488Z"></path><path data-c="72" d="M36 46H50Q89 46 97 60V68Q97 77 97 91T98 122T98 161T98 203Q98 234 98 269T98 328L97 351Q94 370 83 376T38 385H20V408Q20 431 22 431L32 432Q42 433 60 434T96 436Q112 437 131 438T160 441T171 442H174V373Q213 441 271 441H277Q322 441 343 419T364 373Q364 352 351 337T313 322Q288 322 276 338T263 372Q263 381 265 388T270 400T273 405Q271 407 250 401Q234 393 226 386Q179 341 179 207V154Q179 141 179 127T179 101T180 81T180 66V61Q181 59 183 57T188 54T193 51T200 49T207 48T216 47T225 47T235 46T245 46H276V0H267Q249 3 140 3Q37 3 28 0H20V46H36Z" transform="translate(681,0)"></path><path data-c="65" d="M28 218Q28 273 48 318T98 391T163 433T229 448Q282 448 320 430T378 380T406 316T415 245Q415 238 408 231H126V216Q126 68 226 36Q246 30 270 30Q312 30 342 62Q359 79 369 104L379 128Q382 131 395 131H398Q415 131 415 121Q415 117 412 108Q393 53 349 21T250 -11Q155 -11 92 58T28 218ZM333 275Q322 403 238 411H236Q228 411 220 410T195 402T166 381T143 340T127 274V267H333V275Z" transform="translate(1073,0)"></path><path data-c="64" d="M376 495Q376 511 376 535T377 568Q377 613 367 624T316 637H298V660Q298 683 300 683L310 684Q320 685 339 686T376 688Q393 689 413 690T443 693T454 694H457V390Q457 84 458 81Q461 61 472 55T517 46H535V0Q533 0 459 -5T380 -11H373V44L365 37Q307 -11 235 -11Q158 -11 96 50T34 215Q34 315 97 378T244 442Q319 442 376 393V495ZM373 342Q328 405 260 405Q211 405 173 369Q146 341 139 305T131 211Q131 155 138 120T173 59Q203 26 251 26Q322 26 373 103V342Z" transform="translate(1517,0)"></path></g></g><g data-mml-node="mo" transform="translate(3491,0)"><path data-c="28" d="M94 250Q94 319 104 381T127 488T164 576T202 643T244 695T277 729T302 750H315H319Q333 750 333 741Q333 738 316 720T275 667T226 581T184 443T167 250T184 58T225 -81T274 -167T316 -220T333 -241Q333 -250 318 -250H315H302L274 -226Q180 -141 137 -14T94 250Z"></path></g><g data-mml-node="mi" transform="translate(3880,0)"><path data-c="1D435" d="M231 637Q204 637 199 638T194 649Q194 676 205 682Q206 683 335 683Q594 683 608 681Q671 671 713 636T756 544Q756 480 698 429T565 360L555 357Q619 348 660 311T702 219Q702 146 630 78T453 1Q446 0 242 0Q42 0 39 2Q35 5 35 10Q35 17 37 24Q42 43 47 45Q51 46 62 46H68Q95 46 128 49Q142 52 147 61Q150 65 219 339T288 628Q288 635 231 637ZM649 544Q649 574 634 600T585 634Q578 636 493 637Q473 637 451 637T416 636H403Q388 635 384 626Q382 622 352 506Q352 503 351 500L320 374H401Q482 374 494 376Q554 386 601 434T649 544ZM595 229Q595 273 572 302T512 336Q506 337 429 337Q311 337 310 336Q310 334 293 263T258 122L240 52Q240 48 252 48T333 46Q422 46 429 47Q491 54 543 105T595 229Z"></path></g><g data-mml-node="mo" transform="translate(4639,0)"><path data-c="29" d="M60 749L64 750Q69 750 74 750H86L114 726Q208 641 251 514T294 250Q294 182 284 119T261 12T224 -76T186 -143T145 -194T113 -227T90 -246Q87 -249 86 -250H74Q66 -250 63 -250T58 -247T55 -238Q56 -237 66 -225Q221 -64 221 250T66 725Q56 737 55 738Q55 746 60 749Z"></path></g></g></g><g data-mml-node="TeXAtom" data-mjx-texclass="ORD" transform="translate(12476,0)"><g data-mml-node="mi"><path data-c="44" d="M130 622Q123 629 119 631T103 634T60 637H27V683H228Q399 682 419 682T461 676Q504 667 546 641T626 573T685 470T708 336Q708 210 634 116T442 3Q429 1 228 0H27V46H60Q102 47 111 49T130 61V622ZM593 338Q593 439 571 501T493 602Q439 637 355 637H322H294Q238 637 234 628Q231 624 231 344Q231 62 232 59Q233 49 248 48T339 46H350Q456 46 515 95Q561 133 577 191T593 338Z"></path><path data-c="6F" d="M28 214Q28 309 93 378T250 448Q340 448 405 380T471 215Q471 120 407 55T250 -10Q153 -10 91 57T28 214ZM250 30Q372 30 372 193V225V250Q372 272 371 288T364 326T348 362T317 390T268 410Q263 411 252 411Q222 411 195 399Q152 377 139 338T126 246V226Q126 130 145 91Q177 30 250 30Z" transform="translate(764,0)"></path><path data-c="6D" d="M41 46H55Q94 46 102 60V68Q102 77 102 91T102 122T103 161T103 203Q103 234 103 269T102 328V351Q99 370 88 376T43 385H25V408Q25 431 27 431L37 432Q47 433 65 434T102 436Q119 437 138 438T167 441T178 442H181V402Q181 364 182 364T187 369T199 384T218 402T247 421T285 437Q305 442 336 442Q351 442 364 440T387 434T406 426T421 417T432 406T441 395T448 384T452 374T455 366L457 361L460 365Q463 369 466 373T475 384T488 397T503 410T523 422T546 432T572 439T603 442Q729 442 740 329Q741 322 741 190V104Q741 66 743 59T754 49Q775 46 803 46H819V0H811L788 1Q764 2 737 2T699 3Q596 3 587 0H579V46H595Q656 46 656 62Q657 64 657 200Q656 335 655 343Q649 371 635 385T611 402T585 404Q540 404 506 370Q479 343 472 315T464 232V168V108Q464 78 465 68T468 55T477 49Q498 46 526 46H542V0H534L510 1Q487 2 460 2T422 3Q319 3 310 0H302V46H318Q379 46 379 62Q380 64 380 200Q379 335 378 343Q372 371 358 385T334 402T308 404Q263 404 229 370Q202 343 195 315T187 232V168V108Q187 78 188 68T191 55T200 49Q221 46 249 46H265V0H257L234 1Q210 2 183 2T145 3Q42 3 33 0H25V46H41Z" transform="translate(1264,0)"></path></g></g><g data-mml-node="mo" transform="translate(14573,0)"><path data-c="28" d="M94 250Q94 319 104 381T127 488T164 576T202 643T244 695T277 729T302 750H315H319Q333 750 333 741Q333 738 316 720T275 667T226 581T184 443T167 250T184 58T225 -81T274 -167T316 -220T333 -241Q333 -250 318 -250H315H302L274 -226Q180 -141 137 -14T94 250Z"></path></g><g data-mml-node="mi" transform="translate(14962,0)"><path data-c="1D443" d="M287 628Q287 635 230 637Q206 637 199 638T192 648Q192 649 194 659Q200 679 203 681T397 683Q587 682 600 680Q664 669 707 631T751 530Q751 453 685 389Q616 321 507 303Q500 302 402 301H307L277 182Q247 66 247 59Q247 55 248 54T255 50T272 48T305 46H336Q342 37 342 35Q342 19 335 5Q330 0 319 0Q316 0 282 1T182 2Q120 2 87 2T51 1Q33 1 33 11Q33 13 36 25Q40 41 44 43T67 46Q94 46 127 49Q141 52 146 61Q149 65 218 339T287 628ZM645 554Q645 567 643 575T634 597T609 619T560 635Q553 636 480 637Q463 637 445 637T416 636T404 636Q391 635 386 627Q384 621 367 550T332 412T314 344Q314 342 395 342H407H430Q542 342 590 392Q617 419 631 471T645 554Z"></path></g><g data-mml-node="mo" transform="translate(15713,0)"><path data-c="29" d="M60 749L64 750Q69 750 74 750H86L114 726Q208 641 251 514T294 250Q294 182 284 119T261 12T224 -76T186 -143T145 -194T113 -227T90 -246Q87 -249 86 -250H74Q66 -250 63 -250T58 -247T55 -238Q56 -237 66 -225Q221 -64 221 250T66 725Q56 737 55 738Q55 746 60 749Z"></path></g></g></g></svg></mjx-container></li>
<li>收敛后确定直接支配者（Immediate Dominator）</li>
</ul>
</li>
<li><p><strong>支配边界计算</strong>：</p>
<p><mjx-container class="MathJax" jax="SVG"><svg style="vertical-align: -0.566ex;" xmlns="http://www.w3.org/2000/svg" width="53.803ex" height="2.262ex" role="img" focusable="false" viewBox="0 -750 23780.8 1000"><g stroke="currentColor" fill="currentColor" stroke-width="0" transform="scale(1,-1)"><g data-mml-node="math"><g data-mml-node="mi"><path data-c="1D437" d="M287 628Q287 635 230 637Q207 637 200 638T193 647Q193 655 197 667T204 682Q206 683 403 683Q570 682 590 682T630 676Q702 659 752 597T803 431Q803 275 696 151T444 3L430 1L236 0H125H72Q48 0 41 2T33 11Q33 13 36 25Q40 41 44 43T67 46Q94 46 127 49Q141 52 146 61Q149 65 218 339T287 628ZM703 469Q703 507 692 537T666 584T629 613T590 629T555 636Q553 636 541 636T512 636T479 637H436Q392 637 386 627Q384 623 313 339T242 52Q242 48 253 48T330 47Q335 47 349 47T373 46Q499 46 581 128Q617 164 640 212T683 339T703 469Z"></path></g><g data-mml-node="mi" transform="translate(828,0)"><path data-c="1D439" d="M48 1Q31 1 31 11Q31 13 34 25Q38 41 42 43T65 46Q92 46 125 49Q139 52 144 61Q146 66 215 342T285 622Q285 629 281 629Q273 632 228 634H197Q191 640 191 642T193 659Q197 676 203 680H742Q749 676 749 669Q749 664 736 557T722 447Q720 440 702 440H690Q683 445 683 453Q683 454 686 477T689 530Q689 560 682 579T663 610T626 626T575 633T503 634H480Q398 633 393 631Q388 629 386 623Q385 622 352 492L320 363H375Q378 363 398 363T426 364T448 367T472 374T489 386Q502 398 511 419T524 457T529 475Q532 480 548 480H560Q567 475 567 470Q567 467 536 339T502 207Q500 200 482 200H470Q463 206 463 212Q463 215 468 234T473 274Q473 303 453 310T364 317H309L277 190Q245 66 245 60Q245 46 334 46H359Q365 40 365 39T363 19Q359 6 353 0H336Q295 2 185 2Q120 2 86 2T48 1Z"></path></g><g data-mml-node="mo" transform="translate(1577,0)"><path data-c="28" d="M94 250Q94 319 104 381T127 488T164 576T202 643T244 695T277 729T302 750H315H319Q333 750 333 741Q333 738 316 720T275 667T226 581T184 443T167 250T184 58T225 -81T274 -167T316 -220T333 -241Q333 -250 318 -250H315H302L274 -226Q180 -141 137 -14T94 250Z"></path></g><g data-mml-node="mi" transform="translate(1966,0)"><path data-c="1D44B" d="M42 0H40Q26 0 26 11Q26 15 29 27Q33 41 36 43T55 46Q141 49 190 98Q200 108 306 224T411 342Q302 620 297 625Q288 636 234 637H206Q200 643 200 645T202 664Q206 677 212 683H226Q260 681 347 681Q380 681 408 681T453 682T473 682Q490 682 490 671Q490 670 488 658Q484 643 481 640T465 637Q434 634 411 620L488 426L541 485Q646 598 646 610Q646 628 622 635Q617 635 609 637Q594 637 594 648Q594 650 596 664Q600 677 606 683H618Q619 683 643 683T697 681T738 680Q828 680 837 683H845Q852 676 852 672Q850 647 840 637H824Q790 636 763 628T722 611T698 593L687 584Q687 585 592 480L505 384Q505 383 536 304T601 142T638 56Q648 47 699 46Q734 46 734 37Q734 35 732 23Q728 7 725 4T711 1Q708 1 678 1T589 2Q528 2 496 2T461 1Q444 1 444 10Q444 11 446 25Q448 35 450 39T455 44T464 46T480 47T506 54Q523 62 523 64Q522 64 476 181L429 299Q241 95 236 84Q232 76 232 72Q232 53 261 47Q262 47 267 47T273 46Q276 46 277 46T280 45T283 42T284 35Q284 26 282 19Q279 6 276 4T261 1Q258 1 243 1T201 2T142 2Q64 2 42 0Z"></path></g><g data-mml-node="mo" transform="translate(2818,0)"><path data-c="29" d="M60 749L64 750Q69 750 74 750H86L114 726Q208 641 251 514T294 250Q294 182 284 119T261 12T224 -76T186 -143T145 -194T113 -227T90 -246Q87 -249 86 -250H74Q66 -250 63 -250T58 -247T55 -238Q56 -237 66 -225Q221 -64 221 250T66 725Q56 737 55 738Q55 746 60 749Z"></path></g><g data-mml-node="mo" transform="translate(3484.8,0)"><path data-c="3D" d="M56 347Q56 360 70 367H707Q722 359 722 347Q722 336 708 328L390 327H72Q56 332 56 347ZM56 153Q56 168 72 173H708Q722 163 722 153Q722 140 707 133H70Q56 140 56 153Z"></path></g><g data-mml-node="mo" transform="translate(4540.6,0)"><path data-c="7B" d="M434 -231Q434 -244 428 -250H410Q281 -250 230 -184Q225 -177 222 -172T217 -161T213 -148T211 -133T210 -111T209 -84T209 -47T209 0Q209 21 209 53Q208 142 204 153Q203 154 203 155Q189 191 153 211T82 231Q71 231 68 234T65 250T68 266T82 269Q116 269 152 289T203 345Q208 356 208 377T209 529V579Q209 634 215 656T244 698Q270 724 324 740Q361 748 377 749Q379 749 390 749T408 750H428Q434 744 434 732Q434 719 431 716Q429 713 415 713Q362 710 332 689T296 647Q291 634 291 499V417Q291 370 288 353T271 314Q240 271 184 255L170 250L184 245Q202 239 220 230T262 196T290 137Q291 131 291 1Q291 -134 296 -147Q306 -174 339 -192T415 -213Q429 -213 431 -216Q434 -219 434 -231Z"></path></g><g data-mml-node="mi" transform="translate(5040.6,0)"><path data-c="1D44C" d="M66 637Q54 637 49 637T39 638T32 641T30 647T33 664T42 682Q44 683 56 683Q104 680 165 680Q288 680 306 683H316Q322 677 322 674T320 656Q316 643 310 637H298Q242 637 242 624Q242 619 292 477T343 333L346 336Q350 340 358 349T379 373T411 410T454 461Q546 568 561 587T577 618Q577 634 545 637Q528 637 528 647Q528 649 530 661Q533 676 535 679T549 683Q551 683 578 682T657 680Q684 680 713 681T746 682Q763 682 763 673Q763 669 760 657T755 643Q753 637 734 637Q662 632 617 587Q608 578 477 424L348 273L322 169Q295 62 295 57Q295 46 363 46Q379 46 384 45T390 35Q390 33 388 23Q384 6 382 4T366 1Q361 1 324 1T232 2Q170 2 138 2T102 1Q84 1 84 9Q84 14 87 24Q88 27 89 30T90 35T91 39T93 42T96 44T101 45T107 45T116 46T129 46Q168 47 180 50T198 63Q201 68 227 171L252 274L129 623Q128 624 127 625T125 627T122 629T118 631T113 633T105 634T96 635T83 636T66 637Z"></path></g><g data-mml-node="mo" transform="translate(6081.3,0)"><path data-c="2223" d="M139 -249H137Q125 -249 119 -235V251L120 737Q130 750 139 750Q152 750 159 735V-235Q151 -249 141 -249H139Z"></path></g><g data-mml-node="mi" transform="translate(6637.1,0)"><path data-c="2203" d="M56 661T56 674T70 694H487Q497 686 500 679V15Q497 10 487 1L279 0H70Q56 7 56 20T70 40H460V327H84Q70 334 70 347T84 367H460V654H70Q56 661 56 674Z"></path></g><g data-mml-node="mi" transform="translate(7193.1,0)"><path data-c="1D443" d="M287 628Q287 635 230 637Q206 637 199 638T192 648Q192 649 194 659Q200 679 203 681T397 683Q587 682 600 680Q664 669 707 631T751 530Q751 453 685 389Q616 321 507 303Q500 302 402 301H307L277 182Q247 66 247 59Q247 55 248 54T255 50T272 48T305 46H336Q342 37 342 35Q342 19 335 5Q330 0 319 0Q316 0 282 1T182 2Q120 2 87 2T51 1Q33 1 33 11Q33 13 36 25Q40 41 44 43T67 46Q94 46 127 49Q141 52 146 61Q149 65 218 339T287 628ZM645 554Q645 567 643 575T634 597T609 619T560 635Q553 636 480 637Q463 637 445 637T416 636T404 636Q391 635 386 627Q384 621 367 550T332 412T314 344Q314 342 395 342H407H430Q542 342 590 392Q617 419 631 471T645 554Z"></path></g><g data-mml-node="mo" transform="translate(8221.9,0)"><path data-c="2208" d="M84 250Q84 372 166 450T360 539Q361 539 377 539T419 540T469 540H568Q583 532 583 520Q583 511 570 501L466 500Q355 499 329 494Q280 482 242 458T183 409T147 354T129 306T124 272V270H568Q583 262 583 250T568 230H124V228Q124 207 134 177T167 112T231 48T328 7Q355 1 466 0H570Q583 -10 583 -20Q583 -32 568 -40H471Q464 -40 446 -40T417 -41Q262 -41 172 45Q84 127 84 250Z"></path></g><g data-mml-node="TeXAtom" data-mjx-texclass="ORD" transform="translate(9166.7,0)"><g data-mml-node="mi"><path data-c="50" d="M62 0Q55 7 55 11Q55 27 62 41Q66 46 96 46Q132 47 143 50T160 62Q162 66 231 344T301 626Q301 627 300 629V631Q294 637 239 637H221Q215 642 215 645T217 664Q221 677 227 683H404H431H502Q578 683 615 675T684 636Q729 595 729 531Q729 462 671 396T524 308Q499 302 404 301H318L288 182Q258 63 258 55T310 46Q341 46 343 40Q345 37 342 21Q337 3 333 1Q330 0 321 0Q317 0 301 0T255 1T190 2Q97 2 72 0H62ZM626 555V562Q626 620 552 635Q546 636 481 637Q466 637 450 637T423 636T412 636Q401 635 398 627Q396 622 361 484Q353 452 344 416T330 362L325 344Q325 342 390 342H427Q523 342 567 386Q596 415 611 473T626 555Z"></path><path data-c="72" d="M81 278Q75 284 75 289Q77 301 89 339Q122 442 183 442Q219 442 241 425T271 384L283 396Q327 442 384 442Q424 442 454 421T484 362Q484 327 464 312T424 296Q407 296 396 305T385 331Q385 352 394 365T414 384T424 390Q409 405 378 405Q322 405 276 315L268 300L234 161Q200 25 196 16Q182 -11 152 -11T120 18Q120 23 159 181Q199 343 199 346Q202 360 202 372Q202 405 182 405Q164 405 150 377T128 316T117 280Q115 278 98 278H81Z" transform="translate(678,0)"></path><path data-c="65" d="M107 166Q107 230 131 283T193 369T270 420T345 441Q346 441 352 441T361 442H364Q409 442 439 418T470 355Q470 270 366 239Q308 223 218 223H205Q189 164 189 125Q189 83 206 55T261 27Q309 27 353 50T426 109Q436 121 440 121T453 111T466 97Q469 92 455 77Q424 41 372 16T258 -10Q184 -10 146 41T107 166ZM416 333T416 354T401 390T360 405Q322 405 292 384T246 336T223 288T215 261Q215 260 240 260Q262 261 276 262T314 266T353 275T384 291T408 317Z" transform="translate(1100,0)"></path><path data-c="64" d="M418 54Q418 26 438 26Q466 26 494 131Q500 151 504 152Q507 153 516 153H521H526Q543 153 543 144Q543 143 541 129Q531 91 521 65T487 14T434 -11T383 3T354 30T347 48H346Q345 47 342 45T337 40Q282 -11 228 -11Q172 -11 137 34T101 146Q101 255 174 348T337 441Q354 441 368 437T390 427T404 414T413 404T417 400L471 616Q471 629 464 632T425 637H404Q398 643 398 645T400 664Q405 683 413 683Q418 683 478 688T555 694Q567 694 567 686Q567 676 495 389L419 86Q418 80 418 61V54ZM397 323Q397 329 394 340T385 367T365 394T333 405Q302 405 271 372Q249 349 235 316T203 215Q184 135 184 108V100Q184 71 191 56Q204 26 235 26Q267 26 300 57T344 112Q347 117 372 217T397 323Z" transform="translate(1560,0)"></path></g></g><g data-mml-node="mo" transform="translate(11237.7,0)"><path data-c="28" d="M94 250Q94 319 104 381T127 488T164 576T202 643T244 695T277 729T302 750H315H319Q333 750 333 741Q333 738 316 720T275 667T226 581T184 443T167 250T184 58T225 -81T274 -167T316 -220T333 -241Q333 -250 318 -250H315H302L274 -226Q180 -141 137 -14T94 250Z"></path></g><g data-mml-node="mi" transform="translate(11626.7,0)"><path data-c="1D44C" d="M66 637Q54 637 49 637T39 638T32 641T30 647T33 664T42 682Q44 683 56 683Q104 680 165 680Q288 680 306 683H316Q322 677 322 674T320 656Q316 643 310 637H298Q242 637 242 624Q242 619 292 477T343 333L346 336Q350 340 358 349T379 373T411 410T454 461Q546 568 561 587T577 618Q577 634 545 637Q528 637 528 647Q528 649 530 661Q533 676 535 679T549 683Q551 683 578 682T657 680Q684 680 713 681T746 682Q763 682 763 673Q763 669 760 657T755 643Q753 637 734 637Q662 632 617 587Q608 578 477 424L348 273L322 169Q295 62 295 57Q295 46 363 46Q379 46 384 45T390 35Q390 33 388 23Q384 6 382 4T366 1Q361 1 324 1T232 2Q170 2 138 2T102 1Q84 1 84 9Q84 14 87 24Q88 27 89 30T90 35T91 39T93 42T96 44T101 45T107 45T116 46T129 46Q168 47 180 50T198 63Q201 68 227 171L252 274L129 623Q128 624 127 625T125 627T122 629T118 631T113 633T105 634T96 635T83 636T66 637Z"></path></g><g data-mml-node="mo" transform="translate(12389.7,0)"><path data-c="29" d="M60 749L64 750Q69 750 74 750H86L114 726Q208 641 251 514T294 250Q294 182 284 119T261 12T224 -76T186 -143T145 -194T113 -227T90 -246Q87 -249 86 -250H74Q66 -250 63 -250T58 -247T55 -238Q56 -237 66 -225Q221 -64 221 250T66 725Q56 737 55 738Q55 746 60 749Z"></path></g><g data-mml-node="mo" transform="translate(12778.7,0)"><path data-c="2C" d="M78 35T78 60T94 103T137 121Q165 121 187 96T210 8Q210 -27 201 -60T180 -117T154 -158T130 -185T117 -194Q113 -194 104 -185T95 -172Q95 -168 106 -156T131 -126T157 -76T173 -3V9L172 8Q170 7 167 6T161 3T152 1T140 0Q113 0 96 17Z"></path></g><g data-mml-node="mstyle" transform="translate(13056.7,0)"><g data-mml-node="mspace"></g></g><g data-mml-node="mi" transform="translate(13501.3,0)"><path data-c="1D44B" d="M42 0H40Q26 0 26 11Q26 15 29 27Q33 41 36 43T55 46Q141 49 190 98Q200 108 306 224T411 342Q302 620 297 625Q288 636 234 637H206Q200 643 200 645T202 664Q206 677 212 683H226Q260 681 347 681Q380 681 408 681T453 682T473 682Q490 682 490 671Q490 670 488 658Q484 643 481 640T465 637Q434 634 411 620L488 426L541 485Q646 598 646 610Q646 628 622 635Q617 635 609 637Q594 637 594 648Q594 650 596 664Q600 677 606 683H618Q619 683 643 683T697 681T738 680Q828 680 837 683H845Q852 676 852 672Q850 647 840 637H824Q790 636 763 628T722 611T698 593L687 584Q687 585 592 480L505 384Q505 383 536 304T601 142T638 56Q648 47 699 46Q734 46 734 37Q734 35 732 23Q728 7 725 4T711 1Q708 1 678 1T589 2Q528 2 496 2T461 1Q444 1 444 10Q444 11 446 25Q448 35 450 39T455 44T464 46T480 47T506 54Q523 62 523 64Q522 64 476 181L429 299Q241 95 236 84Q232 76 232 72Q232 53 261 47Q262 47 267 47T273 46Q276 46 277 46T280 45T283 42T284 35Q284 26 282 19Q279 6 276 4T261 1Q258 1 243 1T201 2T142 2Q64 2 42 0Z"></path></g><g data-mml-node="mtext" transform="translate(14353.3,0)"><path data-c="A0" d=""></path></g><g data-mml-node="TeXAtom" data-mjx-texclass="ORD" transform="translate(14603.3,0)"><g data-mml-node="mi"><path data-c="64" d="M376 495Q376 511 376 535T377 568Q377 613 367 624T316 637H298V660Q298 683 300 683L310 684Q320 685 339 686T376 688Q393 689 413 690T443 693T454 694H457V390Q457 84 458 81Q461 61 472 55T517 46H535V0Q533 0 459 -5T380 -11H373V44L365 37Q307 -11 235 -11Q158 -11 96 50T34 215Q34 315 97 378T244 442Q319 442 376 393V495ZM373 342Q328 405 260 405Q211 405 173 369Q146 341 139 305T131 211Q131 155 138 120T173 59Q203 26 251 26Q322 26 373 103V342Z"></path><path data-c="6F" d="M28 214Q28 309 93 378T250 448Q340 448 405 380T471 215Q471 120 407 55T250 -10Q153 -10 91 57T28 214ZM250 30Q372 30 372 193V225V250Q372 272 371 288T364 326T348 362T317 390T268 410Q263 411 252 411Q222 411 195 399Q152 377 139 338T126 246V226Q126 130 145 91Q177 30 250 30Z" transform="translate(556,0)"></path><path data-c="6D" d="M41 46H55Q94 46 102 60V68Q102 77 102 91T102 122T103 161T103 203Q103 234 103 269T102 328V351Q99 370 88 376T43 385H25V408Q25 431 27 431L37 432Q47 433 65 434T102 436Q119 437 138 438T167 441T178 442H181V402Q181 364 182 364T187 369T199 384T218 402T247 421T285 437Q305 442 336 442Q351 442 364 440T387 434T406 426T421 417T432 406T441 395T448 384T452 374T455 366L457 361L460 365Q463 369 466 373T475 384T488 397T503 410T523 422T546 432T572 439T603 442Q729 442 740 329Q741 322 741 190V104Q741 66 743 59T754 49Q775 46 803 46H819V0H811L788 1Q764 2 737 2T699 3Q596 3 587 0H579V46H595Q656 46 656 62Q657 64 657 200Q656 335 655 343Q649 371 635 385T611 402T585 404Q540 404 506 370Q479 343 472 315T464 232V168V108Q464 78 465 68T468 55T477 49Q498 46 526 46H542V0H534L510 1Q487 2 460 2T422 3Q319 3 310 0H302V46H318Q379 46 379 62Q380 64 380 200Q379 335 378 343Q372 371 358 385T334 402T308 404Q263 404 229 370Q202 343 195 315T187 232V168V108Q187 78 188 68T191 55T200 49Q221 46 249 46H265V0H257L234 1Q210 2 183 2T145 3Q42 3 33 0H25V46H41Z" transform="translate(1056,0)"></path></g></g><g data-mml-node="mtext" transform="translate(16492.3,0)"><path data-c="A0" d=""></path></g><g data-mml-node="mi" transform="translate(16742.3,0)"><path data-c="1D443" d="M287 628Q287 635 230 637Q206 637 199 638T192 648Q192 649 194 659Q200 679 203 681T397 683Q587 682 600 680Q664 669 707 631T751 530Q751 453 685 389Q616 321 507 303Q500 302 402 301H307L277 182Q247 66 247 59Q247 55 248 54T255 50T272 48T305 46H336Q342 37 342 35Q342 19 335 5Q330 0 319 0Q316 0 282 1T182 2Q120 2 87 2T51 1Q33 1 33 11Q33 13 36 25Q40 41 44 43T67 46Q94 46 127 49Q141 52 146 61Q149 65 218 339T287 628ZM645 554Q645 567 643 575T634 597T609 619T560 635Q553 636 480 637Q463 637 445 637T416 636T404 636Q391 635 386 627Q384 621 367 550T332 412T314 344Q314 342 395 342H407H430Q542 342 590 392Q617 419 631 471T645 554Z"></path></g><g data-mml-node="mo" transform="translate(17715.6,0)"><path data-c="2227" d="M318 591Q325 598 333 598Q344 598 348 591Q349 590 414 445T545 151T611 -4Q609 -22 591 -22Q588 -22 586 -21T581 -20T577 -17T575 -13T572 -9T570 -4L333 528L96 -4Q87 -20 80 -21Q78 -22 75 -22Q57 -22 55 -4Q55 2 120 150T251 444T318 591Z"></path></g><g data-mml-node="mi" transform="translate(18604.8,0)"><path data-c="1D44B" d="M42 0H40Q26 0 26 11Q26 15 29 27Q33 41 36 43T55 46Q141 49 190 98Q200 108 306 224T411 342Q302 620 297 625Q288 636 234 637H206Q200 643 200 645T202 664Q206 677 212 683H226Q260 681 347 681Q380 681 408 681T453 682T473 682Q490 682 490 671Q490 670 488 658Q484 643 481 640T465 637Q434 634 411 620L488 426L541 485Q646 598 646 610Q646 628 622 635Q617 635 609 637Q594 637 594 648Q594 650 596 664Q600 677 606 683H618Q619 683 643 683T697 681T738 680Q828 680 837 683H845Q852 676 852 672Q850 647 840 637H824Q790 636 763 628T722 611T698 593L687 584Q687 585 592 480L505 384Q505 383 536 304T601 142T638 56Q648 47 699 46Q734 46 734 37Q734 35 732 23Q728 7 725 4T711 1Q708 1 678 1T589 2Q528 2 496 2T461 1Q444 1 444 10Q444 11 446 25Q448 35 450 39T455 44T464 46T480 47T506 54Q523 62 523 64Q522 64 476 181L429 299Q241 95 236 84Q232 76 232 72Q232 53 261 47Q262 47 267 47T273 46Q276 46 277 46T280 45T283 42T284 35Q284 26 282 19Q279 6 276 4T261 1Q258 1 243 1T201 2T142 2Q64 2 42 0Z"></path></g><g data-mml-node="mtext" transform="translate(19456.8,0)"><path data-c="A0" d=""></path></g><g data-mml-node="mo" transform="translate(19706.8,0)"><path data-c="21" d="M78 661Q78 682 96 699T138 716T180 700T199 661Q199 654 179 432T158 206Q156 198 139 198Q121 198 119 206Q118 209 98 431T78 661ZM79 61Q79 89 97 105T141 121Q164 119 181 104T198 61Q198 31 181 16T139 1Q114 1 97 16T79 61Z"></path></g><g data-mml-node="TeXAtom" data-mjx-texclass="ORD" transform="translate(19984.8,0)"><g data-mml-node="mi"><path data-c="73" d="M295 316Q295 356 268 385T190 414Q154 414 128 401Q98 382 98 349Q97 344 98 336T114 312T157 287Q175 282 201 278T245 269T277 256Q294 248 310 236T342 195T359 133Q359 71 321 31T198 -10H190Q138 -10 94 26L86 19L77 10Q71 4 65 -1L54 -11H46H42Q39 -11 33 -5V74V132Q33 153 35 157T45 162H54Q66 162 70 158T75 146T82 119T101 77Q136 26 198 26Q295 26 295 104Q295 133 277 151Q257 175 194 187T111 210Q75 227 54 256T33 318Q33 357 50 384T93 424T143 442T187 447H198Q238 447 268 432L283 424L292 431Q302 440 314 448H322H326Q329 448 335 442V310L329 304H301Q295 310 295 316Z"></path><path data-c="64" d="M376 495Q376 511 376 535T377 568Q377 613 367 624T316 637H298V660Q298 683 300 683L310 684Q320 685 339 686T376 688Q393 689 413 690T443 693T454 694H457V390Q457 84 458 81Q461 61 472 55T517 46H535V0Q533 0 459 -5T380 -11H373V44L365 37Q307 -11 235 -11Q158 -11 96 50T34 215Q34 315 97 378T244 442Q319 442 376 393V495ZM373 342Q328 405 260 405Q211 405 173 369Q146 341 139 305T131 211Q131 155 138 120T173 59Q203 26 251 26Q322 26 373 103V342Z" transform="translate(394,0)"></path><path data-c="6F" d="M28 214Q28 309 93 378T250 448Q340 448 405 380T471 215Q471 120 407 55T250 -10Q153 -10 91 57T28 214ZM250 30Q372 30 372 193V225V250Q372 272 371 288T364 326T348 362T317 390T268 410Q263 411 252 411Q222 411 195 399Q152 377 139 338T126 246V226Q126 130 145 91Q177 30 250 30Z" transform="translate(950,0)"></path><path data-c="6D" d="M41 46H55Q94 46 102 60V68Q102 77 102 91T102 122T103 161T103 203Q103 234 103 269T102 328V351Q99 370 88 376T43 385H25V408Q25 431 27 431L37 432Q47 433 65 434T102 436Q119 437 138 438T167 441T178 442H181V402Q181 364 182 364T187 369T199 384T218 402T247 421T285 437Q305 442 336 442Q351 442 364 440T387 434T406 426T421 417T432 406T441 395T448 384T452 374T455 366L457 361L460 365Q463 369 466 373T475 384T488 397T503 410T523 422T546 432T572 439T603 442Q729 442 740 329Q741 322 741 190V104Q741 66 743 59T754 49Q775 46 803 46H819V0H811L788 1Q764 2 737 2T699 3Q596 3 587 0H579V46H595Q656 46 656 62Q657 64 657 200Q656 335 655 343Q649 371 635 385T611 402T585 404Q540 404 506 370Q479 343 472 315T464 232V168V108Q464 78 465 68T468 55T477 49Q498 46 526 46H542V0H534L510 1Q487 2 460 2T422 3Q319 3 310 0H302V46H318Q379 46 379 62Q380 64 380 200Q379 335 378 343Q372 371 358 385T334 402T308 404Q263 404 229 370Q202 343 195 315T187 232V168V108Q187 78 188 68T191 55T200 49Q221 46 249 46H265V0H257L234 1Q210 2 183 2T145 3Q42 3 33 0H25V46H41Z" transform="translate(1450,0)"></path></g></g><g data-mml-node="mtext" transform="translate(22267.8,0)"><path data-c="A0" d=""></path></g><g data-mml-node="mi" transform="translate(22517.8,0)"><path data-c="1D44C" d="M66 637Q54 637 49 637T39 638T32 641T30 647T33 664T42 682Q44 683 56 683Q104 680 165 680Q288 680 306 683H316Q322 677 322 674T320 656Q316 643 310 637H298Q242 637 242 624Q242 619 292 477T343 333L346 336Q350 340 358 349T379 373T411 410T454 461Q546 568 561 587T577 618Q577 634 545 637Q528 637 528 647Q528 649 530 661Q533 676 535 679T549 683Q551 683 578 682T657 680Q684 680 713 681T746 682Q763 682 763 673Q763 669 760 657T755 643Q753 637 734 637Q662 632 617 587Q608 578 477 424L348 273L322 169Q295 62 295 57Q295 46 363 46Q379 46 384 45T390 35Q390 33 388 23Q384 6 382 4T366 1Q361 1 324 1T232 2Q170 2 138 2T102 1Q84 1 84 9Q84 14 87 24Q88 27 89 30T90 35T91 39T93 42T96 44T101 45T107 45T116 46T129 46Q168 47 180 50T198 63Q201 68 227 171L252 274L129 623Q128 624 127 625T125 627T122 629T118 631T113 633T105 634T96 635T83 636T66 637Z"></path></g><g data-mml-node="mo" transform="translate(23280.8,0)"><path data-c="7D" d="M65 731Q65 745 68 747T88 750Q171 750 216 725T279 670Q288 649 289 635T291 501Q292 362 293 357Q306 312 345 291T417 269Q428 269 431 266T434 250T431 234T417 231Q380 231 345 210T298 157Q293 143 292 121T291 -28V-79Q291 -134 285 -156T256 -198Q202 -250 89 -250Q71 -250 68 -247T65 -230Q65 -224 65 -223T66 -218T69 -214T77 -213Q91 -213 108 -210T146 -200T183 -177T207 -139Q208 -134 209 3L210 139Q223 196 280 230Q315 247 330 250Q305 257 280 270Q225 304 212 352L210 362L209 498Q208 635 207 640Q195 680 154 696T77 713Q68 713 67 716T65 731Z"></path></g></g></g></svg></mjx-container></p>
</li>
</ol>
<h3 id="2-3-遇到的困难"><a href="#2-3-遇到的困难" class="headerlink" title="2.3 遇到的困难"></a>2.3 遇到的困难</h3><ul>
<li><strong>图不连通</strong>：某些基本块可能因优化变成孤立块，需在 CFG 构建后清理。</li>
<li><strong>循环处理</strong>：回边的存在使得支配关系计算需要多轮迭代才能收敛。</li>
</ul>
<hr>
<h2 id="3-不可达代码消除-RemoveUnReachCode"><a href="#3-不可达代码消除-RemoveUnReachCode" class="headerlink" title="3. 不可达代码消除 (RemoveUnReachCode)"></a>3. 不可达代码消除 (RemoveUnReachCode)</h2><h3 id="3-1-功能描述"><a href="#3-1-功能描述" class="headerlink" title="3.1 功能描述"></a>3.1 功能描述</h3><p>删除从函数入口无法到达的基本块以及跳转指令后的死指令。</p>
<h3 id="3-2-实现细节"><a href="#3-2-实现细节" class="headerlink" title="3.2 实现细节"></a>3.2 实现细节</h3><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1. 删除跳转后的指令</span></span><br><span class="line"><span class="keyword">for</span> (IrBasicBlock block : function.getBasicBlocks()) {</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">hasJump</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">    Iterator&lt;Instruction&gt; it = block.getInstructions().iterator();</span><br><span class="line">    <span class="keyword">while</span> (it.hasNext()) {</span><br><span class="line">        <span class="type">Instruction</span> <span class="variable">instr</span> <span class="operator">=</span> it.next();</span><br><span class="line">        <span class="keyword">if</span> (hasJump) {</span><br><span class="line">            instr.removeAllValueUse();</span><br><span class="line">            it.remove();</span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">if</span> (instr <span class="keyword">instanceof</span> JumpInstr || instr <span class="keyword">instanceof</span> BranchInstr </span><br><span class="line">            || instr <span class="keyword">instanceof</span> ReturnInstr) {</span><br><span class="line">            hasJump = <span class="literal">true</span>;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. DFS 标记可达块</span></span><br><span class="line"><span class="comment">// 3. 删除未标记的块</span></span><br></pre></td></tr></table></figure></div>
<h3 id="3-3-遇到的困难"><a href="#3-3-遇到的困难" class="headerlink" title="3.3 遇到的困难"></a>3.3 遇到的困难</h3><ul>
<li><strong>Use-Def 链维护</strong>：删除指令时必须调用 <code>removeAllValueUse()</code> 断开所有引用，否则后续优化会访问到悬空指针。</li>
</ul>
<hr>
<h2 id="4-死代码消除-RemoveDeadCode"><a href="#4-死代码消除-RemoveDeadCode" class="headerlink" title="4. 死代码消除 (RemoveDeadCode)"></a>4. 死代码消除 (RemoveDeadCode)</h2><h3 id="4-1-功能描述"><a href="#4-1-功能描述" class="headerlink" title="4.1 功能描述"></a>4.1 功能描述</h3><p>基于活跃性分析，删除结果未被使用且无副作用的指令。</p>
<h3 id="4-2-核心算法"><a href="#4-2-核心算法" class="headerlink" title="4.2 核心算法"></a>4.2 核心算法</h3><p>采用 Mark-Sweep 思想：</p>
<ol>
<li><p><strong>标记关键指令</strong>（Critical Instructions）：</p>
<ul>
<li><code>ReturnInstr</code>：函数必须返回</li>
<li><code>CallInstr</code>：调用有副作用的函数（IO、修改全局变量）</li>
<li><code>StoreInstr</code>：内存写入</li>
<li><code>BranchInstr</code> / <code>JumpInstr</code>：控制流</li>
</ul>
</li>
<li><p><strong>反向传播</strong>：从关键指令出发，将其操作数（若为指令）也标记为活跃。</p>
</li>
<li><p><strong>清扫</strong>：删除未标记的指令。</p>
</li>
</ol>
<h3 id="4-3-副作用函数识别"><a href="#4-3-副作用函数识别" class="headerlink" title="4.3 副作用函数识别"></a>4.3 副作用函数识别</h3><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">dfsSideFunction</span><span class="params">(IrFunction func, HashSet&lt;IrFunction&gt; visited)</span> {</span><br><span class="line">    <span class="comment">// 递归分析调用链</span></span><br><span class="line">    <span class="keyword">if</span> (函数调用了 IO 函数或包含 Store) {</span><br><span class="line">        sideEffectFunctions.add(func);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<h3 id="4-4-额外功能"><a href="#4-4-额外功能" class="headerlink" title="4.4 额外功能"></a>4.4 额外功能</h3><ul>
<li><strong>无用 Phi 删除</strong>：若 Phi 只有一个入边值，直接替换为该值。</li>
<li><strong>基本块合并</strong>：若块 A 只有一个后继 B，B 只有一个前驱 A，可合并。</li>
<li><strong>无用函数删除</strong>：若函数从未被调用（除 main 外），直接删除。</li>
</ul>
<h3 id="4-5-遇到的困难"><a href="#4-5-遇到的困难" class="headerlink" title="4.5 遇到的困难"></a>4.5 遇到的困难</h3><ul>
<li><strong>全局变量副作用传播</strong>：函数 A 调用 B，B 修改全局变量，则 A 也有副作用。需建立调用图进行传递闭包。</li>
<li><strong>条件删除的正确性</strong>：即使 branch 条件可常量折叠为 true，也不能直接删除 branch 指令，应替换为 jump。</li>
</ul>
<hr>
<h2 id="5-Mem2Reg-与-SSA-构造"><a href="#5-Mem2Reg-与-SSA-构造" class="headerlink" title="5. Mem2Reg 与 SSA 构造"></a>5. Mem2Reg 与 SSA 构造</h2><h3 id="5-1-功能描述"><a href="#5-1-功能描述" class="headerlink" title="5.1 功能描述"></a>5.1 功能描述</h3><p>将局部标量变量的 <code>alloca</code> + <code>load</code> + <code>store</code> 模式提升为虚拟寄存器操作，构造 SSA 形式。</p>
<h3 id="5-2-适用条件"><a href="#5-2-适用条件" class="headerlink" title="5.2 适用条件"></a>5.2 适用条件</h3><p>仅处理 <strong>非数组</strong> 的 <code>AllocateInstr</code>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="type">boolean</span> <span class="title function_">isValueAllocate</span><span class="params">(Instruction instr)</span> {</span><br><span class="line">    <span class="keyword">if</span> (instr <span class="keyword">instanceof</span> AllocateInstr) {</span><br><span class="line">        <span class="type">IrType</span> <span class="variable">targetType</span> <span class="operator">=</span> ((IrPointerType) instr.getType()).getTargetType();</span><br><span class="line">        <span class="keyword">return</span> !(targetType <span class="keyword">instanceof</span> IrArrayType);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h3 id="5-3-实现流程"><a href="#5-3-实现流程" class="headerlink" title="5.3 实现流程"></a>5.3 实现流程</h3><p>对于每个可提升的 <code>alloca</code>：</p>
<ol>
<li>调用 <code>InsertPhi</code> 在支配边界插入 Phi 指令</li>
<li>按支配序重命名变量，替换 load/store</li>
</ol>
<hr>
<h2 id="6-Phi-指令插入-InsertPhi"><a href="#6-Phi-指令插入-InsertPhi" class="headerlink" title="6. Phi 指令插入 (InsertPhi)"></a>6. Phi 指令插入 (InsertPhi)</h2><h3 id="6-1-功能描述"><a href="#6-1-功能描述" class="headerlink" title="6.1 功能描述"></a>6.1 功能描述</h3><p>为 Mem2Reg 服务，在控制流汇聚点插入 Phi 指令。</p>
<h3 id="6-2-算法步骤"><a href="#6-2-算法步骤" class="headerlink" title="6.2 算法步骤"></a>6.2 算法步骤</h3><ol>
<li><p><strong>收集定值点和使用点</strong>：遍历 <code>alloca</code> 的所有 user，分类为 Store（定值）和 Load（使用）。</p>
</li>
<li><p><strong>在支配边界插入 Phi</strong>：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (!defineBlockStack.isEmpty()) {</span><br><span class="line">    <span class="type">IrBasicBlock</span> <span class="variable">defBlock</span> <span class="operator">=</span> defineBlockStack.pop();</span><br><span class="line">    <span class="keyword">for</span> (IrBasicBlock frontier : defBlock.getDominateFrontiers()) {</span><br><span class="line">        <span class="keyword">if</span> (!addedPhiBlocks.contains(frontier)) {</span><br><span class="line">            insertPhiInstr(frontier);</span><br><span class="line">            addedPhiBlocks.add(frontier);</span><br><span class="line">            <span class="comment">// Phi 本身也是定值点，继续传播</span></span><br><span class="line">            <span class="keyword">if</span> (!defineBlocks.contains(frontier)) {</span><br><span class="line">                defineBlockStack.push(frontier);</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>变量重命名</strong>：按支配树 DFS，维护值栈：</p>
<ul>
<li>遇到 Store：入栈</li>
<li>遇到 Load：替换为栈顶值</li>
<li>遇到 Phi（作为定值）：入栈</li>
<li>离开块时恢复栈状态</li>
</ul>
</li>
</ol>
<h3 id="6-3-遇到的困难"><a href="#6-3-遇到的困难" class="headerlink" title="6.3 遇到的困难"></a>6.3 遇到的困难</h3><ul>
<li><strong>Phi 操作数顺序</strong>：Phi 的入边值与前驱块的顺序必须严格对应。</li>
<li><strong>未定义变量</strong>：若变量在某路径上未赋值就使用，栈为空，此时返回默认值 <code>0</code>。</li>
</ul>
<hr>
<h2 id="7-Phi-指令消除-RemovePhi"><a href="#7-Phi-指令消除-RemovePhi" class="headerlink" title="7. Phi 指令消除 (RemovePhi)"></a>7. Phi 指令消除 (RemovePhi)</h2><h3 id="7-1-功能描述"><a href="#7-1-功能描述" class="headerlink" title="7.1 功能描述"></a>7.1 功能描述</h3><p>将 SSA 形式的 Phi 指令转换为并行复制（Parallel Copy），再转换为 Move 指令，以便后端生成 MIPS。</p>
<h3 id="7-2-算法步骤"><a href="#7-2-算法步骤" class="headerlink" title="7.2 算法步骤"></a>7.2 算法步骤</h3><ol>
<li><p><strong>Phi -&gt; Parallel Copy</strong>：</p>
<ul>
<li>若块只有一个前驱：在前驱块末尾插入 ParallelCopyInstr</li>
<li>若块有多个前驱：插入中间块，避免关键边问题</li>
</ul>
</li>
<li><p><strong>Parallel Copy -&gt; Move 序列</strong>：</p>
<ul>
<li>检测循环冲突（<mjx-container class="MathJax" jax="SVG"><svg style="vertical-align: -0.439ex;" xmlns="http://www.w3.org/2000/svg" width="14.642ex" height="2.009ex" role="img" focusable="false" viewBox="0 -694 6471.8 888"><g stroke="currentColor" fill="currentColor" stroke-width="0" transform="scale(1,-1)"><g data-mml-node="math"><g data-mml-node="mi"><path data-c="1D44E" d="M33 157Q33 258 109 349T280 441Q331 441 370 392Q386 422 416 422Q429 422 439 414T449 394Q449 381 412 234T374 68Q374 43 381 35T402 26Q411 27 422 35Q443 55 463 131Q469 151 473 152Q475 153 483 153H487Q506 153 506 144Q506 138 501 117T481 63T449 13Q436 0 417 -8Q409 -10 393 -10Q359 -10 336 5T306 36L300 51Q299 52 296 50Q294 48 292 46Q233 -10 172 -10Q117 -10 75 30T33 157ZM351 328Q351 334 346 350T323 385T277 405Q242 405 210 374T160 293Q131 214 119 129Q119 126 119 118T118 106Q118 61 136 44T179 26Q217 26 254 59T298 110Q300 114 325 217T351 328Z"></path></g><g data-mml-node="mo" transform="translate(806.8,0)"><path data-c="2190" d="M944 261T944 250T929 230H165Q167 228 182 216T211 189T244 152T277 96T303 25Q308 7 308 0Q308 -11 288 -11Q281 -11 278 -11T272 -7T267 2T263 21Q245 94 195 151T73 236Q58 242 55 247Q55 254 59 257T73 264Q121 283 158 314T215 375T247 434T264 480L267 497Q269 503 270 505T275 509T288 511Q308 511 308 500Q308 493 303 475Q293 438 278 406T246 352T215 315T185 287T165 270H929Q944 261 944 250Z"></path></g><g data-mml-node="mi" transform="translate(2084.6,0)"><path data-c="1D44F" d="M73 647Q73 657 77 670T89 683Q90 683 161 688T234 694Q246 694 246 685T212 542Q204 508 195 472T180 418L176 399Q176 396 182 402Q231 442 283 442Q345 442 383 396T422 280Q422 169 343 79T173 -11Q123 -11 82 27T40 150V159Q40 180 48 217T97 414Q147 611 147 623T109 637Q104 637 101 637H96Q86 637 83 637T76 640T73 647ZM336 325V331Q336 405 275 405Q258 405 240 397T207 376T181 352T163 330L157 322L136 236Q114 150 114 114Q114 66 138 42Q154 26 178 26Q211 26 245 58Q270 81 285 114T318 219Q336 291 336 325Z"></path></g><g data-mml-node="mo" transform="translate(2513.6,0)"><path data-c="2C" d="M78 35T78 60T94 103T137 121Q165 121 187 96T210 8Q210 -27 201 -60T180 -117T154 -158T130 -185T117 -194Q113 -194 104 -185T95 -172Q95 -168 106 -156T131 -126T157 -76T173 -3V9L172 8Q170 7 167 6T161 3T152 1T140 0Q113 0 96 17Z"></path></g><g data-mml-node="mstyle" transform="translate(2791.6,0)"><g data-mml-node="mspace"></g></g><g data-mml-node="mi" transform="translate(3958.2,0)"><path data-c="1D44F" d="M73 647Q73 657 77 670T89 683Q90 683 161 688T234 694Q246 694 246 685T212 542Q204 508 195 472T180 418L176 399Q176 396 182 402Q231 442 283 442Q345 442 383 396T422 280Q422 169 343 79T173 -11Q123 -11 82 27T40 150V159Q40 180 48 217T97 414Q147 611 147 623T109 637Q104 637 101 637H96Q86 637 83 637T76 640T73 647ZM336 325V331Q336 405 275 405Q258 405 240 397T207 376T181 352T163 330L157 322L136 236Q114 150 114 114Q114 66 138 42Q154 26 178 26Q211 26 245 58Q270 81 285 114T318 219Q336 291 336 325Z"></path></g><g data-mml-node="mo" transform="translate(4665,0)"><path data-c="2190" d="M944 261T944 250T929 230H165Q167 228 182 216T211 189T244 152T277 96T303 25Q308 7 308 0Q308 -11 288 -11Q281 -11 278 -11T272 -7T267 2T263 21Q245 94 195 151T73 236Q58 242 55 247Q55 254 59 257T73 264Q121 283 158 314T215 375T247 434T264 480L267 497Q269 503 270 505T275 509T288 511Q308 511 308 500Q308 493 303 475Q293 438 278 406T246 352T215 315T185 287T165 270H929Q944 261 944 250Z"></path></g><g data-mml-node="mi" transform="translate(5942.8,0)"><path data-c="1D44E" d="M33 157Q33 258 109 349T280 441Q331 441 370 392Q386 422 416 422Q429 422 439 414T449 394Q449 381 412 234T374 68Q374 43 381 35T402 26Q411 27 422 35Q443 55 463 131Q469 151 473 152Q475 153 483 153H487Q506 153 506 144Q506 138 501 117T481 63T449 13Q436 0 417 -8Q409 -10 393 -10Q359 -10 336 5T306 36L300 51Q299 52 296 50Q294 48 292 46Q233 -10 172 -10Q117 -10 75 30T33 157ZM351 328Q351 334 346 350T323 385T277 405Q242 405 210 374T160 293Q131 214 119 129Q119 126 119 118T118 106Q118 61 136 44T179 26Q217 26 254 59T298 110Q300 114 325 217T351 328Z"></path></g></g></g></svg></mjx-container>）：引入临时变量打破循环</li>
<li>检测寄存器冲突：若 src 被后续 dst 覆盖，提前备份</li>
</ul>
</li>
</ol>
<h3 id="7-3-实现代码片段"><a href="#7-3-实现代码片段" class="headerlink" title="7.3 实现代码片段"></a>7.3 实现代码片段</h3><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 检测循环冲突</span></span><br><span class="line"><span class="keyword">private</span> <span class="type">boolean</span> <span class="title function_">haveCircleConflict</span><span class="params">(ParallelCopyInstr copy, <span class="type">int</span> index)</span> {</span><br><span class="line">    <span class="type">IrValue</span> <span class="variable">dstValue</span> <span class="operator">=</span> copy.getDstList().get(index);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> index + <span class="number">1</span>; i &lt; srcList.size(); i++) {</span><br><span class="line">        <span class="keyword">if</span> (srcList.get(i).equals(dstValue)) {</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>; <span class="comment">// 后续有 move 使用当前 dst 作为 src</span></span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<h3 id="7-4-遇到的困难"><a href="#7-4-遇到的困难" class="headerlink" title="7.4 遇到的困难"></a>7.4 遇到的困难</h3><ul>
<li><strong>关键边</strong>：当一个块有多个前驱，且某前驱有多个后继时，不能直接在前驱块末尾插入 copy，需插入中间块。</li>
<li><strong>MIPS 生成时机</strong>：<code>RemovePhi</code> 必须在 LLVM IR 输出后、MIPS 生成前执行，否则 Move 指令会污染 IR。</li>
</ul>
<hr>
<h2 id="8-全局值编号与代数化简-Lvn-GVN"><a href="#8-全局值编号与代数化简-Lvn-GVN" class="headerlink" title="8. 全局值编号与代数化简 (Lvn/GVN)"></a>8. 全局值编号与代数化简 (Lvn/GVN)</h2><h3 id="8-1-功能描述"><a href="#8-1-功能描述" class="headerlink" title="8.1 功能描述"></a>8.1 功能描述</h3><p><code>Lvn</code> 实现了两个核心功能：</p>
<ol>
<li><strong>全局值编号 (GVN)</strong>：消除冗余计算</li>
<li><strong>代数化简</strong>：常量折叠、恒等式化简</li>
</ol>
<h3 id="8-2-GVN-实现"><a href="#8-2-GVN-实现" class="headerlink" title="8.2 GVN 实现"></a>8.2 GVN 实现</h3><p>按支配树 DFS 遍历，维护哈希表：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">gvnVisit</span><span class="params">(IrBasicBlock block)</span> {</span><br><span class="line">    foldValue(block);  <span class="comment">// 常量折叠</span></span><br><span class="line">    foldInstr(block, addedInstr);  <span class="comment">// GVN</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (IrBasicBlock child : block.getDirectDominateBlocks()) {</span><br><span class="line">        gvnVisit(child);</span><br><span class="line">    }</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 退出时移除本块加入的映射（作用域）</span></span><br><span class="line">    <span class="keyword">for</span> (Instruction instr : addedInstr) {</span><br><span class="line">        gvnHashMap.remove(instr.getGvnHash());</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h3 id="8-3-哈希冲突与交换律"><a href="#8-3-哈希冲突与交换律" class="headerlink" title="8.3 哈希冲突与交换律"></a>8.3 哈希冲突与交换律</h3><p>对于比较指令 <code>icmp eq %a, %b</code> 和 <code>icmp eq %b, %a</code>，必须生成相同哈希：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// CompareInstr.java</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">getGvnHash</span><span class="params">()</span> {</span><br><span class="line">    <span class="type">String</span> <span class="variable">op1</span> <span class="operator">=</span> getHashForValue(getOp1());</span><br><span class="line">    <span class="type">String</span> <span class="variable">op2</span> <span class="operator">=</span> getHashForValue(getOp2());</span><br><span class="line">    <span class="keyword">if</span> (cond == EQ || cond == NE) {</span><br><span class="line">        <span class="comment">// 对于交换律成立的比较，排序操作数</span></span><br><span class="line">        <span class="keyword">if</span> (op1.compareTo(op2) &gt; <span class="number">0</span>) {</span><br><span class="line">            <span class="keyword">return</span> op2 + <span class="string">" "</span> + cond + <span class="string">" "</span> + op1;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> op1 + <span class="string">" "</span> + cond + <span class="string">" "</span> + op2;</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h3 id="8-4-代数化简规则"><a href="#8-4-代数化简规则" class="headerlink" title="8.4 代数化简规则"></a>8.4 代数化简规则</h3><div class="table-container">
<table>
<thead>
<tr>
<th>表达式</th>
<th>化简结果</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>x + 0</code></td>
<td><code>x</code></td>
</tr>
<tr>
<td><code>x - 0</code></td>
<td><code>x</code></td>
</tr>
<tr>
<td><code>x - x</code></td>
<td><code>0</code></td>
</tr>
<tr>
<td><code>x * 0</code></td>
<td><code>0</code></td>
</tr>
<tr>
<td><code>x * 1</code></td>
<td><code>x</code></td>
</tr>
<tr>
<td><code>x / 1</code></td>
<td><code>x</code></td>
</tr>
<tr>
<td><code>x / x</code></td>
<td><code>1</code></td>
</tr>
<tr>
<td><code>x % 1</code></td>
<td><code>0</code></td>
</tr>
<tr>
<td><code>0 &amp;&amp; x</code></td>
<td><code>0</code></td>
</tr>
<tr>
<td><code>1 &amp;&amp; x</code></td>
<td><code>x</code></td>
</tr>
<tr>
<td>`0</td>
<td></td>
<td>x`</td>
<td><code>x</code></td>
</tr>
<tr>
<td>`1</td>
<td></td>
<td>x`</td>
<td><code>1</code></td>
</tr>
</tbody>
</table>
</div>
<h3 id="8-5-遇到的困难"><a href="#8-5-遇到的困难" class="headerlink" title="8.5 遇到的困难"></a>8.5 遇到的困难</h3><ol>
<li><p><strong>逻辑运算误判</strong>：最初错误地将 <code>0 || x</code> 化简为 <code>1</code>，导致条件判断逻辑紊乱。正确结果应为 <code>x</code>。</p>
</li>
<li><p><strong>操作数缺失崩溃</strong>：函数内联可能产生”残疾”指令（操作数不完整），导致 <code>getOp2()</code> 抛出 <code>IndexOutOfBoundsException</code>。</p>
<ul>
<li><strong>解决方案</strong>：在 <code>foldAluInstr</code> 入口添加防御性检查：<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (aluInstr.getUseValueCount() &lt; <span class="number">2</span>) <span class="keyword">return</span> <span class="literal">false</span>;</span><br></pre></td></tr></table></figure></div>
</li>
</ul>
</li>
</ol>
<hr>
<h2 id="9-全局代码移动-GCM-与循环不变量外提-LICM"><a href="#9-全局代码移动-GCM-与循环不变量外提-LICM" class="headerlink" title="9. 全局代码移动 (GCM) 与循环不变量外提 (LICM)"></a>9. 全局代码移动 (GCM) 与循环不变量外提 (LICM)</h2><h3 id="9-1-功能描述"><a href="#9-1-功能描述" class="headerlink" title="9.1 功能描述"></a>9.1 功能描述</h3><p>GCM 通过 Schedule Early + Schedule Late 算法，将指令移动到最优位置，自动实现：</p>
<ul>
<li><strong>循环不变量外提 (LICM)</strong>：将循环内不变计算移到循环外</li>
<li><strong>代码下沉</strong>：将只在部分路径使用的计算下沉，减少寄存器压力</li>
</ul>
<h3 id="9-2-核心算法"><a href="#9-2-核心算法" class="headerlink" title="9.2 核心算法"></a>9.2 核心算法</h3><ol>
<li><p><strong>Schedule Early</strong>：根据操作数定义位置，确定指令最早可放置的块（必须被所有操作数支配）。</p>
</li>
<li><p><strong>Schedule Late</strong>：根据使用点，确定指令最晚必须放置的块（必须支配所有使用者）。</p>
</li>
<li><p><strong>选择最优块</strong>：在 Early 和 Late 之间，选择 <strong>循环深度 (Loop Depth) 最小</strong> 的块。</p>
</li>
</ol>
<h3 id="9-3-循环深度计算-LoopAnalysis"><a href="#9-3-循环深度计算-LoopAnalysis" class="headerlink" title="9.3 循环深度计算 (LoopAnalysis)"></a>9.3 循环深度计算 (LoopAnalysis)</h3><p>通过检测回边（back edge）识别循环：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 回边定义：边 (u, v) 满足 v 支配 u</span></span><br><span class="line"><span class="keyword">for</span> (IrBasicBlock succ : block.getSuccessors()) {</span><br><span class="line">    <span class="keyword">if</span> (succ.getDominators().contains(block) </span><br><span class="line">        || succ == block) { <span class="comment">// succ 支配 block，即 succ 是循环头</span></span><br><span class="line">        markLoopBlocks(block, succ);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h3 id="9-4-Pinned-指令"><a href="#9-4-Pinned-指令" class="headerlink" title="9.4 Pinned 指令"></a>9.4 Pinned 指令</h3><p>以下指令不能移动，必须”钉”在原位置：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="type">boolean</span> <span class="title function_">isPinned</span><span class="params">(Instruction instr)</span> {</span><br><span class="line">    <span class="comment">// 除法可能触发异常</span></span><br><span class="line">    <span class="keyword">if</span> (instr <span class="keyword">instanceof</span> AluInstr alu) {</span><br><span class="line">        <span class="keyword">if</span> (alu.getAluOp() == SDIV || alu.getAluOp() == SREM) {</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> instr <span class="keyword">instanceof</span> BranchInstr ||</span><br><span class="line">           instr <span class="keyword">instanceof</span> JumpInstr ||</span><br><span class="line">           instr <span class="keyword">instanceof</span> ReturnInstr ||</span><br><span class="line">           instr <span class="keyword">instanceof</span> StoreInstr ||</span><br><span class="line">           instr <span class="keyword">instanceof</span> LoadInstr ||  <span class="comment">// 内存读取有依赖</span></span><br><span class="line">           instr <span class="keyword">instanceof</span> CallInstr ||  <span class="comment">// 函数调用有副作用</span></span><br><span class="line">           instr <span class="keyword">instanceof</span> AllocateInstr ||</span><br><span class="line">           instr <span class="keyword">instanceof</span> GepInstr ||</span><br><span class="line">           instr <span class="keyword">instanceof</span> PhiInstr;</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h3 id="9-5-遇到的困难"><a href="#9-5-遇到的困难" class="headerlink" title="9.5 遇到的困难"></a>9.5 遇到的困难</h3><ol>
<li><p><strong>Load 被错误外提</strong>：最初未将 <code>LoadInstr</code> 设为 Pinned，导致全局变量的读取被提到循环外，使用旧值。</p>
<ul>
<li><strong>解决方案</strong>：将所有 <code>Load</code> 设为 Pinned。</li>
</ul>
</li>
<li><p><strong>除法安全投机</strong>：<code>div</code> 指令可能除零，若被提出 <code>if (b != 0)</code> 块，会触发运行时异常。</p>
<ul>
<li><strong>解决方案</strong>：将 <code>SDIV</code> 和 <code>SREM</code> 设为 Pinned。</li>
</ul>
</li>
<li><p><strong>Return 指令后插入</strong>：<code>addInstrBeforeJump</code> 方法最初未处理 <code>ReturnInstr</code>，导致移动的指令被插到 <code>ret</code> 之后成为死代码。</p>
<ul>
<li><strong>解决方案</strong>：<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (lastInstr <span class="keyword">instanceof</span> JumpInstr || </span><br><span class="line">    lastInstr <span class="keyword">instanceof</span> BranchInstr ||</span><br><span class="line">    lastInstr <span class="keyword">instanceof</span> ReturnInstr) {</span><br><span class="line">    instructions.add(size - <span class="number">1</span>, instr); <span class="comment">// 插在终止指令前</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
</li>
</ul>
</li>
<li><p><strong>Phi 用户的前驱匹配</strong>：Phi 指令的使用点是 <strong>前驱块</strong>，而非 Phi 所在块：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (userInstr <span class="keyword">instanceof</span> PhiInstr phi) {</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; phi.getUseValueList().size(); i++) {</span><br><span class="line">        <span class="keyword">if</span> (phi.getUseValueList().get(i) == instr) {</span><br><span class="line">            <span class="type">IrBasicBlock</span> <span class="variable">phiPred</span> <span class="operator">=</span> phi.getParentBlock()</span><br><span class="line">                .getPredecessors().get(i);</span><br><span class="line">            lca = findLCA(lca, phiPred);</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
</li>
</ol>
<hr>
<h2 id="10-函数内联-FuncInline"><a href="#10-函数内联-FuncInline" class="headerlink" title="10. 函数内联 (FuncInline)"></a>10. 函数内联 (FuncInline)</h2><h3 id="10-1-功能描述"><a href="#10-1-功能描述" class="headerlink" title="10.1 功能描述"></a>10.1 功能描述</h3><p>将被调用函数的代码体复制到调用点，消除函数调用开销，并暴露更多优化机会。</p>
<h3 id="10-2-内联策略"><a href="#10-2-内联策略" class="headerlink" title="10.2 内联策略"></a>10.2 内联策略</h3><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="type">boolean</span> <span class="title function_">canInline</span><span class="params">(IrFunction function)</span> {</span><br><span class="line">    <span class="keyword">if</span> (function.isBuiltIn()) <span class="keyword">return</span> <span class="literal">false</span>;  <span class="comment">// 不内联库函数</span></span><br><span class="line">    </span><br><span class="line">    <span class="type">int</span> <span class="variable">instrCount</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (IrBasicBlock bb : function.getBasicBlocks()) {</span><br><span class="line">        instrCount += bb.getInstructions().size();</span><br><span class="line">        <span class="keyword">for</span> (Instruction i : bb.getInstructions()) {</span><br><span class="line">            <span class="comment">// 不内联递归函数</span></span><br><span class="line">            <span class="keyword">if</span> (i <span class="keyword">instanceof</span> CallInstr c &amp;&amp; c.getCalledFunction() == function) {</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> instrCount &lt; <span class="number">500</span>;  <span class="comment">// 指令数阈值</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<h3 id="10-3-内联步骤"><a href="#10-3-内联步骤" class="headerlink" title="10.3 内联步骤"></a>10.3 内联步骤</h3><ol>
<li><p><strong>分割调用块</strong>：将 Call 指令后的代码移至新的 Split Block。</p>
</li>
<li><p><strong>克隆被调用函数</strong>：</p>
<ul>
<li>建立 <strong>Value Map</strong>（旧值 -&gt; 新值）和 <strong>Block Map</strong>（旧块 -&gt; 新块）</li>
<li>形参映射到实参</li>
<li>克隆所有基本块和指令</li>
</ul>
</li>
<li><p><strong>缝合 CFG</strong>：</p>
<ul>
<li>调用块末尾添加 <code>jump</code> 到 Callee Entry</li>
<li>Callee 的所有 <code>return</code> 替换为 <code>jump</code> 到 Split Block</li>
</ul>
</li>
<li><p><strong>处理返回值</strong>：</p>
<ul>
<li>单返回点：直接替换</li>
<li>多返回点：插入 Phi 节点汇聚返回值</li>
</ul>
</li>
</ol>
<h3 id="10-4-指令克隆"><a href="#10-4-指令克隆" class="headerlink" title="10.4 指令克隆"></a>10.4 指令克隆</h3><p>需为每种指令类型编写克隆逻辑：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> Instruction <span class="title function_">cloneInstruction</span><span class="params">(Instruction old, </span></span><br><span class="line"><span class="params">        Map&lt;IrValue, IrValue&gt; valMap, Map&lt;IrBasicBlock, IrBasicBlock&gt; blkMap)</span> {</span><br><span class="line">    <span class="keyword">if</span> (old <span class="keyword">instanceof</span> AluInstr alu) {</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">AluInstr</span>(alu.getAluOp(), </span><br><span class="line">            mapValue(alu.getOp1(), valMap), </span><br><span class="line">            mapValue(alu.getOp2(), valMap), <span class="string">""</span>);</span><br><span class="line">    } <span class="keyword">else</span> <span class="keyword">if</span> (old <span class="keyword">instanceof</span> BranchInstr br) {</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">BranchInstr</span>(mapValue(br.getCondition(), valMap), </span><br><span class="line">            blkMap.get(br.getTrueBlock()),</span><br><span class="line">            blkMap.get(br.getFalseBlock()), <span class="string">""</span>);</span><br><span class="line">    }</span><br><span class="line">    <span class="comment">// ... 其他指令类型</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h3 id="10-5-遇到的困难"><a href="#10-5-遇到的困难" class="headerlink" title="10.5 遇到的困难"></a>10.5 遇到的困难</h3><ol>
<li><p><strong>Phi 指令克隆</strong>：Phi 的入边块列表需要先创建空 Phi，待 CFG 缝合完毕后再填充：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第一遍：创建空 Phi</span></span><br><span class="line"><span class="keyword">if</span> (old <span class="keyword">instanceof</span> PhiInstr) {</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">PhiInstr</span>(old.getType(), <span class="literal">null</span>, <span class="string">""</span>);</span><br><span class="line">}</span><br><span class="line"><span class="comment">// 第二遍：填充 Incoming</span></span><br><span class="line"><span class="keyword">for</span> (PhiInstr phi : clonedPhis) {</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; originPhi.size(); i++) {</span><br><span class="line">        phi.addIncoming(mapValue(originVal[i]), blockMap.get(originBlk[i]));</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>Value Map 空值</strong>：若 <code>mapValue</code> 返回 <code>null</code>，传给构造函数会导致操作数缺失。</p>
<ul>
<li><strong>解决方案</strong>：常量和全局值不需要映射，直接返回原值：<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (old <span class="keyword">instanceof</span> IrConstant || old <span class="keyword">instanceof</span> IrGlobalValue) {</span><br><span class="line">    <span class="keyword">return</span> old;</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
</li>
</ul>
</li>
</ol>
<hr>
<h2 id="11-活跃变量分析-ActiveAnalysis"><a href="#11-活跃变量分析-ActiveAnalysis" class="headerlink" title="11. 活跃变量分析 (ActiveAnalysis)"></a>11. 活跃变量分析 (ActiveAnalysis)</h2><h3 id="11-1-功能描述"><a href="#11-1-功能描述" class="headerlink" title="11.1 功能描述"></a>11.1 功能描述</h3><p>分析每个基本块的入口和出口处哪些变量是活跃的（Live）。</p>
<h3 id="11-2-数据流方程"><a href="#11-2-数据流方程" class="headerlink" title="11.2 数据流方程"></a>11.2 数据流方程</h3><ul>
<li><strong>Use[B]</strong>：块 B 中使用的、在块内定义前就需要的变量</li>
<li><strong>Def[B]</strong>：块 B 中定义的变量</li>
<li><strong>In[B]</strong> = <mjx-container class="MathJax" jax="SVG"><svg style="vertical-align: -0.566ex;" xmlns="http://www.w3.org/2000/svg" width="27.031ex" height="2.262ex" role="img" focusable="false" viewBox="0 -750 11947.9 1000"><g stroke="currentColor" fill="currentColor" stroke-width="0" transform="scale(1,-1)"><g data-mml-node="math"><g data-mml-node="TeXAtom" data-mjx-texclass="ORD"><g data-mml-node="mi"><path data-c="55" d="M128 622Q121 629 117 631T101 634T58 637H25V683H36Q57 680 180 680Q315 680 324 683H335V637H302Q262 636 251 634T233 622L232 418V291Q232 189 240 145T280 67Q325 24 389 24Q454 24 506 64T571 183Q575 206 575 410V598Q569 608 565 613T541 627T489 637H472V683H481Q496 680 598 680T715 683H724V637H707Q634 633 622 598L621 399Q620 194 617 180Q617 179 615 171Q595 83 531 31T389 -22Q304 -22 226 33T130 192Q129 201 128 412V622Z"></path><path data-c="73" d="M295 316Q295 356 268 385T190 414Q154 414 128 401Q98 382 98 349Q97 344 98 336T114 312T157 287Q175 282 201 278T245 269T277 256Q294 248 310 236T342 195T359 133Q359 71 321 31T198 -10H190Q138 -10 94 26L86 19L77 10Q71 4 65 -1L54 -11H46H42Q39 -11 33 -5V74V132Q33 153 35 157T45 162H54Q66 162 70 158T75 146T82 119T101 77Q136 26 198 26Q295 26 295 104Q295 133 277 151Q257 175 194 187T111 210Q75 227 54 256T33 318Q33 357 50 384T93 424T143 442T187 447H198Q238 447 268 432L283 424L292 431Q302 440 314 448H322H326Q329 448 335 442V310L329 304H301Q295 310 295 316Z" transform="translate(750,0)"></path><path data-c="65" d="M28 218Q28 273 48 318T98 391T163 433T229 448Q282 448 320 430T378 380T406 316T415 245Q415 238 408 231H126V216Q126 68 226 36Q246 30 270 30Q312 30 342 62Q359 79 369 104L379 128Q382 131 395 131H398Q415 131 415 121Q415 117 412 108Q393 53 349 21T250 -11Q155 -11 92 58T28 218ZM333 275Q322 403 238 411H236Q228 411 220 410T195 402T166 381T143 340T127 274V267H333V275Z" transform="translate(1144,0)"></path></g></g><g data-mml-node="mo" transform="translate(1588,0)"><path data-c="5B" d="M118 -250V750H255V710H158V-210H255V-250H118Z"></path></g><g data-mml-node="mi" transform="translate(1866,0)"><path data-c="1D435" d="M231 637Q204 637 199 638T194 649Q194 676 205 682Q206 683 335 683Q594 683 608 681Q671 671 713 636T756 544Q756 480 698 429T565 360L555 357Q619 348 660 311T702 219Q702 146 630 78T453 1Q446 0 242 0Q42 0 39 2Q35 5 35 10Q35 17 37 24Q42 43 47 45Q51 46 62 46H68Q95 46 128 49Q142 52 147 61Q150 65 219 339T288 628Q288 635 231 637ZM649 544Q649 574 634 600T585 634Q578 636 493 637Q473 637 451 637T416 636H403Q388 635 384 626Q382 622 352 506Q352 503 351 500L320 374H401Q482 374 494 376Q554 386 601 434T649 544ZM595 229Q595 273 572 302T512 336Q506 337 429 337Q311 337 310 336Q310 334 293 263T258 122L240 52Q240 48 252 48T333 46Q422 46 429 47Q491 54 543 105T595 229Z"></path></g><g data-mml-node="mo" transform="translate(2625,0)"><path data-c="5D" d="M22 710V750H159V-250H22V-210H119V710H22Z"></path></g><g data-mml-node="mo" transform="translate(3125.2,0)"><path data-c="222A" d="M591 598H592Q604 598 611 583V376Q611 345 611 296Q610 162 606 148Q605 146 605 145Q586 68 507 23T333 -22Q268 -22 209 -1T106 66T56 173Q55 180 55 384L56 585Q66 598 75 598Q85 598 95 585V378L96 172L98 162Q112 95 181 57T332 18Q415 18 487 58T570 175Q571 180 571 383V583Q579 598 591 598Z"></path></g><g data-mml-node="mo" transform="translate(4014.4,0)"><path data-c="28" d="M94 250Q94 319 104 381T127 488T164 576T202 643T244 695T277 729T302 750H315H319Q333 750 333 741Q333 738 316 720T275 667T226 581T184 443T167 250T184 58T225 -81T274 -167T316 -220T333 -241Q333 -250 318 -250H315H302L274 -226Q180 -141 137 -14T94 250Z"></path></g><g data-mml-node="TeXAtom" data-mjx-texclass="ORD" transform="translate(4403.4,0)"><g data-mml-node="mi"><path data-c="4F" d="M56 340Q56 423 86 494T164 610T270 680T388 705Q521 705 621 601T722 341Q722 260 693 191T617 75T510 4T388 -22T267 3T160 74T85 189T56 340ZM467 647Q426 665 388 665Q360 665 331 654T269 620T213 549T179 439Q174 411 174 354Q174 144 277 61Q327 20 385 20H389H391Q474 20 537 99Q603 188 603 354Q603 411 598 439Q577 592 467 647Z"></path><path data-c="75" d="M383 58Q327 -10 256 -10H249Q124 -10 105 89Q104 96 103 226Q102 335 102 348T96 369Q86 385 36 385H25V408Q25 431 27 431L38 432Q48 433 67 434T105 436Q122 437 142 438T172 441T184 442H187V261Q188 77 190 64Q193 49 204 40Q224 26 264 26Q290 26 311 35T343 58T363 90T375 120T379 144Q379 145 379 161T380 201T380 248V315Q380 361 370 372T320 385H302V431Q304 431 378 436T457 442H464V264Q464 84 465 81Q468 61 479 55T524 46H542V0Q540 0 467 -5T390 -11H383V58Z" transform="translate(778,0)"></path><path data-c="74" d="M27 422Q80 426 109 478T141 600V615H181V431H316V385H181V241Q182 116 182 100T189 68Q203 29 238 29Q282 29 292 100Q293 108 293 146V181H333V146V134Q333 57 291 17Q264 -10 221 -10Q187 -10 162 2T124 33T105 68T98 100Q97 107 97 248V385H18V422H27Z" transform="translate(1334,0)"></path></g></g><g data-mml-node="mo" transform="translate(6126.4,0)"><path data-c="5B" d="M118 -250V750H255V710H158V-210H255V-250H118Z"></path></g><g data-mml-node="mi" transform="translate(6404.4,0)"><path data-c="1D435" d="M231 637Q204 637 199 638T194 649Q194 676 205 682Q206 683 335 683Q594 683 608 681Q671 671 713 636T756 544Q756 480 698 429T565 360L555 357Q619 348 660 311T702 219Q702 146 630 78T453 1Q446 0 242 0Q42 0 39 2Q35 5 35 10Q35 17 37 24Q42 43 47 45Q51 46 62 46H68Q95 46 128 49Q142 52 147 61Q150 65 219 339T288 628Q288 635 231 637ZM649 544Q649 574 634 600T585 634Q578 636 493 637Q473 637 451 637T416 636H403Q388 635 384 626Q382 622 352 506Q352 503 351 500L320 374H401Q482 374 494 376Q554 386 601 434T649 544ZM595 229Q595 273 572 302T512 336Q506 337 429 337Q311 337 310 336Q310 334 293 263T258 122L240 52Q240 48 252 48T333 46Q422 46 429 47Q491 54 543 105T595 229Z"></path></g><g data-mml-node="mo" transform="translate(7163.4,0)"><path data-c="5D" d="M22 710V750H159V-250H22V-210H119V710H22Z"></path></g><g data-mml-node="mo" transform="translate(7663.7,0)"><path data-c="2212" d="M84 237T84 250T98 270H679Q694 262 694 250T679 230H98Q84 237 84 250Z"></path></g><g data-mml-node="TeXAtom" data-mjx-texclass="ORD" transform="translate(8663.9,0)"><g data-mml-node="mi"><path data-c="44" d="M130 622Q123 629 119 631T103 634T60 637H27V683H228Q399 682 419 682T461 676Q504 667 546 641T626 573T685 470T708 336Q708 210 634 116T442 3Q429 1 228 0H27V46H60Q102 47 111 49T130 61V622ZM593 338Q593 439 571 501T493 602Q439 637 355 637H322H294Q238 637 234 628Q231 624 231 344Q231 62 232 59Q233 49 248 48T339 46H350Q456 46 515 95Q561 133 577 191T593 338Z"></path><path data-c="65" d="M28 218Q28 273 48 318T98 391T163 433T229 448Q282 448 320 430T378 380T406 316T415 245Q415 238 408 231H126V216Q126 68 226 36Q246 30 270 30Q312 30 342 62Q359 79 369 104L379 128Q382 131 395 131H398Q415 131 415 121Q415 117 412 108Q393 53 349 21T250 -11Q155 -11 92 58T28 218ZM333 275Q322 403 238 411H236Q228 411 220 410T195 402T166 381T143 340T127 274V267H333V275Z" transform="translate(764,0)"></path><path data-c="66" d="M273 0Q255 3 146 3Q43 3 34 0H26V46H42Q70 46 91 49Q99 52 103 60Q104 62 104 224V385H33V431H104V497L105 564L107 574Q126 639 171 668T266 704Q267 704 275 704T289 705Q330 702 351 679T372 627Q372 604 358 590T321 576T284 590T270 627Q270 647 288 667H284Q280 668 273 668Q245 668 223 647T189 592Q183 572 182 497V431H293V385H185V225Q185 63 186 61T189 57T194 54T199 51T206 49T213 48T222 47T231 47T241 46T251 46H282V0H273Z" transform="translate(1208,0)"></path></g></g><g data-mml-node="mo" transform="translate(10243.9,0)"><path data-c="5B" d="M118 -250V750H255V710H158V-210H255V-250H118Z"></path></g><g data-mml-node="mi" transform="translate(10521.9,0)"><path data-c="1D435" d="M231 637Q204 637 199 638T194 649Q194 676 205 682Q206 683 335 683Q594 683 608 681Q671 671 713 636T756 544Q756 480 698 429T565 360L555 357Q619 348 660 311T702 219Q702 146 630 78T453 1Q446 0 242 0Q42 0 39 2Q35 5 35 10Q35 17 37 24Q42 43 47 45Q51 46 62 46H68Q95 46 128 49Q142 52 147 61Q150 65 219 339T288 628Q288 635 231 637ZM649 544Q649 574 634 600T585 634Q578 636 493 637Q473 637 451 637T416 636H403Q388 635 384 626Q382 622 352 506Q352 503 351 500L320 374H401Q482 374 494 376Q554 386 601 434T649 544ZM595 229Q595 273 572 302T512 336Q506 337 429 337Q311 337 310 336Q310 334 293 263T258 122L240 52Q240 48 252 48T333 46Q422 46 429 47Q491 54 543 105T595 229Z"></path></g><g data-mml-node="mo" transform="translate(11280.9,0)"><path data-c="5D" d="M22 710V750H159V-250H22V-210H119V710H22Z"></path></g><g data-mml-node="mo" transform="translate(11558.9,0)"><path data-c="29" d="M60 749L64 750Q69 750 74 750H86L114 726Q208 641 251 514T294 250Q294 182 284 119T261 12T224 -76T186 -143T145 -194T113 -227T90 -246Q87 -249 86 -250H74Q66 -250 63 -250T58 -247T55 -238Q56 -237 66 -225Q221 -64 221 250T66 725Q56 737 55 738Q55 746 60 749Z"></path></g></g></g></svg></mjx-container></li>
<li><strong>Out[B]</strong> = <mjx-container class="MathJax" jax="SVG"><svg style="vertical-align: -1.044ex;" xmlns="http://www.w3.org/2000/svg" width="14.999ex" height="2.741ex" role="img" focusable="false" viewBox="0 -750 6629.4 1211.6"><g stroke="currentColor" fill="currentColor" stroke-width="0" transform="scale(1,-1)"><g data-mml-node="math"><g data-mml-node="munder"><g data-mml-node="mo" transform="translate(0 -0.5)"><path data-c="22C3" d="M96 750Q103 750 109 748T120 744T127 737T133 730T137 723T139 718V395L140 73L142 60Q159 -43 237 -104T416 -166Q521 -166 597 -103T690 60L692 73L694 718Q708 749 735 749Q765 749 775 720Q777 714 777 398Q777 78 776 71Q766 -51 680 -140Q571 -249 416 -249H411Q261 -249 152 -140Q66 -51 56 71Q55 78 55 398Q55 714 57 720Q60 734 70 740Q80 750 96 750Z"></path></g><g data-mml-node="TeXAtom" transform="translate(866,-284.9) scale(0.707)" data-mjx-texclass="ORD"><g data-mml-node="mi"><path data-c="1D446" d="M308 24Q367 24 416 76T466 197Q466 260 414 284Q308 311 278 321T236 341Q176 383 176 462Q176 523 208 573T273 648Q302 673 343 688T407 704H418H425Q521 704 564 640Q565 640 577 653T603 682T623 704Q624 704 627 704T632 705Q645 705 645 698T617 577T585 459T569 456Q549 456 549 465Q549 471 550 475Q550 478 551 494T553 520Q553 554 544 579T526 616T501 641Q465 662 419 662Q362 662 313 616T263 510Q263 480 278 458T319 427Q323 425 389 408T456 390Q490 379 522 342T554 242Q554 216 546 186Q541 164 528 137T492 78T426 18T332 -20Q320 -22 298 -22Q199 -22 144 33L134 44L106 13Q83 -14 78 -18T65 -22Q52 -22 52 -14Q52 -11 110 221Q112 227 130 227H143Q149 221 149 216Q149 214 148 207T144 186T142 153Q144 114 160 87T203 47T255 29T308 24Z"></path></g><g data-mml-node="mo" transform="translate(645,0)"><path data-c="2208" d="M84 250Q84 372 166 450T360 539Q361 539 377 539T419 540T469 540H568Q583 532 583 520Q583 511 570 501L466 500Q355 499 329 494Q280 482 242 458T183 409T147 354T129 306T124 272V270H568Q583 262 583 250T568 230H124V228Q124 207 134 177T167 112T231 48T328 7Q355 1 466 0H570Q583 -10 583 -20Q583 -32 568 -40H471Q464 -40 446 -40T417 -41Q262 -41 172 45Q84 127 84 250Z"></path></g><g data-mml-node="TeXAtom" data-mjx-texclass="ORD" transform="translate(1312,0)"><g data-mml-node="mi"><path data-c="53" d="M55 507Q55 590 112 647T243 704H257Q342 704 405 641L426 672Q431 679 436 687T446 700L449 704Q450 704 453 704T459 705H463Q466 705 472 699V462L466 456H448Q437 456 435 459T430 479Q413 605 329 646Q292 662 254 662Q201 662 168 626T135 542Q135 508 152 480T200 435Q210 431 286 412T370 389Q427 367 463 314T500 191Q500 110 448 45T301 -21Q245 -21 201 -4T140 27L122 41Q118 36 107 21T87 -7T78 -21Q76 -22 68 -22H64Q61 -22 55 -16V101Q55 220 56 222Q58 227 76 227H89Q95 221 95 214Q95 182 105 151T139 90T205 42T305 24Q352 24 386 62T420 155Q420 198 398 233T340 281Q284 295 266 300Q261 301 239 306T206 314T174 325T141 343T112 367T85 402Q55 451 55 507Z"></path><path data-c="75" d="M383 58Q327 -10 256 -10H249Q124 -10 105 89Q104 96 103 226Q102 335 102 348T96 369Q86 385 36 385H25V408Q25 431 27 431L38 432Q48 433 67 434T105 436Q122 437 142 438T172 441T184 442H187V261Q188 77 190 64Q193 49 204 40Q224 26 264 26Q290 26 311 35T343 58T363 90T375 120T379 144Q379 145 379 161T380 201T380 248V315Q380 361 370 372T320 385H302V431Q304 431 378 436T457 442H464V264Q464 84 465 81Q468 61 479 55T524 46H542V0Q540 0 467 -5T390 -11H383V58Z" transform="translate(556,0)"></path><path data-c="63" d="M370 305T349 305T313 320T297 358Q297 381 312 396Q317 401 317 402T307 404Q281 408 258 408Q209 408 178 376Q131 329 131 219Q131 137 162 90Q203 29 272 29Q313 29 338 55T374 117Q376 125 379 127T395 129H409Q415 123 415 120Q415 116 411 104T395 71T366 33T318 2T249 -11Q163 -11 99 53T34 214Q34 318 99 383T250 448T370 421T404 357Q404 334 387 320Z" transform="translate(1112,0)"></path><path data-c="63" d="M370 305T349 305T313 320T297 358Q297 381 312 396Q317 401 317 402T307 404Q281 408 258 408Q209 408 178 376Q131 329 131 219Q131 137 162 90Q203 29 272 29Q313 29 338 55T374 117Q376 125 379 127T395 129H409Q415 123 415 120Q415 116 411 104T395 71T366 33T318 2T249 -11Q163 -11 99 53T34 214Q34 318 99 383T250 448T370 421T404 357Q404 334 387 320Z" transform="translate(1556,0)"></path></g></g><g data-mml-node="mo" transform="translate(3312,0)"><path data-c="28" d="M94 250Q94 319 104 381T127 488T164 576T202 643T244 695T277 729T302 750H315H319Q333 750 333 741Q333 738 316 720T275 667T226 581T184 443T167 250T184 58T225 -81T274 -167T316 -220T333 -241Q333 -250 318 -250H315H302L274 -226Q180 -141 137 -14T94 250Z"></path></g><g data-mml-node="mi" transform="translate(3701,0)"><path data-c="1D435" d="M231 637Q204 637 199 638T194 649Q194 676 205 682Q206 683 335 683Q594 683 608 681Q671 671 713 636T756 544Q756 480 698 429T565 360L555 357Q619 348 660 311T702 219Q702 146 630 78T453 1Q446 0 242 0Q42 0 39 2Q35 5 35 10Q35 17 37 24Q42 43 47 45Q51 46 62 46H68Q95 46 128 49Q142 52 147 61Q150 65 219 339T288 628Q288 635 231 637ZM649 544Q649 574 634 600T585 634Q578 636 493 637Q473 637 451 637T416 636H403Q388 635 384 626Q382 622 352 506Q352 503 351 500L320 374H401Q482 374 494 376Q554 386 601 434T649 544ZM595 229Q595 273 572 302T512 336Q506 337 429 337Q311 337 310 336Q310 334 293 263T258 122L240 52Q240 48 252 48T333 46Q422 46 429 47Q491 54 543 105T595 229Z"></path></g><g data-mml-node="mo" transform="translate(4460,0)"><path data-c="29" d="M60 749L64 750Q69 750 74 750H86L114 726Q208 641 251 514T294 250Q294 182 284 119T261 12T224 -76T186 -143T145 -194T113 -227T90 -246Q87 -249 86 -250H74Q66 -250 63 -250T58 -247T55 -238Q56 -237 66 -225Q221 -64 221 250T66 725Q56 737 55 738Q55 746 60 749Z"></path></g></g></g><g data-mml-node="TeXAtom" data-mjx-texclass="ORD" transform="translate(4511.4,0)"><g data-mml-node="mi"><path data-c="49" d="M328 0Q307 3 180 3T32 0H21V46H43Q92 46 106 49T126 60Q128 63 128 342Q128 620 126 623Q122 628 118 630T96 635T43 637H21V683H32Q53 680 180 680T328 683H339V637H317Q268 637 254 634T234 623Q232 620 232 342Q232 63 234 60Q238 55 242 53T264 48T317 46H339V0H328Z"></path><path data-c="6E" d="M41 46H55Q94 46 102 60V68Q102 77 102 91T102 122T103 161T103 203Q103 234 103 269T102 328V351Q99 370 88 376T43 385H25V408Q25 431 27 431L37 432Q47 433 65 434T102 436Q119 437 138 438T167 441T178 442H181V402Q181 364 182 364T187 369T199 384T218 402T247 421T285 437Q305 442 336 442Q450 438 463 329Q464 322 464 190V104Q464 66 466 59T477 49Q498 46 526 46H542V0H534L510 1Q487 2 460 2T422 3Q319 3 310 0H302V46H318Q379 46 379 62Q380 64 380 200Q379 335 378 343Q372 371 358 385T334 402T308 404Q263 404 229 370Q202 343 195 315T187 232V168V108Q187 78 188 68T191 55T200 49Q221 46 249 46H265V0H257L234 1Q210 2 183 2T145 3Q42 3 33 0H25V46H41Z" transform="translate(361,0)"></path></g></g><g data-mml-node="mo" transform="translate(5428.4,0)"><path data-c="5B" d="M118 -250V750H255V710H158V-210H255V-250H118Z"></path></g><g data-mml-node="mi" transform="translate(5706.4,0)"><path data-c="1D446" d="M308 24Q367 24 416 76T466 197Q466 260 414 284Q308 311 278 321T236 341Q176 383 176 462Q176 523 208 573T273 648Q302 673 343 688T407 704H418H425Q521 704 564 640Q565 640 577 653T603 682T623 704Q624 704 627 704T632 705Q645 705 645 698T617 577T585 459T569 456Q549 456 549 465Q549 471 550 475Q550 478 551 494T553 520Q553 554 544 579T526 616T501 641Q465 662 419 662Q362 662 313 616T263 510Q263 480 278 458T319 427Q323 425 389 408T456 390Q490 379 522 342T554 242Q554 216 546 186Q541 164 528 137T492 78T426 18T332 -20Q320 -22 298 -22Q199 -22 144 33L134 44L106 13Q83 -14 78 -18T65 -22Q52 -22 52 -14Q52 -11 110 221Q112 227 130 227H143Q149 221 149 216Q149 214 148 207T144 186T142 153Q144 114 160 87T203 47T255 29T308 24Z"></path></g><g data-mml-node="mo" transform="translate(6351.4,0)"><path data-c="5D" d="M22 710V750H159V-250H22V-210H119V710H22Z"></path></g></g></g></svg></mjx-container></li>
</ul>
<h3 id="11-3-应用场景"><a href="#11-3-应用场景" class="headerlink" title="11.3 应用场景"></a>11.3 应用场景</h3><ul>
<li>寄存器分配：判断变量生命周期</li>
<li>死代码消除：未活跃的定值可删除</li>
<li>RemovePhi：判断 Move 后变量是否还被使用</li>
</ul>
<hr>
<h2 id="12-寄存器分配-RegisterAllocator"><a href="#12-寄存器分配-RegisterAllocator" class="headerlink" title="12. 寄存器分配 (RegisterAllocator)"></a>12. 寄存器分配 (RegisterAllocator)</h2><h3 id="12-1-功能描述"><a href="#12-1-功能描述" class="headerlink" title="12.1 功能描述"></a>12.1 功能描述</h3><p>将 IR 虚拟寄存器映射到 MIPS 物理寄存器。</p>
<h3 id="12-2-分配策略"><a href="#12-2-分配策略" class="headerlink" title="12.2 分配策略"></a>12.2 分配策略</h3><p>按支配树遍历，采用线性扫描思想：</p>
<ol>
<li>对每条定值指令，分配一个空闲寄存器</li>
<li>当变量最后一次使用后，释放其寄存器</li>
<li>跨块变量通过 <code>InValueSet</code> 和 <code>OutValueSet</code> 传递</li>
</ol>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">allocateInstruction</span><span class="params">(Instruction instr, HashSet&lt;IrValue&gt; defineSet)</span> {</span><br><span class="line">    <span class="keyword">if</span> (!instr.defValue()) <span class="keyword">return</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> (Register reg : registerSet) {</span><br><span class="line">        <span class="keyword">if</span> (!allocatedRegister.contains(reg)) {</span><br><span class="line">            registerValueMap.put(reg, instr);</span><br><span class="line">            valueRegisterMap.put(instr, reg);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">checkAndFreeRegister</span><span class="params">(Instruction instr, ...)</span> {</span><br><span class="line">    <span class="keyword">for</span> (IrValue usedValue : instr.getUseValueList()) {</span><br><span class="line">        <span class="keyword">if</span> (lastUse(usedValue) == instr &amp;&amp; !isBlockOutValue(usedValue)) {</span><br><span class="line">            registerValueMap.remove(valueRegisterMap.get(usedValue));</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<h3 id="12-3-遇到的困难"><a href="#12-3-遇到的困难" class="headerlink" title="12.3 遇到的困难"></a>12.3 遇到的困难</h3><ul>
<li><strong>跨块变量</strong>：进入子块时暂存当前寄存器状态，返回后恢复。</li>
<li><strong>寄存器不足</strong>：当可用寄存器耗尽时，需溢出到栈上（通过 <code>$k0</code> 临时寄存器中转）。</li>
</ul>
<hr>
<h2 id="13-窥孔优化-PeepHole"><a href="#13-窥孔优化-PeepHole" class="headerlink" title="13. 窥孔优化 (PeepHole)"></a>13. 窥孔优化 (PeepHole)</h2><h3 id="13-1-功能描述"><a href="#13-1-功能描述" class="headerlink" title="13.1 功能描述"></a>13.1 功能描述</h3><p>在 MIPS 汇编层面进行局部模式匹配和替换，消除冗余指令。</p>
<h3 id="13-2-优化模式"><a href="#13-2-优化模式" class="headerlink" title="13.2 优化模式"></a>13.2 优化模式</h3><h4 id="13-2-1-连续-Store-优化"><a href="#13-2-1-连续-Store-优化" class="headerlink" title="13.2.1 连续 Store 优化"></a>13.2.1 连续 Store 优化</h4><div class="code-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">sw $t1, 0($sp)</span><br><span class="line">sw $t2, 0($sp)  -&gt;  删除第一条 sw</span><br></pre></td></tr></table></figure></div>
<h4 id="13-2-2-连续-Move-优化"><a href="#13-2-2-连续-Move-优化" class="headerlink" title="13.2.2 连续 Move 优化"></a>13.2.2 连续 Move 优化</h4><div class="code-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">move $a, $b</span><br><span class="line">move $c, $a  -&gt;  move $a, $b; move $c, $b</span><br></pre></td></tr></table></figure></div>
<h4 id="13-2-3-Load-Store-优化"><a href="#13-2-3-Load-Store-优化" class="headerlink" title="13.2.3 Load-Store 优化"></a>13.2.3 Load-Store 优化</h4><div class="code-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">lw $t, offset($base)</span><br><span class="line">sw $t, offset($base)  -&gt;  删除 sw</span><br></pre></td></tr></table></figure></div>
<h4 id="13-2-4-Store-Load-优化"><a href="#13-2-4-Store-Load-优化" class="headerlink" title="13.2.4 Store-Load 优化"></a>13.2.4 Store-Load 优化</h4><div class="code-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">sw $t1, offset($base)</span><br><span class="line">lw $t2, offset($base)  -&gt;  move $t2, $t1</span><br></pre></td></tr></table></figure></div>
<h4 id="13-2-5-冗余-Move-删除"><a href="#13-2-5-冗余-Move-删除" class="headerlink" title="13.2.5 冗余 Move 删除"></a>13.2.5 冗余 Move 删除</h4><div class="code-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">move $t, $t  -&gt;  删除</span><br></pre></td></tr></table></figure></div>
<h4 id="13-2-6-无用跳转删除"><a href="#13-2-6-无用跳转删除" class="headerlink" title="13.2.6 无用跳转删除"></a>13.2.6 无用跳转删除</h4><div class="code-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">j label</span><br><span class="line">label:  -&gt;  删除 j</span><br></pre></td></tr></table></figure></div>
<h3 id="13-3-实现框架"><a href="#13-3-实现框架" class="headerlink" title="13.3 实现框架"></a>13.3 实现框架</h3><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">peep</span><span class="params">()</span> {</span><br><span class="line">    <span class="type">boolean</span> <span class="variable">finished</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">while</span> (!finished) {</span><br><span class="line">        finished = <span class="literal">true</span>;</span><br><span class="line">        finished &amp;= peepContinueSW();</span><br><span class="line">        finished &amp;= peepContinueMove();</span><br><span class="line">        finished &amp;= peepLoadStore();</span><br><span class="line">        finished &amp;= peepLoadAfterStore();</span><br><span class="line">        finished &amp;= peepRedundantMove();</span><br><span class="line">        finished &amp;= peepUselessJump();</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<hr>
<h2 id="14-后端强度削减"><a href="#14-后端强度削减" class="headerlink" title="14. 后端强度削减"></a>14. 后端强度削减</h2><h3 id="14-1-乘法优化"><a href="#14-1-乘法优化" class="headerlink" title="14.1 乘法优化"></a>14.1 乘法优化</h3><p>在 <code>AluInstr.toMips()</code> 中，将乘以 2 的幂次替换为左移：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (FINE_TUNING &amp;&amp; aluOp == AluOp.MUL) {</span><br><span class="line">    <span class="keyword">if</span> (op2 <span class="keyword">instanceof</span> IrConstantInt constant) {</span><br><span class="line">        <span class="type">int</span> <span class="variable">value</span> <span class="operator">=</span> constant.getValue();</span><br><span class="line">        <span class="type">int</span> <span class="variable">power</span> <span class="operator">=</span> Integer.numberOfTrailingZeros(value);</span><br><span class="line">        <span class="keyword">if</span> (value &gt; <span class="number">0</span> &amp;&amp; (value &amp; (value - <span class="number">1</span>)) == <span class="number">0</span>) {</span><br><span class="line">            <span class="comment">// x * 2^k -&gt; x &lt;&lt; k</span></span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">MipsAlu</span>(AluType.SLL, targetReg, srcReg, power);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h3 id="14-2-除法优化"><a href="#14-2-除法优化" class="headerlink" title="14.2 除法优化"></a>14.2 除法优化</h3><h4 id="14-2-1-2-的幂次除法优化"><a href="#14-2-1-2-的幂次除法优化" class="headerlink" title="14.2.1 2 的幂次除法优化"></a>14.2.1 2 的幂次除法优化</h4><p>有符号除法需特殊处理负数舍入（向零取整）：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (FINE_TUNING &amp;&amp; aluOp == AluOp.SDIV) {</span><br><span class="line">    <span class="comment">// x / 2^k (signed)</span></span><br><span class="line">    <span class="comment">// 算法：(x + (x &gt;&gt; 31 &gt;&gt;&gt; (32-k))) &gt;&gt; k</span></span><br><span class="line">    <span class="comment">// 即：若 x &lt; 0，加上 (2^k - 1) 的偏移再右移</span></span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">MipsAlu</span>(AluType.SRA, tempReg, srcReg, <span class="number">31</span>);     <span class="comment">// 符号扩展</span></span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">MipsAlu</span>(AluType.SRL, tempReg, tempReg, <span class="number">32</span> - k); <span class="comment">// 取低 k 位</span></span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">MipsAlu</span>(AluType.ADDU, tempReg, srcReg, tempReg);</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">MipsAlu</span>(AluType.SRA, targetReg, tempReg, k);</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h4 id="14-2-2-Magic-Number-算法（通用常数除法优化）"><a href="#14-2-2-Magic-Number-算法（通用常数除法优化）" class="headerlink" title="14.2.2 Magic Number 算法（通用常数除法优化）"></a>14.2.2 Magic Number 算法（通用常数除法优化）</h4><p>对于<strong>非 2 的幂次</strong>的常数除法（如 <code>x / 7</code>），采用论文 <em>“Division by Invariant Integers using Multiplication”</em> (Granlund &amp; Montgomery, 1994) 提出的 Magic Number 算法。</p>
<p><strong>核心思想</strong>：将除法 <code>x / d</code> 转换为高位乘法 <code>(x * m) &gt;&gt; (32 + s)</code>，其中 <code>m</code> 是预先计算的”魔数”，<code>s</code> 是移位量。</p>
<p><strong>MagicNumber 计算类</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MagicNumber</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="type">long</span> m;  <span class="comment">// 魔数</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> s;   <span class="comment">// 移位量</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> a;   <span class="comment">// 加法修正标志（当 m &gt;= 2^31 时需要）</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MagicNumber</span><span class="params">(<span class="type">int</span> d)</span> {</span><br><span class="line">        <span class="type">long</span> <span class="variable">ad</span> <span class="operator">=</span> Math.abs((<span class="type">long</span>) d);</span><br><span class="line">        <span class="type">int</span> <span class="variable">p</span> <span class="operator">=</span> <span class="number">31</span>;</span><br><span class="line">        <span class="type">long</span> <span class="variable">t</span> <span class="operator">=</span> <span class="number">1L</span> &lt;&lt; <span class="number">31</span>;</span><br><span class="line">        <span class="type">long</span> <span class="variable">anc</span> <span class="operator">=</span> t - <span class="number">1</span> - (t % ad);</span><br><span class="line">        <span class="type">long</span> <span class="variable">q1</span> <span class="operator">=</span> t / anc, r1 = t % anc;</span><br><span class="line">        <span class="type">long</span> <span class="variable">q2</span> <span class="operator">=</span> t / ad, r2 = t % ad;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">while</span> (<span class="literal">true</span>) {</span><br><span class="line">            p++;</span><br><span class="line">            q1 = <span class="number">2</span> * q1; r1 = <span class="number">2</span> * r1;</span><br><span class="line">            <span class="keyword">if</span> (r1 &gt;= anc) { q1++; r1 -= anc; }</span><br><span class="line">            q2 = <span class="number">2</span> * q2; r2 = <span class="number">2</span> * r2;</span><br><span class="line">            <span class="keyword">if</span> (r2 &gt;= ad) { q2++; r2 -= ad; }</span><br><span class="line">            <span class="keyword">if</span> (q1 &gt; ad - r2 || (q1 == ad - r2 &amp;&amp; r1 == <span class="number">0</span>)) <span class="keyword">break</span>;</span><br><span class="line">        }</span><br><span class="line">        <span class="built_in">this</span>.m = q2 + <span class="number">1</span>;</span><br><span class="line">        <span class="built_in">this</span>.s = p - <span class="number">32</span>;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">this</span>.m &gt;= (<span class="number">1L</span> &lt;&lt; <span class="number">31</span>)) {</span><br><span class="line">            <span class="built_in">this</span>.a = <span class="number">1</span>;</span><br><span class="line">            <span class="built_in">this</span>.m -= (<span class="number">1L</span> &lt;&lt; <span class="number">32</span>);  <span class="comment">// 转为有符号表示</span></span><br><span class="line">        } <span class="keyword">else</span> {</span><br><span class="line">            <span class="built_in">this</span>.a = <span class="number">0</span>;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>MIPS 代码生成</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">optimizeConstDiv</span><span class="params">(Register regL, Register regResult, IrValue op1, <span class="type">int</span> d)</span> {</span><br><span class="line">    <span class="type">MagicNumber</span> <span class="variable">magic</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MagicNumber</span>(Math.abs(d));</span><br><span class="line">    </span><br><span class="line">    loadValueToRegister(op1, regL);</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">MarsLi</span>(Register.K1, (<span class="type">int</span>) magic.m);        <span class="comment">// 加载魔数</span></span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">MipsMdu</span>(MduType.MULT, regL, Register.K1);  <span class="comment">// 有符号乘法</span></span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">MipsMdu</span>(MduType.MFHI, Register.K1);        <span class="comment">// 取高 32 位</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (magic.a == <span class="number">1</span>) {  <span class="comment">// 若魔数溢出，需加回被除数</span></span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">MipsAlu</span>(AluType.ADDU, Register.K1, Register.K1, regL);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">if</span> (magic.s &gt; <span class="number">0</span>) {   <span class="comment">// 右移 s 位</span></span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">MipsAlu</span>(AluType.SRA, Register.K1, Register.K1, magic.s);</span><br><span class="line">    }</span><br><span class="line">    <span class="comment">// 修正符号位（向零取整）</span></span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">MipsAlu</span>(AluType.SRA, Register.K0, regL, <span class="number">31</span>);</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">MipsAlu</span>(AluType.SUBU, Register.K1, Register.K1, Register.K0);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (d &lt; <span class="number">0</span>) {  <span class="comment">// 若除数为负，取反</span></span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">MipsAlu</span>(AluType.SUBU, regResult, Register.ZERO, Register.K1);</span><br><span class="line">    } <span class="keyword">else</span> {</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">MarsMove</span>(regResult, Register.K1);</span><br><span class="line">    }</span><br><span class="line">    saveResult(<span class="built_in">this</span>, regResult);</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>性能收益</strong>：</p>
<ul>
<li>原 <code>div</code> 指令：15 周期</li>
<li>Magic Number 方案：约 8-10 条 OTHER 指令（约 8-10 周期）+ 1 条 <code>mult</code>（5 周期）</li>
<li><strong>净节省约 5 周期/每次常数除法</strong></li>
</ul>
<h4 id="14-2-3-取模优化-SREM"><a href="#14-2-3-取模优化-SREM" class="headerlink" title="14.2.3 取模优化 (SREM)"></a>14.2.3 取模优化 (SREM)</h4><p>取模运算通过 <code>a % d = a - (a / d) * d</code> 复用除法优化结果：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">optimizeConstRem</span><span class="params">(Register regL, Register regResult, IrValue op1, <span class="type">int</span> d)</span> {</span><br><span class="line">    <span class="type">Register</span> <span class="variable">regQ</span> <span class="operator">=</span> Register.K1;</span><br><span class="line">    optimizeConstDiv(regL, regQ, op1, d);  <span class="comment">// 复用除法优化</span></span><br><span class="line">    </span><br><span class="line">    loadValueToRegister(op1, regL);</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">MarsLi</span>(Register.K0, d);</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">MipsMdu</span>(MduType.MULT, regQ, Register.K0);</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">MipsMdu</span>(MduType.MFLO, Register.K0);</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">MipsAlu</span>(AluType.SUBU, regResult, regL, Register.K0);</span><br><span class="line">    saveResult(<span class="built_in">this</span>, regResult);</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
]]></content>
      <tags>
        <tag>编译</tag>
      </tags>
  </entry>
  <entry>
    <title>BUAA_Compiler 设计文档</title>
    <url>/2026/01/01/compiler-design-report/</url>
    <content><![CDATA[<h2 id="一、参考编译器介绍"><a href="#一、参考编译器介绍" class="headerlink" title="一、参考编译器介绍"></a>一、参考编译器介绍</h2><h3 id="1-1-参考方案"><a href="#1-1-参考方案" class="headerlink" title="1.1 参考方案"></a>1.1 参考方案</h3><p>设计时比较了GCC、LLVM等编译器的架构，最终选择参考 <strong>LLVM</strong> 的思路。选择 LLVM 主要基于两点考虑：一是其 SSA 形式的中间表示为数据流分析提供了良好的基础，二是模块化的架构便于分阶段实现和后期扩展。</p>
<h3 id="1-2-LLVM-架构分析"><a href="#1-2-LLVM-架构分析" class="headerlink" title="1.2 LLVM 架构分析"></a>1.2 LLVM 架构分析</h3><p>LLVM 采用三阶段设计：前端负责词法、语法和语义分析，生成统一的中间表示；中端基于 IR 实施各类优化变换；后端负责目标代码生成。这种分层架构具有以下优势：</p>
<ul>
<li><strong>前端</strong>输出的 IR 采用统一格式，使得优化器无需关注源语言的具体语法细节</li>
<li><strong>中端</strong>将优化逻辑封装为独立的 Pass，便于模块化开发与测试，在后期添加新优化时能够快速集成</li>
<li><strong>后端</strong>专注于寄存器分配和指令选择，本项目的 MIPS 代码生成部分借鉴了这一思路</li>
</ul>
<h3 id="1-3-LLVM-接口设计分析"><a href="#1-3-LLVM-接口设计分析" class="headerlink" title="1.3 LLVM 接口设计分析"></a>1.3 LLVM 接口设计分析</h3><p>LLVM 各模块间通过清晰的 C++ 类接口进行交互，实现了高度解耦：</p>
<p><strong>前端到中端接口</strong>：</p>
<ul>
<li><strong>数据流向</strong>：前端 AST <mjx-container class="MathJax" jax="SVG"><svg style="vertical-align: -0.025ex;" xmlns="http://www.w3.org/2000/svg" width="2.262ex" height="1.181ex" role="img" focusable="false" viewBox="0 -511 1000 522"><g stroke="currentColor" fill="currentColor" stroke-width="0" transform="scale(1,-1)"><g data-mml-node="math"><g data-mml-node="mo"><path data-c="2192" d="M56 237T56 250T70 270H835Q719 357 692 493Q692 494 692 496T691 499Q691 511 708 511H711Q720 511 723 510T729 506T732 497T735 481T743 456Q765 389 816 336T935 261Q944 258 944 250Q944 244 939 241T915 231T877 212Q836 186 806 152T761 85T740 35T732 4Q730 -6 727 -8T711 -11Q691 -11 691 0Q691 7 696 25Q728 151 835 230H70Q56 237 56 250Z"></path></g></g></g></svg></mjx-container> LLVM IR Module。</li>
<li><strong>核心类</strong>：<code>LLVMContext</code> (保存全局状态), <code>Module</code> (IR 顶层容器), <code>IRBuilder</code> (指令构建辅助类)。</li>
<li><strong>交互方式</strong>：前端遍历 AST，调用 <code>IRBuilder</code> 的方法（如 <code>CreateAdd</code>, <code>CreateBr</code>）向 BasicBlock 中插入指令。</li>
</ul>
<p><strong>中端优化接口 (参考 Legacy PM 模型)</strong>：</p>
<ul>
<li><strong>Pass 架构</strong>：所有的优化均继承自 <code>Pass</code> 基类（如 <code>FunctionPass</code>, <code>ModulePass</code>）。</li>
<li><strong>核心方法</strong>：子类实现 <code>runOnFunction(Function &amp;F)</code> 或 <code>runOnModule(Module &amp;M)</code> 虚接口来执行优化逻辑。</li>
<li><strong>调度机制</strong>：<code>PassManager</code> 维护 Pass 队列，根据 Analysis 的依赖关系自动调度执行顺序。</li>
</ul>
<p><strong>中端到后端接口</strong>：</p>
<ul>
<li><strong>输入</strong>：经过优化的 IR <code>Module</code>。</li>
<li><strong>核心流程</strong>：通过 <code>TargetMachine</code> 接口创建 <code>PassManager</code>，注入后端特有的 Pass（指令选择 <code>SelectionDAG</code>、寄存器分配 <code>RegAlloc</code>、栈帧布局等），最终输出汇编文本或对象文件。</li>
</ul>
<p><strong>Use-Def 链（IR 核心机制）</strong>：</p>
<ul>
<li><strong>继承关系</strong>：<code>Instruction</code> 类继承自 <code>User</code>，而 <code>User</code> 继承自 <code>Value</code>。</li>
<li><strong>遍历接口</strong>：<ul>
<li><code>Value::users()</code>：遍历所有使用该值的地方（Def-Use 链）。</li>
<li><code>User::operands()</code>：遍历该指令用到的所有操作数（Use-Def 链）。</li>
</ul>
</li>
<li><strong>替换接口</strong>：<code>Value::replaceAllUsesWith(Value *NewVal)</code> (RAUW)，是实现优化的核心原语。</li>
</ul>
<h3 id="1-4-LLVM-文件组织分析"><a href="#1-4-LLVM-文件组织分析" class="headerlink" title="1.4 LLVM 文件组织分析"></a>1.4 LLVM 文件组织分析</h3><p>LLVM 采用分层的目录结构，各模块职责清晰：</p>
<div class="code-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">llvm/</span><br><span class="line">├── include/llvm/           # 头文件</span><br><span class="line">│   ├── IR/                 #   IR相关类定义（Module, Function, BasicBlock, Instruction）</span><br><span class="line">│   ├── Pass.h              #   Pass基类定义</span><br><span class="line">│   ├── PassManager.h       #   Pass管理器</span><br><span class="line">│   └── Transforms/         #   优化Pass接口</span><br><span class="line">├── lib/</span><br><span class="line">│   ├── IR/                 # IR实现（Value, User, Use等）</span><br><span class="line">│   ├── Transforms/         # 优化Pass实现</span><br><span class="line">│   │   ├── Scalar/         #   标量优化（DCE, SCCP, GVN等）</span><br><span class="line">│   │   ├── IPO/            #   过程间优化</span><br><span class="line">│   │   └── Vectorize/      #   向量化</span><br><span class="line">│   ├── CodeGen/            # 后端代码生成</span><br><span class="line">│   │   ├── SelectionDAG/   #   指令选择</span><br><span class="line">│   │   └── RegAlloc/       #   寄存器分配</span><br><span class="line">│   └── Target/             # 各目标平台实现</span><br><span class="line">│       ├── X86/</span><br><span class="line">│       ├── ARM/</span><br><span class="line">│       └── Mips/</span><br><span class="line">└── tools/</span><br><span class="line">    ├── clang/              # C/C++前端</span><br><span class="line">    └── opt/                # 优化器工具</span><br></pre></td></tr></table></figure></div>
<p><strong>关键设计特点</strong>：</p>
<ul>
<li><strong>头文件分离</strong>：接口定义集中在 <code>include/</code> 目录，便于外部使用</li>
<li><strong>按功能分模块</strong>：IR、优化、代码生成分别独立</li>
<li><strong>目标平台隔离</strong>：不同架构的后端互不干扰</li>
<li><strong>工具与库分离</strong>：核心库在 <code>lib/</code>，命令行工具在 <code>tools/</code></li>
</ul>
<hr>
<h2 id="二、编译器总体设计"><a href="#二、编译器总体设计" class="headerlink" title="二、编译器总体设计"></a>二、编译器总体设计</h2><h3 id="2-1-设计目标"><a href="#2-1-设计目标" class="headerlink" title="2.1 设计目标"></a>2.1 设计目标</h3><p>本编译器的设计目标是将 SysY 源代码完整编译为 MIPS32 汇编代码。除基本的编译功能外，还需实现错误检查机制以及中端代码优化。</p>
<h3 id="2-2-整体架构"><a href="#2-2-整体架构" class="headerlink" title="2.2 整体架构"></a>2.2 整体架构</h3><p>编译流程采用流水线式的阶段划分，各阶段处理完成后将结果传递给下一阶段：</p>
<ol>
<li><strong>Lexer/Parser</strong>：词法分析器生成 Token 流，递归下降解析器根据文法规则构建抽象语法树。这部分实现相对直观，主要按照文法定义进行。</li>
<li><strong>语义分析和 IR 生成</strong>：采用两遍遍历的策略。第一遍进行语义检查（符号定义检查、类型匹配验证等），第二遍生成 LLVM 风格的中间表示。初期曾尝试单遍完成，但发现错误恢复逻辑难以维护，因此改为两遍处理。</li>
<li><strong>优化器</strong>：对中间表示实施常量折叠、死代码删除等优化变换。优化过程具有迭代性，某次优化的结果可能为后续优化创造新的机会。</li>
<li><strong>后端代码生成</strong>：将中间表示翻译为 MIPS 汇编，核心工作是寄存器分配。在实现过程中对分配策略进行了多次调整，逐步提升了寄存器利用率。</li>
</ol>
<h3 id="2-3-模块划分设计"><a href="#2-3-模块划分设计" class="headerlink" title="2.3 模块划分设计"></a>2.3 模块划分设计</h3><p>根据编译阶段和功能职责，将编译器划分为六个主要模块：</p>
<div class="code-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">src/</span><br><span class="line">├── frontend/              # 前端模块</span><br><span class="line">│   ├── lexer/             #   词法分析</span><br><span class="line">│   ├── parser/            #   语法分析</span><br><span class="line">│   └── ast/               #   抽象语法树</span><br><span class="line">├── midend/                # 中端模块</span><br><span class="line">│   ├── symbol/            #   符号表</span><br><span class="line">│   ├── llvm/              #   LLVM IR表示</span><br><span class="line">│   └── visit/             #   AST遍历器</span><br><span class="line">├── optimize/              # 优化模块</span><br><span class="line">├── backend/               # 后端模块</span><br><span class="line">│   └── mips/              #   MIPS生成</span><br><span class="line">├── error/                 # 错误处理模块</span><br><span class="line">└── util/                  # 工具模块</span><br></pre></td></tr></table></figure></div>
<h3 id="2-4-核心接口"><a href="#2-4-核心接口" class="headerlink" title="2.4 核心接口"></a>2.4 核心接口</h3><h4 id="2-4-1-模块间接口定义"><a href="#2-4-1-模块间接口定义" class="headerlink" title="2.4.1 模块间接口定义"></a>2.4.1 模块间接口定义</h4><p>各模块通过明确的接口进行数据传递，实现解耦设计：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>模块</th>
<th>输入接口</th>
<th>输出接口</th>
<th>核心方法</th>
<th>接口说明</th>
</tr>
</thead>
<tbody>
<tr>
<td><strong>Lexer</strong></td>
<td><code>String source</code>, <code>ErrorRecorder recorder</code></td>
<td><code>List&lt;Token&gt;</code></td>
<td><code>analyze()</code></td>
<td>将源代码字符串扫描为Token流，错误记录到recorder</td>
</tr>
<tr>
<td><strong>Parser</strong></td>
<td><code>List&lt;Token&gt;</code>, <code>ErrorRecorder recorder</code></td>
<td><code>CompUnit</code> (AST根节点)</td>
<td><code>parse()</code></td>
<td>根据Token流构建抽象语法树，同步构建符号表</td>
</tr>
<tr>
<td><strong>Visitor</strong></td>
<td><code>CompUnit</code></td>
<td><code>IrModule</code></td>
<td><code>generate()</code></td>
<td>遍历AST生成SSA形式的LLVM IR</td>
</tr>
<tr>
<td><strong>OptimizeManager</strong></td>
<td><code>IrModule</code></td>
<td><code>IrModule</code></td>
<td><code>init(IrModule)</code>, <code>optimize()</code></td>
<td>调度多个优化Pass对IR进行变换</td>
</tr>
<tr>
<td><strong>MipsBuilder</strong></td>
<td><code>IrModule</code></td>
<td><code>String</code> (MIPS汇编)</td>
<td><code>toMips()</code></td>
<td>将IR翻译为MIPS32汇编代码</td>
</tr>
<tr>
<td><strong>ErrorRecorder</strong></td>
<td>各模块错误信息</td>
<td><code>List&lt;Error&gt;</code></td>
<td><code>addError(int line, ErrorType type)</code>, <code>printErrors()</code></td>
<td>跨模块的错误收集与输出</td>
</tr>
</tbody>
</table>
</div>
<h4 id="2-4-2-主编译流程实现"><a href="#2-4-2-主编译流程实现" class="headerlink" title="2.4.2 主编译流程实现"></a>2.4.2 主编译流程实现</h4><p>各模块通过接口串联，在<code>Compiler.java</code>中实现完整编译流程：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Compiler</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> {</span><br><span class="line">        <span class="comment">// 读取源文件</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">sourceCode</span> <span class="operator">=</span> FileHandler.readFile(<span class="string">"testfile.txt"</span>);</span><br><span class="line">        <span class="type">ErrorRecorder</span> <span class="variable">recorder</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ErrorRecorder</span>();</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 1. 词法分析接口</span></span><br><span class="line">        <span class="type">Lexer</span> <span class="variable">lexer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Lexer</span>(sourceCode, recorder);</span><br><span class="line">        lexer.analyze();</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 2. 语法分析接口</span></span><br><span class="line">        <span class="type">Parser</span> <span class="variable">parser</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Parser</span>(lexer.getTokens(), recorder);</span><br><span class="line">        SymbolTable.initGlobalTable(recorder);</span><br><span class="line">        <span class="type">CompUnit</span> <span class="variable">astRoot</span> <span class="operator">=</span> parser.parse();</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 3. 语义分析接口</span></span><br><span class="line">        <span class="type">SemanticAnalyzer</span> <span class="variable">analyzer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SemanticAnalyzer</span>();</span><br><span class="line">        analyzer.analyze(astRoot);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 4. 优化前IR生成</span></span><br><span class="line">        <span class="type">Visitor</span> <span class="variable">visitorBeforeOpt</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Visitor</span>(astRoot);</span><br><span class="line">        <span class="type">IrModule</span> <span class="variable">llvmModuleBeforeOpt</span> <span class="operator">=</span> visitorBeforeOpt.generate();</span><br><span class="line">        <span class="type">MipsModule</span> <span class="variable">mipsModuleBeforeOpt</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MipsModule</span>();</span><br><span class="line">        MipsBuilder.setMipsModule(mipsModuleBeforeOpt);</span><br><span class="line">        llvmModuleBeforeOpt.toMips();</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 5. 重置符号表遍历状态（支持多次遍历）</span></span><br><span class="line">        SymbolTable.resetTraversal();</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 6. 优化后IR生成</span></span><br><span class="line">        <span class="type">Visitor</span> <span class="variable">visitorAfterOpt</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Visitor</span>(astRoot);</span><br><span class="line">        <span class="type">IrModule</span> <span class="variable">llvmModuleAfterOpt</span> <span class="operator">=</span> visitorAfterOpt.generate();</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 7. 优化接口</span></span><br><span class="line">        <span class="keyword">if</span> (Setting.FINE_TUNING) {</span><br><span class="line">            <span class="comment">// 构建名称映射</span></span><br><span class="line">            <span class="keyword">for</span> (IrFunction function : llvmModuleAfterOpt.getFunctionList()) {</span><br><span class="line">                <span class="keyword">if</span> (!function.isBuiltIn()) {</span><br><span class="line">                    function.buildNameMap();</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">            <span class="comment">// 执行优化Pass</span></span><br><span class="line">            OptimizeManager.init(llvmModuleAfterOpt);</span><br><span class="line">            OptimizeManager.optimize();</span><br><span class="line">            <span class="comment">// 移除Phi指令</span></span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">RemovePhi</span>().Optimize();</span><br><span class="line">        }</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 8. MIPS生成接口</span></span><br><span class="line">        <span class="type">MipsModule</span> <span class="variable">mipsModuleAfterOpt</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MipsModule</span>();</span><br><span class="line">        MipsBuilder.setMipsModule(mipsModuleAfterOpt);</span><br><span class="line">        llvmModuleAfterOpt.toMips();</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 9. 窥孔优化</span></span><br><span class="line">        <span class="keyword">if</span> (Setting.FINE_TUNING) {</span><br><span class="line">            <span class="type">PeepHole</span> <span class="variable">peepHole</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">PeepHole</span>(mipsModuleAfterOpt);</span><br><span class="line">            peepHole.peep();</span><br><span class="line">        }</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 10. 输出结果</span></span><br><span class="line">        <span class="keyword">if</span> (recorder.hasErrors()) {</span><br><span class="line">            OutputHandler.writeErrorOutput(recorder.getErrors(), <span class="string">"error.txt"</span>);</span><br><span class="line">        } <span class="keyword">else</span> {</span><br><span class="line">            OutputHandler.writeLexerOutput(lexer.getTokens(), <span class="string">"lexer.txt"</span>);</span><br><span class="line">            OutputHandler.writeTraceOutput(parser.getOutput(), <span class="string">"parser.txt"</span>);</span><br><span class="line">            OutputHandler.writeLlvmOutput(llvmModuleAfterOpt.toString(), <span class="string">"llvm_ir.txt"</span>);</span><br><span class="line">            OutputHandler.writeMipsOutput(mipsModuleAfterOpt.toString(), <span class="string">"mips.txt"</span>);</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<h4 id="2-4-3-错误处理的跨模块接口"><a href="#2-4-3-错误处理的跨模块接口" class="headerlink" title="2.4.3 错误处理的跨模块接口"></a>2.4.3 错误处理的跨模块接口</h4><p><code>ErrorRecorder</code> 作为共享对象在各模块间传递，实现统一的错误收集，各模块通过依赖注入获取 <code>ErrorRecorder</code> 实例：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ErrorRecorder</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> List&lt;Error&gt; errorList;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 添加错误（由Lexer、Parser、Visitor调用）</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addError</span><span class="params">(<span class="type">int</span> lineNumber， ErrorType errorType)</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 检查是否有错误</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">hasError</span><span class="params">()</span>;</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<hr>
<h2 id="三、词法分析设计"><a href="#三、词法分析设计" class="headerlink" title="三、词法分析设计"></a>三、词法分析设计</h2><h3 id="3-1-编码前设计"><a href="#3-1-编码前设计" class="headerlink" title="3.1 编码前设计"></a>3.1 编码前设计</h3><h4 id="3-1-1-扫描策略"><a href="#3-1-1-扫描策略" class="headerlink" title="3.1.1 扫描策略"></a>3.1.1 扫描策略</h4><p>采用向前偷看的线性扫描策略。基本逻辑为：读取首字符后，根据其所属字符类分发至不同的处理函数（如 <code>scanNumber</code>， <code>scanIdentifier</code> 等）。</p>
<h4 id="3-1-2-核心数据结构"><a href="#3-1-2-核心数据结构" class="headerlink" title="3.1.2 核心数据结构"></a>3.1.2 核心数据结构</h4><p><strong>Lexer 类的关键字段</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Lexer</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String source;                    <span class="comment">// 源代码字符串</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> List&lt;Token&gt; tokens;               <span class="comment">// 生成的Token列表</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">currentIndex</span> <span class="operator">=</span> <span class="number">0</span>;                   <span class="comment">// 当前扫描位置</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">lineNumber</span> <span class="operator">=</span> <span class="number">1</span>;                     <span class="comment">// 当前行号</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Map&lt;String， TokenType&gt; KEYWORDS;  <span class="comment">// 关键字映射表</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h4 id="3-1-3-异常处理预设"><a href="#3-1-3-异常处理预设" class="headerlink" title="3.1.3 异常处理预设"></a>3.1.3 异常处理预设</h4><p>针对文法中的非法符号（a 类错误），预设了错误记录机制。当遇到非预期字符（如单独的 <code>&amp;</code>）时，系统将记录当前行号及错误类型，并在内部通过容错逻辑将其暂拟为最接近的合法 Token 继续分析，以避免解析过程中断。</p>
<h3 id="3-2-实现过程中的调整"><a href="#3-2-实现过程中的调整" class="headerlink" title="3.2 实现过程中的调整"></a>3.2 实现过程中的调整</h3><h4 id="3-2-1-跨行注释的处理"><a href="#3-2-1-跨行注释的处理" class="headerlink" title="3.2.1 跨行注释的处理"></a>3.2.1 跨行注释的处理</h4><p>初始实现中未充分考虑多行注释 <code>/* ... */</code> 及字符串常量可能跨行的情况。测试时发现行号记录出现偏差，定位到问题后在扫描循环中增加了换行符检测逻辑，每遇到 <code>\n</code> 即更新行号计数器。这一修改虽然简单，但对后续所有错误信息的准确定位至关重要。</p>
<h4 id="3-2-2-关键字识别的优化"><a href="#3-2-2-关键字识别的优化" class="headerlink" title="3.2.2 关键字识别的优化"></a>3.2.2 关键字识别的优化</h4><p>最初采用分支结构判断关键字，随着关键字数量增加，代码维护性下降。改为使用 <code>HashMap</code> 存储关键字映射，在扫描到标识符时直接查表判断。这一调整既简化了代码结构，也提升了检索效率。</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Lexer.java 中的关键字映射示例</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Map&lt;String， TokenType&gt; KEYWORDS;</span><br><span class="line"><span class="keyword">static</span> {</span><br><span class="line">    KEYWORDS = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">    KEYWORDS.put(<span class="string">"const"</span>， TokenType.CONSTTK);</span><br><span class="line">    KEYWORDS.put(<span class="string">"int"</span>， TokenType.INTTK);</span><br><span class="line">    KEYWORDS.put(<span class="string">"void"</span>， TokenType.VOIDTK);</span><br><span class="line">    KEYWORDS.put(<span class="string">"if"</span>， TokenType.IFTK);</span><br><span class="line">    <span class="comment">// ... 其余关键字映射</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<ol>
<li><strong>单/多字符算子区分</strong>：针对 <code>&amp;&amp;</code>、<code>||</code> 等算子，强化了对不完整输入（如单独的 <code>&amp;</code>）的捕获能力，并将其规整到统一的错误处理流程中。</li>
</ol>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> <span class="string">'&amp;'</span>:</span><br><span class="line"> 	<span class="keyword">if</span> (match(<span class="string">'&amp;'</span>)) {</span><br><span class="line">      	addToken(TokenType.AND);</span><br><span class="line">   	} <span class="keyword">else</span> {</span><br><span class="line">        recorder.addError(lineNumber， ErrorType.ILLEGAL_SYMBOL);</span><br><span class="line">        addToken(TokenType.AND);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">break</span>;</span><br></pre></td></tr></table></figure></div>
<hr>
<h2 id="四、语法分析设计"><a href="#四、语法分析设计" class="headerlink" title="四、语法分析设计"></a>四、语法分析设计</h2><h3 id="4-1-编码前设计"><a href="#4-1-编码前设计" class="headerlink" title="4.1 编码前设计"></a>4.1 编码前设计</h3><h4 id="4-1-1-解析方案选择"><a href="#4-1-1-解析方案选择" class="headerlink" title="4.1.1 解析方案选择"></a>4.1.1 解析方案选择</h4><p>采用<strong>递归下降分析法</strong>。该方案的优势在于与 EBNF 文法描述高度对应，且便于在解析过程中嵌入 AST 构建逻辑与错误恢复机制。</p>
<h4 id="4-1-2-Parser-核心设计"><a href="#4-1-2-Parser-核心设计" class="headerlink" title="4.1.2 Parser 核心设计"></a>4.1.2 Parser 核心设计</h4><p><strong>Parser 类的关键字段</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Parser</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> List&lt;Token&gt; tokens;                <span class="comment">// Token流(由Lexer提供)</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">currentIndex</span> <span class="operator">=</span> <span class="number">0</span>;                    <span class="comment">// 当前解析位置</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> ErrorRecorder errorRecorder;       <span class="comment">// 错误记录器</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> List&lt;String&gt; outputBuffer;         <span class="comment">// 语法分析结果输出缓冲区</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Map&lt;TokenType， ErrorType&gt; MISSING_TOKEN_ERROR_MAP;  <span class="comment">// 缺失符号映射</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>核心方法</strong>：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 获取当前Token</span></span><br><span class="line"><span class="keyword">public</span> Token <span class="title function_">getCurrentToken</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 向前查看第n个Token</span></span><br><span class="line"><span class="keyword">public</span> Token <span class="title function_">peek</span><span class="params">(<span class="type">int</span> offset)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 期望特定Token，支持错误恢复</span></span><br><span class="line"><span class="keyword">public</span> Token <span class="title function_">expect</span><span class="params">(TokenType expectedType)</span>;</span><br><span class="line"><span class="keyword">public</span> Token <span class="title function_">expect</span><span class="params">(TokenType expectedType， ErrorType errorCode)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 记录非终结符</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">logComponent</span><span class="params">(SyntaxType type)</span>;</span><br></pre></td></tr></table></figure></div>
<h4 id="4-1-3-文法重构：消除左递归"><a href="#4-1-3-文法重构：消除左递归" class="headerlink" title="4.1.3 文法重构：消除左递归"></a>4.1.3 文法重构：消除左递归</h4><p>SysY 原始文法中的表达式部分（如 <code>AddExp</code>）存在直接左递归，不符合 LL(1) 预期。</p>
<p>实现中将其改造为：<code>AddExp → MulExp { ('+' | '-') MulExp }</code>。这种迭代式的处理方式在解析时能够正确维持运算符的左结合性。</p>
<h4 id="4-1-4-AST-节点设计"><a href="#4-1-4-AST-节点设计" class="headerlink" title="4.1.4 AST 节点设计"></a>4.1.4 AST 节点设计</h4><p>每个语法成分对应一个 AST 节点类，实现 <code>parse()</code> 静态方法用于递归构建。</p>
<p><strong>以 AddExp 为例</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AddExp</span> <span class="keyword">extends</span> <span class="title class_">Exp</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Exp left， right;   <span class="comment">// 左右操作数</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Token op;          <span class="comment">// 运算符</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Exp <span class="title function_">parse</span><span class="params">(Parser parser)</span> {</span><br><span class="line">        <span class="type">Exp</span> <span class="variable">left</span> <span class="operator">=</span> MulExp.parse(parser);</span><br><span class="line">        parser.logComponent(SyntaxType.AddExp);</span><br><span class="line">        <span class="keyword">while</span> (parser.match(TokenType.PLUS， TokenType.MINU)) {</span><br><span class="line">            <span class="type">Token</span> <span class="variable">op</span> <span class="operator">=</span> parser.advance();</span><br><span class="line">            <span class="type">Exp</span> <span class="variable">right</span> <span class="operator">=</span> MulExp.parse(parser);</span><br><span class="line">            parser.logComponent(SyntaxType.AddExp);</span><br><span class="line">            left = <span class="keyword">new</span> <span class="title class_">AddExp</span>(left， op， right);  <span class="comment">// 左结合</span></span><br><span class="line">        }</span><br><span class="line">        <span class="keyword">return</span> left;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h4 id="4-1-5-错误恢复逻辑分析"><a href="#4-1-5-错误恢复逻辑分析" class="headerlink" title="4.1.5 错误恢复逻辑分析"></a>4.1.5 错误恢复逻辑分析</h4><p>针对 i/j/k 类（缺少分隔符）错误，Parser 实现了基于虚拟 Token 补全的恢复策略。当 <code>expect</code> 方法检测到缺失符号时，会向 ErrorRecorder 报告行号，并生成一个临时的虚拟 Token 插入解析流，以保障后续节点解析的连续性。</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Token <span class="title function_">expect</span><span class="params">(TokenType expectedType， ErrorType errorCode)</span> {</span><br><span class="line">    <span class="keyword">if</span> (getCurrentToken().getTokenType() == expectedType) {</span><br><span class="line">        <span class="keyword">return</span> advance();  <span class="comment">// 匹配成功，前进并返回</span></span><br><span class="line">    }</span><br><span class="line">    <span class="comment">// 不匹配：记录错误，返回虚拟Token继续解析</span></span><br><span class="line">    errorRecorder.addError(getErrorLine()， errorCode);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Token</span>(<span class="string">""</span>， expectedType， getErrorLine());</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<h3 id="4-2-实现过程中的调整"><a href="#4-2-实现过程中的调整" class="headerlink" title="4.2 实现过程中的调整"></a>4.2 实现过程中的调整</h3><h4 id="4-2-1-声明和函数定义的区分"><a href="#4-2-1-声明和函数定义的区分" class="headerlink" title="4.2.1 声明和函数定义的区分"></a>4.2.1 声明和函数定义的区分</h4><p>变量声明 <code>int a;</code> 和函数定义 <code>int func()</code> 的开头标记序列相同，单一 Token 偷看无法区分。解决方案是采用多 Token 预读，通过判断标识符后是否为左括号来确定语法类型。这种偷看策略虽然简单，但能有效解决该场景下的歧义问题。</p>
<h4 id="4-2-2-输出格式的调整"><a href="#4-2-2-输出格式的调整" class="headerlink" title="4.2.2 输出格式的调整"></a>4.2.2 输出格式的调整</h4><p>测试阶段发现非终结符的输出时机与预期存在偏差。需要逐一调整 <code>logComponent</code> 的插入位置，通过反复测试验证输出顺序。特别是表达式部分，在将左递归改写为迭代形式后，输出时机的把控需要格外注意。</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// AddExp.java - 注意logComponent的调用时机</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> Exp <span class="title function_">parse</span><span class="params">(Parser parser)</span> {</span><br><span class="line">    <span class="type">Exp</span> <span class="variable">left</span> <span class="operator">=</span> MulExp.parse(parser);</span><br><span class="line">    parser.logComponent(SyntaxType.AddExp);  <span class="comment">// 首次输出</span></span><br><span class="line">    <span class="keyword">while</span> (parser.match(PLUS， MINU)) {</span><br><span class="line">        <span class="type">Token</span> <span class="variable">op</span> <span class="operator">=</span> parser.advance();</span><br><span class="line">        <span class="type">Exp</span> <span class="variable">right</span> <span class="operator">=</span> MulExp.parse(parser);</span><br><span class="line">        parser.logComponent(SyntaxType.AddExp);  <span class="comment">// 每次合并后输出</span></span><br><span class="line">        left = <span class="keyword">new</span> <span class="title class_">AddExp</span>(left， op， right);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> left;</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<hr>
<h2 id="五、语义分析设计"><a href="#五、语义分析设计" class="headerlink" title="五、语义分析设计"></a>五、语义分析设计</h2><h3 id="5-1-编码前设计"><a href="#5-1-编码前设计" class="headerlink" title="5.1 编码前设计"></a>5.1 编码前设计</h3><h4 id="5-1-1-嵌套作用域管理"><a href="#5-1-1-嵌套作用域管理" class="headerlink" title="5.1.1 嵌套作用域管理"></a>5.1.1 嵌套作用域管理</h4><p>采用<strong>层次化符号表</strong>模型。每个 <code>SymbolTable</code> 实例维护一个指向父表的引用（<code>fatherTable</code>）及子表列表(<code>sonTables</code>)。这种结构天然支持作用域的嵌套寻址逻辑：当检索某一标识符时，系统将自当前表向全局表递归向上检索，直到匹配成功或触发”未定义符号”异常。</p>
<h4 id="5-1-2-符号表数据结构"><a href="#5-1-2-符号表数据结构" class="headerlink" title="5.1.2 符号表数据结构"></a>5.1.2 符号表数据结构</h4><p><strong>SymbolTable 类的核心字段</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SymbolTable</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">int</span> depth;                         <span class="comment">// 作用域深度</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> index;                               <span class="comment">// 用于遍历子表的索引</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">int</span> scopeId;                       <span class="comment">// 唯一作用域标识符</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> ArrayList&lt;Symbol&gt; symbolList;      <span class="comment">// 符号顺序列表</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Hashtable&lt;String， Symbol&gt; symbolTable;  <span class="comment">// 符号哈希表</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> SymbolTable fatherTable;           <span class="comment">// 父作用域引用</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> ArrayList&lt;SymbolTable&gt; sonTables;  <span class="comment">// 子作用域列表</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> SymbolTable currentSymbolTable;   <span class="comment">// 当前活跃作用域</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> ErrorRecorder errorRecorder;      <span class="comment">// 错误记录器</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>符号检索方法</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在当前表查找</span></span><br><span class="line"><span class="keyword">public</span> Symbol <span class="title function_">getSymbol</span><span class="params">(String symbolName)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 向上递归查找到全局表</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> Symbol <span class="title function_">getSymbolGlobal</span><span class="params">(String symbolName)</span> {</span><br><span class="line">    <span class="type">SymbolTable</span> <span class="variable">table</span> <span class="operator">=</span> currentSymbolTable;</span><br><span class="line">    <span class="keyword">while</span> (table != <span class="literal">null</span>) {</span><br><span class="line">        <span class="type">Symbol</span> <span class="variable">symbol</span> <span class="operator">=</span> table.getSymbol(symbolName);</span><br><span class="line">        <span class="keyword">if</span> (symbol != <span class="literal">null</span>) <span class="keyword">return</span> symbol;</span><br><span class="line">        table = table.fatherTable;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(<span class="string">"Symbol not found： "</span> + symbolName);</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>作用域切换方法</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 创建并进入新的子作用域</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">enterNewScope</span><span class="params">()</span> {</span><br><span class="line">    <span class="type">int</span> <span class="variable">newDepth</span> <span class="operator">=</span> currentSymbolTable.depth + <span class="number">1</span>;</span><br><span class="line">    <span class="type">SymbolTable</span> <span class="variable">newTable</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SymbolTable</span>(currentSymbolTable， newDepth);</span><br><span class="line">    currentSymbolTable.addSonTable(newTable);</span><br><span class="line">    currentSymbolTable = newTable;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回父作用域</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">exitScope</span><span class="params">()</span> {</span><br><span class="line">    <span class="keyword">if</span> (currentSymbolTable.fatherTable != <span class="literal">null</span>) {</span><br><span class="line">        currentSymbolTable = currentSymbolTable.fatherTable;</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h4 id="5-1-3-Symbol-类型体系"><a href="#5-1-3-Symbol-类型体系" class="headerlink" title="5.1.3 Symbol 类型体系"></a>5.1.3 Symbol 类型体系</h4><p>设计了 <code>Symbol</code> 基类及两个主要子类：</p>
<p><strong>Symbol 基类</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">Symbol</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String symbolName;     <span class="comment">// 符号名</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> SymbolType symbolType; <span class="comment">// 符号类型</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">int</span> lineNumber;        <span class="comment">// 定义行号</span></span><br><span class="line">    <span class="keyword">private</span> IrValue irValue;             <span class="comment">// 关联的IR值</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isConst</span><span class="params">()</span>;            <span class="comment">// 是否为常量</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isArray</span><span class="params">()</span>;            <span class="comment">// 是否为数组</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isFunction</span><span class="params">()</span>;         <span class="comment">// 是否为函数</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>ValueSymbol (变量符号)</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ValueSymbol</span> <span class="keyword">extends</span> <span class="title class_">Symbol</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">boolean</span> isArray;       <span class="comment">// 是否为数组</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> arraySize;               <span class="comment">// 数组大小</span></span><br><span class="line">    <span class="keyword">private</span> InitVal initVal;             <span class="comment">// 初始化值</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 工厂方法创建不同类型的符号</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> ValueSymbol <span class="title function_">createVar</span><span class="params">(String name， <span class="type">int</span> lineNumber)</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> ValueSymbol <span class="title function_">createArray</span><span class="params">(String name， <span class="type">int</span> lineNumber， <span class="type">int</span> size)</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> ValueSymbol <span class="title function_">createConst</span><span class="params">(String name， <span class="type">int</span> lineNumber)</span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>FuncSymbol (函数符号)</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">FuncSymbol</span> <span class="keyword">extends</span> <span class="title class_">Symbol</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> List&lt;ValueSymbol&gt; params;  <span class="comment">// 参数列表</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">FuncSymbol</span><span class="params">(String name， SymbolType type， <span class="type">int</span> lineNumber)</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">FuncSymbol</span><span class="params">(String name， SymbolType type， <span class="type">int</span> lineNumber， List&lt;ValueSymbol&gt; params)</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isVoidFunc</span><span class="params">()</span>;         <span class="comment">// 是否为void函数</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isIntFunc</span><span class="params">()</span>;          <span class="comment">// 是否为int函数</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getParamCount</span><span class="params">()</span>;          <span class="comment">// 获取参数数量</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addParam</span><span class="params">(ValueSymbol param)</span>;</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>SymbolType 枚举</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">SymbolType</span> {</span><br><span class="line">    CONST，              <span class="comment">// const int a = 10;</span></span><br><span class="line">    CONST_ARRAY，        <span class="comment">// const int arr[10] = {...};</span></span><br><span class="line">    VAR，                <span class="comment">// int a;</span></span><br><span class="line">    ARRAY，              <span class="comment">// int arr[10];</span></span><br><span class="line">    STATIC_VAR，         <span class="comment">// static int a;</span></span><br><span class="line">    STATIC_ARRAY，       <span class="comment">// static int arr[10];</span></span><br><span class="line">    INT_FUNC，           <span class="comment">// int func()</span></span><br><span class="line">    VOID_FUNC;          <span class="comment">// void func()</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h4 id="5-1-4-SymbolManager-辅助类"><a href="#5-1-4-SymbolManager-辅助类" class="headerlink" title="5.1.4 SymbolManager 辅助类"></a>5.1.4 SymbolManager 辅助类</h4><p>设计了 <code>SymbolManager</code> 工具类用于管理符号相关的上下文状态和语义检查：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SymbolManager</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">FuncSymbol</span> <span class="variable">currentFunc</span> <span class="operator">=</span> <span class="literal">null</span>;  <span class="comment">// 当前所在函数</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="variable">loopDepth</span> <span class="operator">=</span> <span class="number">0</span>;              <span class="comment">// 循环嵌套深度</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 函数上下文管理</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">enterFunc</span><span class="params">(FuncSymbol func)</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">exitFunc</span><span class="params">()</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> FuncSymbol <span class="title function_">getCurrentFunc</span><span class="params">()</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="type">boolean</span> <span class="title function_">isInFunc</span><span class="params">()</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 循环上下文管理</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">enterLoop</span><span class="params">()</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">exitLoop</span><span class="params">()</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="type">boolean</span> <span class="title function_">isInLoop</span><span class="params">()</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 语义错误检查</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">checkConstModify</span><span class="params">(Symbol symbol， <span class="type">int</span> line)</span>;        <span class="comment">// h类错误</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">checkBreakContinue</span><span class="params">(<span class="type">int</span> line)</span>;                     <span class="comment">// m类错误</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">checkReturnState</span><span class="params">(<span class="type">boolean</span> hasReturnValue， <span class="type">int</span> line)</span>; <span class="comment">// f类错误</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">checkMissingReturn</span><span class="params">(<span class="type">boolean</span> hasReturn， <span class="type">int</span> line)</span>;  <span class="comment">// g类错误</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="type">boolean</span> <span class="title function_">checkFuncParamCount</span><span class="params">(FuncSymbol func， <span class="type">int</span> actualCount， <span class="type">int</span> line)</span>; <span class="comment">// d类错误</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">checkFuncParamTypes</span><span class="params">(FuncSymbol func， List&lt;Integer&gt; actualParams， <span class="type">int</span> line)</span>; <span class="comment">// e类错误</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">checkPrintFormat</span><span class="params">(String formatString， <span class="type">int</span> expCount， <span class="type">int</span> line)</span>; <span class="comment">// l类错误</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<p>这种设计将上下文状态管理和错误检查逻辑集中在一个工具类中，避免了在各个Visitor中重复编写状态管理代码。</p>
<h3 id="5-2-实现过程中的调整"><a href="#5-2-实现过程中的调整" class="headerlink" title="5.2 实现过程中的调整"></a>5.2 实现过程中的调整</h3><h4 id="5-2-1-符号表的重复遍历支持"><a href="#5-2-1-符号表的重复遍历支持" class="headerlink" title="5.2.1 符号表的重复遍历支持"></a>5.2.1 符号表的重复遍历支持</h4><p>编译器在生成优化前后的对比代码时，需要对同一棵AST多次生成IR，这要求符号表能够被多次遍历。初期实现中未考虑这一需求，符号表在首次遍历后内部索引未重置，导致第二次遍历时子作用域访问顺序错误。</p>
<p>解决方案是增加 <code>resetTraversal()</code> 方法，在开始新一轮IR生成前递归重置所有符号表的遍历索引：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// SymbolTable.java - 支持多次遍历的重置机制</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">resetTraversal</span><span class="params">()</span> {</span><br><span class="line">    resetTraversalRecursive(currentSymbolTable);</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">resetTraversalRecursive</span><span class="params">(SymbolTable table)</span> {</span><br><span class="line">    <span class="keyword">if</span> (table == <span class="literal">null</span>) <span class="keyword">return</span>;</span><br><span class="line">    table.index = -<span class="number">1</span>;  <span class="comment">// 重置子表遍历索引</span></span><br><span class="line">    <span class="keyword">for</span> (SymbolTable sonTable : table.sonTables) {</span><br><span class="line">        resetTraversalRecursive(sonTable);</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<p>在主编译流程中调用：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Compiler.java</span></span><br><span class="line"><span class="type">IrModule</span> <span class="variable">llvmModuleBeforeOpt</span> <span class="operator">=</span> visitorBeforeOpt.generate();</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line">SymbolTable.resetTraversal();  <span class="comment">// 重置遍历状态</span></span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line"><span class="type">IrModule</span> <span class="variable">llvmModuleAfterOpt</span> <span class="operator">=</span> visitorAfterOpt.generate();</span><br></pre></td></tr></table></figure></div>
<hr>
<h2 id="六、代码生成设计"><a href="#六、代码生成设计" class="headerlink" title="六、代码生成设计"></a>六、代码生成设计</h2><h3 id="6-1-编码前设计"><a href="#6-1-编码前设计" class="headerlink" title="6.1 编码前设计"></a>6.1 编码前设计</h3><h4 id="6-1-1-中间表示（IR）体系"><a href="#6-1-1-中间表示（IR）体系" class="headerlink" title="6.1.1 中间表示（IR）体系"></a>6.1.1 中间表示（IR）体系</h4><p>系统采用符合 <strong>LLVM IR 标准</strong> 的中间语言。选择该体系的主要原因是其具备三个核心优势：基于 SSA 形式的数据流描述、清晰的显式控制流以及强大的中端优化潜力。</p>
<h4 id="6-1-2-IR-四层结构设计"><a href="#6-1-2-IR-四层结构设计" class="headerlink" title="6.1.2 IR 四层结构设计"></a>6.1.2 IR 四层结构设计</h4><p>严格遵循 LLVM 的层次结构：</p>
<p><strong>IrModule (模块层)</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IrModule</span> {</span><br><span class="line">    <span class="keyword">private</span> List&lt;IrGlobalValue&gt; globalValueList;      <span class="comment">// 全局变量</span></span><br><span class="line">    <span class="keyword">private</span> List&lt;IrFunction&gt; functionList;            <span class="comment">// 函数列表</span></span><br><span class="line">    <span class="keyword">private</span> List&lt;IrConstantString&gt; constantStringList; <span class="comment">// 字符串常量</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">toMips</span><span class="params">()</span>;  <span class="comment">// 生成MIPS代码</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>IrFunction (函数层)</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IrFunction</span> <span class="keyword">extends</span> <span class="title class_">IrValue</span> {</span><br><span class="line">    <span class="keyword">private</span> IrType returnType;                    <span class="comment">// 返回值类型</span></span><br><span class="line">    <span class="keyword">private</span> List&lt;IrParameter&gt; parameters;         <span class="comment">// 参数列表</span></span><br><span class="line">    <span class="keyword">private</span> List&lt;IrBasicBlock&gt; basicBlocks;       <span class="comment">// 基本块列表</span></span><br><span class="line">    <span class="keyword">private</span> HashMap&lt;IrValue， Register&gt; valueRegisterMap;  <span class="comment">// 寄存器分配映射</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>IrBasicBlock (基本块层)</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IrBasicBlock</span> <span class="keyword">extends</span> <span class="title class_">IrValue</span> {</span><br><span class="line">    <span class="keyword">private</span> List&lt;Instruction&gt; instructions;       <span class="comment">// 指令列表</span></span><br><span class="line">    <span class="keyword">private</span> IrFunction parentFunction;            <span class="comment">// 所属函数</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 控制流图</span></span><br><span class="line">    <span class="keyword">private</span> List&lt;IrBasicBlock&gt; predecessors， successors;  <span class="comment">// 前驱/后继块</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 支配树(用于优化)</span></span><br><span class="line">    <span class="keyword">private</span> HashSet&lt;IrBasicBlock&gt; dominators;           <span class="comment">// 支配者集合</span></span><br><span class="line">    <span class="keyword">private</span> HashSet&lt;IrBasicBlock&gt; dominateFrontiers;    <span class="comment">// 支配边界</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 活跃变量分析(用于优化)</span></span><br><span class="line">    <span class="keyword">private</span> HashSet&lt;IrValue&gt; inValueSet， outValueSet;   <span class="comment">// 入口/出口活跃变量</span></span><br><span class="line">    <span class="keyword">private</span> HashSet&lt;IrValue&gt; defValueSet， useValueSet;  <span class="comment">// 定义/使用变量</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addInstruction</span><span class="params">(Instruction instr)</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isTerminated</span><span class="params">()</span>;  <span class="comment">// 是否已有终止指令</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>Instruction (指令层)</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">Instruction</span> <span class="keyword">extends</span> <span class="title class_">IrUser</span> {</span><br><span class="line">    <span class="keyword">protected</span> IrBasicBlock parentBlock;           <span class="comment">// 所属基本块</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> InstrType instrType;            <span class="comment">// 指令类型</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isTerminator</span><span class="params">()</span>;                <span class="comment">// 是否为终止指令</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">abstract</span> <span class="type">boolean</span> <span class="title function_">defValue</span><span class="params">()</span>;           <span class="comment">// 是否定义新值</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">eraseFromParent</span><span class="params">()</span>;                <span class="comment">// 删除指令及其use关系</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">abstract</span> String <span class="title function_">toIrString</span><span class="params">(Map&lt;IrValue， String&gt; nameMap)</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title function_">toMips</span><span class="params">()</span>;                <span class="comment">// 翻译为MIPS</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>主要指令类型</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 内存操作： AllocateInstr， StoreInstr， LoadInstr， GepInstr</span></span><br><span class="line"><span class="comment">// 运算： AluInstr， CompareInstr， ExtendInstr， TruncInstr</span></span><br><span class="line"><span class="comment">// 控制流： BranchInstr， JumpInstr， ReturnInstr， CallInstr</span></span><br><span class="line"><span class="comment">// SSA： PhiInstr， ParallelCopyInstr， MoveInstr</span></span><br></pre></td></tr></table></figure></div></p>
<h4 id="6-1-3-Use-Def-链设计"><a href="#6-1-3-Use-Def-链设计" class="headerlink" title="6.1.3 Use-Def 链设计"></a>6.1.3 Use-Def 链设计</h4><p>采用 <code>IrUse</code> 类实现使用者与被使用值之间的关联：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IrUse</span> {</span><br><span class="line">    <span class="keyword">private</span> IrUser user;         <span class="comment">// 使用者</span></span><br><span class="line">    <span class="keyword">private</span> IrValue usedValue;   <span class="comment">// 被使用的值</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<p><strong>IrValue 中的Use管理</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IrValue</span> {</span><br><span class="line">    <span class="keyword">protected</span> List&lt;IrUse&gt; irUseList;     <span class="comment">// 记录所有使用此值的IrUse对象</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addUse</span><span class="params">(IrUse irUse)</span>;     <span class="comment">// 添加新的使用</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">modifyAllUsersToNewValue</span><span class="params">(IrValue newValue)</span>;  <span class="comment">// 替换所有使用点</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>IrUser 作为使用者的基类</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IrUser</span> <span class="keyword">extends</span> <span class="title class_">IrValue</span> {</span><br><span class="line">    <span class="keyword">protected</span> ArrayList&lt;IrValue&gt; irUseValueList;    <span class="comment">// 此User使用的值列表</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addUseValue</span><span class="params">(IrValue value)</span>;         <span class="comment">// 添加使用的值(自动建立use关系)</span></span><br><span class="line">    <span class="keyword">public</span> IrValue <span class="title function_">getUseValue</span><span class="params">(<span class="type">int</span> index)</span>;          <span class="comment">// 获取指定位置的使用值</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">modifyValue</span><span class="params">(IrValue oldValue， IrValue newValue)</span>;  <span class="comment">// 替换使用的值</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p>这种设计实现了双向关联：</p>
<ul>
<li>IrValue通过irUseList知道谁在使用它</li>
<li>IrUser通过irUseValueList知道它使用了哪些值</li>
<li>替换值时自动更新所有引用关系</li>
</ul>
<h4 id="6-1-4-Visitor-模式的-IR-生成器"><a href="#6-1-4-Visitor-模式的-IR-生成器" class="headerlink" title="6.1.4 Visitor 模式的 IR 生成器"></a>6.1.4 Visitor 模式的 IR 生成器</h4><p>基于 <strong>Visitor 模式</strong> 实现 AST 到 IR 的转换映射。</p>
<p><strong>Visitor 类作为协调器</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Visitor</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> IrBuilder builder;              <span class="comment">// IR元素工厂</span></span><br><span class="line">    <span class="keyword">private</span> IrModule <span class="keyword">module</span>;                      <span class="comment">// 当前IR模块</span></span><br><span class="line">    <span class="keyword">private</span> IrFunction currentFunction;           <span class="comment">// 当前函数</span></span><br><span class="line">    <span class="keyword">private</span> IrBasicBlock currentBlock;            <span class="comment">// 当前基本块</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 各类节点的专用访问器</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> VisitorDecl declVisitor;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> VisitorStmt stmtVisitor;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> VisitorExp expVisitor;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> IrModule <span class="title function_">generate</span><span class="params">()</span> {</span><br><span class="line">        <span class="built_in">this</span>.<span class="keyword">module</span> = builder.createIrModule();</span><br><span class="line">        registerBuiltInFunctions();  <span class="comment">// 注册内建函数</span></span><br><span class="line">        declVisitor.visitCompUnit(rootNode);</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>.<span class="keyword">module</span>;</span><br><span class="line">    }</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">registerBuiltInFunctions</span><span class="params">()</span> {</span><br><span class="line">        <span class="comment">// getint， putint， putch， putstr</span></span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>IrBuilder 提供的工厂方法</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IrBuilder</span> {</span><br><span class="line">    <span class="comment">// 模块和函数</span></span><br><span class="line">    <span class="keyword">public</span> IrModule <span class="title function_">createIrModule</span><span class="params">()</span>;</span><br><span class="line">    <span class="keyword">public</span> IrFunction <span class="title function_">createFunction</span><span class="params">(IrModule <span class="keyword">module</span>， String name， IrType returnType)</span>;</span><br><span class="line">    <span class="keyword">public</span> IrBasicBlock <span class="title function_">createBasicBlock</span><span class="params">(IrFunction function)</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 常量创建</span></span><br><span class="line">    <span class="keyword">public</span> IrConstantInt <span class="title function_">createConstantInt32</span><span class="params">(<span class="type">int</span> value)</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 指令创建</span></span><br><span class="line">    <span class="keyword">public</span> AllocateInstr <span class="title function_">createAlloca</span><span class="params">(IrBasicBlock block， IrType type)</span>;</span><br><span class="line">    <span class="keyword">public</span> StoreInstr <span class="title function_">createStore</span><span class="params">(IrBasicBlock block， IrValue value， IrValue ptr)</span>;</span><br><span class="line">    <span class="keyword">public</span> LoadInstr <span class="title function_">createLoad</span><span class="params">(IrBasicBlock block， IrValue ptr)</span>;</span><br><span class="line">    <span class="keyword">public</span> AluInstr <span class="title function_">createAlu</span><span class="params">(IrBasicBlock block， AluOp op， IrValue left， IrValue right)</span>;</span><br><span class="line">    <span class="keyword">public</span> BranchInstr <span class="title function_">createBranch</span><span class="params">(IrBasicBlock block， IrValue cond， </span></span><br><span class="line"><span class="params">                                    IrBasicBlock trueBB， IrBasicBlock falseBB)</span>;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h3 id="6-2-实现过程中的调整"><a href="#6-2-实现过程中的调整" class="headerlink" title="6.2 实现过程中的调整"></a>6.2 实现过程中的调整</h3><h4 id="6-2-1-控制流的-IR-生成"><a href="#6-2-1-控制流的-IR-生成" class="headerlink" title="6.2.1 控制流的 IR 生成"></a>6.2.1 控制流的 IR 生成</h4><p>If 和 For 等控制流语句的 IR 生成涉及多个基本块的创建和跳转关系的维护。实现方案是维护一个当前基本块指针，指令生成时插入到当前块中，遇到分支时创建新的基本块并切换上下文。</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// VisitorStmt.java - If语句的IR生成</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">visitIfStmt</span><span class="params">(Stmt stmt)</span> {</span><br><span class="line">    <span class="type">IrValue</span> <span class="variable">condValue</span> <span class="operator">=</span> visitExp(stmt.getCond());</span><br><span class="line">    condValue = convertToI1(condValue);  <span class="comment">// 确保条件为i1类型</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 创建 then/else/follow 基本块</span></span><br><span class="line">    <span class="type">IrBasicBlock</span> <span class="variable">thenBlock</span> <span class="operator">=</span> builder.createBasicBlock(currentFunc);</span><br><span class="line">    <span class="type">IrBasicBlock</span> <span class="variable">elseBlock</span> <span class="operator">=</span> stmt.hasElse() ? builder.createBasicBlock(currentFunc) : <span class="literal">null</span>;</span><br><span class="line">    <span class="type">IrBasicBlock</span> <span class="variable">followBlock</span> <span class="operator">=</span> builder.createBasicBlock(currentFunc);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 生成条件分支</span></span><br><span class="line">    builder.createBranch(currentBlock， condValue， thenBlock， elseBlock != <span class="literal">null</span> ? elseBlock : followBlock);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 生成then块</span></span><br><span class="line">    coordinator.setCurrentBlock(thenBlock);</span><br><span class="line">    visitStmt(stmt.getThenStmt());</span><br><span class="line">    <span class="keyword">if</span> (!isCurrentBlockTerminated()) {</span><br><span class="line">        builder.createJump(getCurrentBlock()， followBlock);</span><br><span class="line">    }</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 生成else块(如果存在)</span></span><br><span class="line">    <span class="keyword">if</span> (elseBlock != <span class="literal">null</span>) {</span><br><span class="line">        coordinator.setCurrentBlock(elseBlock);</span><br><span class="line">        visitStmt(stmt.getElseStmt());</span><br><span class="line">        <span class="keyword">if</span> (!isCurrentBlockTerminated()) {</span><br><span class="line">            builder.createJump(getCurrentBlock()， followBlock);</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">    </span><br><span class="line">    coordinator.setCurrentBlock(followBlock);</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<h4 id="6-2-2-基本块终止状态检查"><a href="#6-2-2-基本块终止状态检查" class="headerlink" title="6.2.2 基本块终止状态检查"></a>6.2.2 基本块终止状态检查</h4><p>在IR生成过程中发现，某些语句(如return、break、continue)会终止当前基本块。如果不检查终止状态就继续向该块插入指令，会导致不可达代码被生成到同一基本块内。解决方案是在每个语句生成前检查 <code>isCurrentBlockTerminated()</code>，如果已终止则跳过后续指令生成。</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// VisitorStmt.java - 终止状态检查</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">visitAssignStmt</span><span class="params">(Stmt stmt)</span> {</span><br><span class="line">    <span class="keyword">if</span> (isCurrentBlockTerminated()) {</span><br><span class="line">        <span class="keyword">return</span>;  <span class="comment">// 当前块已终止，不再生成指令</span></span><br><span class="line">    }</span><br><span class="line">    <span class="type">IrValue</span> <span class="variable">rValue</span> <span class="operator">=</span> visitExp(stmt.getAssignExp());</span><br><span class="line">    <span class="type">IrValue</span> <span class="variable">lPtr</span> <span class="operator">=</span> visitLVal(stmt.getlVal());</span><br><span class="line">    builder.createStore(currentBlock， rValue， lPtr);</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<h4 id="6-2-3-内建函数的查找机制"><a href="#6-2-3-内建函数的查找机制" class="headerlink" title="6.2.3 内建函数的查找机制"></a>6.2.3 内建函数的查找机制</h4><p>在处理 <code>printf</code> 语句时需要调用 <code>putint</code>、<code>putstr</code> 等内建函数。初期实现中未提供统一的内建函数查找接口，导致每次都需要遍历函数列表。改进后增加了 <code>lookupBuiltinFunction</code> 方法，封装了从IrModule中查找内建函数的逻辑。</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// VisitorStmt.java - 查找内建函数</span></span><br><span class="line"><span class="keyword">private</span> IrFunction <span class="title function_">lookupBuiltinFunction</span><span class="params">(String name)</span> {</span><br><span class="line">    <span class="keyword">for</span> (IrFunction func : <span class="keyword">module</span>.getFunctionList()) {</span><br><span class="line">        <span class="keyword">if</span> (func.getName().equals(name)) <span class="keyword">return</span> func;</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(<span class="string">"Builtin function not found: "</span> + name);</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<hr>
<h2 id="七、代码优化设计"><a href="#七、代码优化设计" class="headerlink" title="七、代码优化设计"></a>七、代码优化设计</h2><h3 id="7-1-编码前设计"><a href="#7-1-编码前设计" class="headerlink" title="7.1 编码前设计"></a>7.1 编码前设计</h3><h4 id="7-1-1-优化架构规划"><a href="#7-1-1-优化架构规划" class="headerlink" title="7.1.1 优化架构规划"></a>7.1.1 优化架构规划</h4><p>构建了可扩展的 <strong>Pass Manager</strong> 架构。</p>
<p><strong>Optimizer 基类</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">Optimizer</span> {</span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">static</span> IrModule irModule;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title function_">Optimize</span><span class="params">()</span>;</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>OptimizeManager 调度器</strong>：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OptimizeManager</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> ArrayList&lt;Optimizer&gt; optimizerList;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">init</span><span class="params">(IrModule irModule)</span> {</span><br><span class="line">        Optimizer.SetIrModule(irModule);</span><br><span class="line">        optimizerList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        optimizerList.add(<span class="keyword">new</span> <span class="title class_">RemoveUnReachCode</span>());</span><br><span class="line">        optimizerList.add(<span class="keyword">new</span> <span class="title class_">MemToReg</span>());</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    }</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">optimize</span><span class="params">()</span> {</span><br><span class="line">        <span class="keyword">for</span> (Optimizer optimizer : optimizerList) {</span><br><span class="line">            optimizer.Optimize();</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<h4 id="7-1-2-核心优化Pass设计"><a href="#7-1-2-核心优化Pass设计" class="headerlink" title="7.1.2 核心优化Pass设计"></a>7.1.2 核心优化Pass设计</h4><p><strong>MemToReg</strong>：</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MemToReg</span> <span class="keyword">extends</span> <span class="title class_">Optimizer</span> {</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">Optimize</span><span class="params">()</span> {</span><br><span class="line">        <span class="keyword">for</span> (IrFunction function : irModule.getFunctionList()) {</span><br><span class="line">            <span class="type">IrBasicBlock</span> <span class="variable">entryBlock</span> <span class="operator">=</span> function.getBasicBlocks().get(<span class="number">0</span>);</span><br><span class="line">            <span class="keyword">for</span> (IrBasicBlock block : function.getBasicBlocks()) {</span><br><span class="line">                <span class="keyword">for</span> (Instruction instr : block.getInstructions()) {</span><br><span class="line">                    <span class="keyword">if</span> (isValueAllocate(instr)) {</span><br><span class="line">                        <span class="type">InsertPhi</span> <span class="variable">insertPhi</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">InsertPhi</span>((AllocateInstr) instr， entryBlock);</span><br><span class="line">                        insertPhi.addPhi();  <span class="comment">// 插入Phi并重命名</span></span><br><span class="line">                    }</span><br><span class="line">                }</span><br><span class="line">            }</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<p><strong>InsertPhi 的工作流程</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">InsertPhi</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> AllocateInstr allocateInstr;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> HashSet&lt;Instruction&gt; defineInstrs;  <span class="comment">// store指令</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> HashSet&lt;Instruction&gt; useInstrs;     <span class="comment">// load指令</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addPhi</span><span class="params">()</span> {</span><br><span class="line">        buildDefineUseRelationship();  <span class="comment">// 1. 收集store和load</span></span><br><span class="line">        insertPhiToBlock();            <span class="comment">// 2. 在支配边界插入Phi</span></span><br><span class="line">        convertLoadStore(entryBlock);  <span class="comment">// 3. 替换load/store</span></span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>其他核心Pass</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 编译期函数求值 - 识别纯函数调用，编译期执行并用常量替换</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Ctfe</span> <span class="keyword">extends</span> <span class="title class_">Optimizer</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 局部值编号 - 为每个基本块建立表达式哈希表，消除重复计算</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Lvn</span> <span class="keyword">extends</span> <span class="title class_">Optimizer</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 全局代码移动 - 上移到最早位置，下沉到最晚位置</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Gcm</span> <span class="keyword">extends</span> <span class="title class_">Optimizer</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 死代码删除 - 删除无副作用且结果未被使用的指令</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RemoveDeadCode</span> <span class="keyword">extends</span> <span class="title class_">Optimizer</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 控制流图构建 - 构建前驱后继，计算支配树和支配边界</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CfgBuilder</span> <span class="keyword">extends</span> <span class="title class_">Optimizer</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 活跃变量分析 - 为每个基本块计算in/out集合</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ActiveAnalysis</span> <span class="keyword">extends</span> <span class="title class_">Optimizer</span>;</span><br></pre></td></tr></table></figure></div></p>
<h4 id="7-1-3-核心算法"><a href="#7-1-3-核心算法" class="headerlink" title="7.1.3 核心算法"></a>7.1.3 核心算法</h4><p>重点预研了基于 SSA 的Mem2Reg算法。设计思路为：首先确定局部变量的支配边界，随后在该位置插入 Phi 节点，最后通过重命名算法消除冗余的内存读写。</p>
<p>支配树算法采用经典的 Lengauer-Tarjan 算法，时间复杂度接近线性。支配边界的计算基于支配树完成，用于确定Phi节点的插入位置。</p>
<h3 id="7-2-实现过程中的调整"><a href="#7-2-实现过程中的调整" class="headerlink" title="7.2 实现过程中的调整"></a>7.2 实现过程中的调整</h3><h4 id="7-2-1-优化的迭代执行"><a href="#7-2-1-优化的迭代执行" class="headerlink" title="7.2.1 优化的迭代执行"></a>7.2.1 优化的迭代执行</h4><p>初始设计中每个优化 Pass 仅执行一次，但测试发现优化效果不够理想。分析原因是某些优化(如常量折叠)会产生新的死代码，而死代码删除又可能暴露新的优化机会，单次执行无法充分挖掘优化潜力。改进方案是采用迭代执行策略，多轮重复核心优化 Pass。经过实验，5 轮迭代基本可以达到收敛。</p>
<div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// OptimizeManager.java - Pass序列配置</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">init</span><span class="params">(IrModule irModule)</span> {</span><br><span class="line">    optimizerList.add(<span class="keyword">new</span> <span class="title class_">RemoveUnReachCode</span>());</span><br><span class="line">    optimizerList.add(<span class="keyword">new</span> <span class="title class_">FuncInline</span>());</span><br><span class="line">    optimizerList.add(<span class="keyword">new</span> <span class="title class_">MemToReg</span>());</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 核心优化循环交替执行5次</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) {</span><br><span class="line">        optimizerList.add(<span class="keyword">new</span> <span class="title class_">Ctfe</span>());</span><br><span class="line">        optimizerList.add(<span class="keyword">new</span> <span class="title class_">Lvn</span>());</span><br><span class="line">        optimizerList.add(<span class="keyword">new</span> <span class="title class_">Gcm</span>());</span><br><span class="line">        optimizerList.add(<span class="keyword">new</span> <span class="title class_">RemoveDeadCode</span>());</span><br><span class="line">    }</span><br><span class="line">    optimizerList.add(<span class="keyword">new</span> <span class="title class_">AllocateRegister</span>());</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div>
<h4 id="7-2-2-编译期函数求值"><a href="#7-2-2-编译期函数求值" class="headerlink" title="7.2.2 编译期函数求值"></a>7.2.2 编译期函数求值</h4><p>测试过程中发现部分函数调用在编译期即可完成求值，特别是一些纯计算函数。针对这种情况增加了 Ctfe (Compile-Time Function Evaluation) 优化，在编译阶段直接计算函数返回值。该优化能够有效减少运行时的函数调用开销。</p>
<h4 id="7-2-3-寄存器分配策略"><a href="#7-2-3-寄存器分配策略" class="headerlink" title="7.2.3 寄存器分配策略"></a>7.2.3 寄存器分配策略</h4><p><code>RegisterAllocator</code> 实现基于活跃变量分析的寄存器分配：</p>
<p><strong>核心数据结构</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">RegisterAllocator</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> IrFunction irFunction;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> ArrayList&lt;Register&gt; registerSet;              <span class="comment">// 可用寄存器池</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> HashMap&lt;Register， IrValue&gt; registerValueMap;  <span class="comment">// 寄存器-&gt;占用它的值</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> HashMap&lt;IrValue， Register&gt; valueRegisterMap;  <span class="comment">// 值-&gt;分配的寄存器</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">allocate</span><span class="params">(IrBasicBlock entryBlock)</span> {</span><br><span class="line">        HashMap&lt;IrValue， Instruction&gt; lastUseMap = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">        recordLastUse(entryBlock， lastUseMap);  <span class="comment">// 记录每个值的最后使用位置</span></span><br><span class="line">        allocateOneBlock(entryBlock， lastUseMap);  <span class="comment">// 分配寄存器</span></span><br><span class="line">        <span class="comment">// 递归处理支配树子节点...</span></span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>分配核心逻辑</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 为一条指令分配寄存器</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">allocateInstruction</span><span class="params">(Instruction instr)</span> {</span><br><span class="line">    <span class="keyword">if</span> (!instr.defValue()) <span class="keyword">return</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 找第一个空闲寄存器</span></span><br><span class="line">    <span class="keyword">for</span> (Register register : registerSet) {</span><br><span class="line">        <span class="keyword">if</span> (!registerValueMap.containsKey(register)) {</span><br><span class="line">            registerValueMap.put(register， instr);</span><br><span class="line">            valueRegisterMap.put(instr， register);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 检查并释放寄存器</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">checkAndFreeRegister</span><span class="params">(Instruction instr， </span></span><br><span class="line"><span class="params">                                   HashMap&lt;IrValue， Instruction&gt; lastUseMap)</span> {</span><br><span class="line">    <span class="keyword">for</span> (IrValue usedValue : instr.getUseValueList()) {</span><br><span class="line">        <span class="comment">// 如果这是值的最后一次使用，且不在out集合中</span></span><br><span class="line">        <span class="keyword">if</span> (lastUseMap.get(usedValue) == instr &amp;&amp;</span><br><span class="line">            !instr.isBlockOutValue(usedValue)) {</span><br><span class="line">            registerValueMap.remove(valueRegisterMap.get(usedValue));</span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h4 id="7-2-4-MIPS-层面的窥孔优化"><a href="#7-2-4-MIPS-层面的窥孔优化" class="headerlink" title="7.2.4 MIPS 层面的窥孔优化"></a>7.2.4 MIPS 层面的窥孔优化</h4><p>在生成 MIPS 汇编后增加了 <code>PeepHole</code> 窥孔优化层，用于消除局部冗余指令。采用滑动窗口方式扫描指令序列，识别并消除特定的冗余模式：</p>
<p><strong>优化模式示例</strong>：<br><div class="code-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">1. 连续store： sw $t1， 0($sp); sw $t2， 0($sp) → 删除第一条</span><br><span class="line">2. 连续move： move $a， $b; move $c， $a → move $a， $b; move $c， $b</span><br><span class="line">3. load-store同地址： lw $t， off($b); sw $t， off($b) → 删除sw</span><br><span class="line">4. store-load同地址： sw $t1， off($b); lw $t2， off($b) → sw...; move $t2， $t1</span><br><span class="line">5. 自我move： move $t， $t → 删除</span><br><span class="line">6. 无效jump： 跳转到下一条指令 → 删除</span><br></pre></td></tr></table></figure></div></p>
<p>窥孔优化迭代执行直到不再有优化发生。</p>
<hr>
<h2 id="八、目标代码生成设计"><a href="#八、目标代码生成设计" class="headerlink" title="八、目标代码生成设计"></a>八、目标代码生成设计</h2><h3 id="8-1-编码前设计"><a href="#8-1-编码前设计" class="headerlink" title="8.1 编码前设计"></a>8.1 编码前设计</h3><h4 id="8-1-1-MIPS-架构规划"><a href="#8-1-1-MIPS-架构规划" class="headerlink" title="8.1.1 MIPS 架构规划"></a>8.1.1 MIPS 架构规划</h4><p>目标平台为 MIPS32 架构，需要将 LLVM IR 翻译为MIPS汇编。设计分为两个阶段：</p>
<ol>
<li>IR指令到MIPS指令的映射</li>
<li>虚拟寄存器到物理寄存器的绑定</li>
</ol>
<h4 id="8-1-2-MIPS-模块结构"><a href="#8-1-2-MIPS-模块结构" class="headerlink" title="8.1.2 MIPS 模块结构"></a>8.1.2 MIPS 模块结构</h4><p><strong>MipsModule 结构</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MipsModule</span> {</span><br><span class="line">    <span class="keyword">private</span> ArrayList&lt;MipsAssembly&gt; dataSegment;  <span class="comment">// 数据段</span></span><br><span class="line">    <span class="keyword">private</span> ArrayList&lt;MipsAssembly&gt; textSegment;  <span class="comment">// 代码段</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addDataSegment</span><span class="params">(MipsAssembly assembly)</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addTextSegment</span><span class="params">(MipsAssembly assembly)</span>;</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>MipsAssembly 指令体系</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 抽象基类</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">MipsAssembly</span> {</span><br><span class="line">    <span class="keyword">protected</span> MipsType mipsType;     <span class="comment">// 指令类型枚举</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">abstract</span> String <span class="title function_">toString</span><span class="params">()</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 算术逻辑指令</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MipsAlu</span> <span class="keyword">extends</span> <span class="title class_">MipsAssembly</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">AluType</span> {</span><br><span class="line">        ADD， SUB， ADDU， SUBU， AND， OR， NOR， XOR， SLT， SLTU，</span><br><span class="line">        SLLV， SRAV， SRLV，  <span class="comment">// 变量移位</span></span><br><span class="line">        ADDI， ADDIU， ANDI， ORI， XORI， SLTI， SLTIU，  <span class="comment">// 立即数</span></span><br><span class="line">        SLL， SRA， SRL  <span class="comment">// 常量移位</span></span><br><span class="line">    }</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> AluType aluType;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Register rd， rs， rt;  <span class="comment">// R型</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Integer immediate;    <span class="comment">// I型</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// R型: add $rd， $rs， $rt</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsAlu</span><span class="params">(AluType aluType， Register rd， Register rs， Register rt)</span>;</span><br><span class="line">    <span class="comment">// I型: addi $rd， $rs， imm</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsAlu</span><span class="params">(AluType aluType， Register rd， Register rs， <span class="type">int</span> immediate)</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// Load/Store指令</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MipsLsu</span> <span class="keyword">extends</span> <span class="title class_">MipsAssembly</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">LsuType</span> {</span><br><span class="line">        LW， LH， LHU， LB， LBU，  <span class="comment">// Load</span></span><br><span class="line">        SW， SH， SB             <span class="comment">// Store</span></span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> LsuType lsuType;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Register rd， base;  <span class="comment">// 数据寄存器， 基址寄存器</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Integer offset;     <span class="comment">// 偏移量</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> String label;       <span class="comment">// 全局变量标签</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// lw $rd， offset($base)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsLsu</span><span class="params">(LsuType lsuType， Register rd， Register base， Integer offset)</span>;</span><br><span class="line">    <span class="comment">// lw $rd， label+offset</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsLsu</span><span class="params">(LsuType lsuType， Register rd， Integer offset， String label)</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 比较指令</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MipsCompare</span> <span class="keyword">extends</span> <span class="title class_">MipsAssembly</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">CompareType</span> { SLT， SLE， SGT， SGE， SEQ， SNE， SLTI }</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsCompare</span><span class="params">(CompareType type， Register rd， Register rs， Register rt)</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsCompare</span><span class="params">(CompareType type， Register rd， Register rs， Integer imm)</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 乘除法指令</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MipsMdu</span> <span class="keyword">extends</span> <span class="title class_">MipsAssembly</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">MduType</span> { MFHI， MFLO， MTHI， MTLO， MULT， DIV }</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsMdu</span><span class="params">(MduType mduType， Register rd)</span>;  <span class="comment">// MF/MT</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsMdu</span><span class="params">(MduType mduType， Register rs， Register rt)</span>;  <span class="comment">// MULT/DIV</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 分支指令</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MipsBranch</span> <span class="keyword">extends</span> <span class="title class_">MipsAssembly</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">BranchType</span> { BEQ， BNE， BGTZ， BLEZ， BGEZ， BLTZ }</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsBranch</span><span class="params">(BranchType type， Register rs， Register rt， String label)</span>;  <span class="comment">// BEQ/BNE</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsBranch</span><span class="params">(BranchType type， Register rs， String label)</span>;  <span class="comment">// 其他</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 跳转指令</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MipsJump</span> <span class="keyword">extends</span> <span class="title class_">MipsAssembly</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">JumpType</span> { J， JAL， JR }</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsJump</span><span class="params">(JumpType jumpType， String targetLabel)</span>;  <span class="comment">// J/JAL</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsJump</span><span class="params">(JumpType jumpType， Register rd)</span>;  <span class="comment">// JR</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 系统调用和标签</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MipsSyscall</span> <span class="keyword">extends</span> <span class="title class_">MipsAssembly</span>;</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MipsLabel</span> <span class="keyword">extends</span> <span class="title class_">MipsAssembly</span> { </span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsLabel</span><span class="params">(String label)</span>; </span><br><span class="line">}</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MipsAnnotation</span> <span class="keyword">extends</span> <span class="title class_">MipsAssembly</span> {  <span class="comment">// 注释</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsAnnotation</span><span class="params">(String comment)</span>;</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 伪指令(Mars模拟器支持)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MarsLi</span> <span class="keyword">extends</span> <span class="title class_">MipsMars</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MarsLi</span><span class="params">(Register rd， Integer number)</span>;  <span class="comment">// li $rd， imm</span></span><br><span class="line">}</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MarsLa</span> <span class="keyword">extends</span> <span class="title class_">MipsMars</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MarsLa</span><span class="params">(Register rd， String label)</span>;    <span class="comment">// la $rd， label</span></span><br><span class="line">}</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MarsMove</span> <span class="keyword">extends</span> <span class="title class_">MipsMars</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MarsMove</span><span class="params">(Register dst， Register src)</span>; <span class="comment">// move $dst， $src</span></span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 数据段指令</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MipsWord</span> <span class="keyword">extends</span> <span class="title class_">MipsDataAssembly</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsWord</span><span class="params">(String label， List&lt;Integer&gt; data)</span>;  <span class="comment">// label: .word ...</span></span><br><span class="line">}</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MipsAsciiz</span> <span class="keyword">extends</span> <span class="title class_">MipsDataAssembly</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsAsciiz</span><span class="params">(String label， String str)</span>;  <span class="comment">// label: .asciiz "..."</span></span><br><span class="line">}</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MipsSpace</span> <span class="keyword">extends</span> <span class="title class_">MipsDataAssembly</span> {</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MipsSpace</span><span class="params">(String label， <span class="type">int</span> size)</span>;  <span class="comment">// label: .space size</span></span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h4 id="8-1-3-寄存器规划"><a href="#8-1-3-寄存器规划" class="headerlink" title="8.1.3 寄存器规划"></a>8.1.3 寄存器规划</h4><p><strong>Register 枚举定义</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">Register</span> {</span><br><span class="line">    ZERO(<span class="string">"$zero"</span>)，  <span class="comment">// 常量0</span></span><br><span class="line">    V0(<span class="string">"$v0"</span>)， V1(<span class="string">"$v1"</span>)，  <span class="comment">// 返回值</span></span><br><span class="line">    A0(<span class="string">"$a0"</span>)， A1(<span class="string">"$a1"</span>)， A2(<span class="string">"$a2"</span>)， A3(<span class="string">"$a3"</span>)，  <span class="comment">// 函数参数</span></span><br><span class="line">    T0(<span class="string">"$t0"</span>)， T1(<span class="string">"$t1"</span>)， T2(<span class="string">"$t2"</span>)， T3(<span class="string">"$t3"</span>)，  <span class="comment">// 临时寄存器(10个)</span></span><br><span class="line">    T4(<span class="string">"$t4"</span>)， T5(<span class="string">"$t5"</span>)， T6(<span class="string">"$t6"</span>)， T7(<span class="string">"$t7"</span>)， T8(<span class="string">"$t8"</span>)， T9(<span class="string">"$t9"</span>)，</span><br><span class="line">    S0(<span class="string">"$s0"</span>)， S1(<span class="string">"$s1"</span>)， S2(<span class="string">"$s2"</span>)， S3(<span class="string">"$s3"</span>)，  <span class="comment">// 保存寄存器(8个)</span></span><br><span class="line">    S4(<span class="string">"$s4"</span>)， S5(<span class="string">"$s5"</span>)， S6(<span class="string">"$s6"</span>)， S7(<span class="string">"$s7"</span>)，</span><br><span class="line">    SP(<span class="string">"$sp"</span>)， RA(<span class="string">"$ra"</span>);  <span class="comment">// 栈指针， 返回地址</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 获取可用于分配的寄存器(18个： 10个$t + 8个$s)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> ArrayList&lt;Register&gt; <span class="title function_">getUsableRegisters</span><span class="params">()</span>;</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h3 id="8-2-实现过程"><a href="#8-2-实现过程" class="headerlink" title="8.2 实现过程"></a>8.2 实现过程</h3><h4 id="8-2-1-MipsBuilder-设计"><a href="#8-2-1-MipsBuilder-设计" class="headerlink" title="8.2.1 MipsBuilder 设计"></a>8.2.1 MipsBuilder 设计</h4><p><code>MipsBuilder</code> 作为IR到MIPS翻译的核心工具类：</p>
<p><strong>核心字段</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MipsBuilder</span> {</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> MipsModule mipsModule;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> IrFunction currentFunction;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> HashMap&lt;IrValue， Register&gt; valueToRegisterMap;  <span class="comment">// IR值-&gt;寄存器</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> HashMap&lt;IrValue， Integer&gt; valueToStackMap;       <span class="comment">// 溢出值-&gt;栈偏移</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> currentStackOffset;</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<p><strong>核心方法</strong>：<br><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 将IR值加载到指定寄存器</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">loadValueToRegister</span><span class="params">(IrValue value， Register targetReg)</span> {</span><br><span class="line">    <span class="keyword">if</span> (value <span class="keyword">instanceof</span> IrConstantInt constInt) {</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">MarsLi</span>(targetReg， constInt.getValue());  <span class="comment">// 常量： li</span></span><br><span class="line">    } <span class="keyword">else</span> <span class="keyword">if</span> (value <span class="keyword">instanceof</span> IrGlobalValue globalValue) {</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">MarsLa</span>(targetReg， globalValue.getName());  <span class="comment">// 全局变量： la</span></span><br><span class="line">    } <span class="keyword">else</span> {</span><br><span class="line">        <span class="type">Register</span> <span class="variable">srcReg</span> <span class="operator">=</span> valueToRegisterMap.get(value);</span><br><span class="line">        <span class="keyword">if</span> (srcReg != <span class="literal">null</span>) {</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">MarsMove</span>(targetReg， srcReg);  <span class="comment">// 寄存器： move</span></span><br><span class="line">        } <span class="keyword">else</span> {</span><br><span class="line">            <span class="type">Integer</span> <span class="variable">offset</span> <span class="operator">=</span> valueToStackMap.get(value);</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">MipsLsu</span>(LW， targetReg， offset， Register.SP);  <span class="comment">// 栈： lw</span></span><br><span class="line">        }</span><br><span class="line">    }</span><br><span class="line">}</span><br><span class="line"></span><br><span class="line"><span class="comment">// 为溢出值分配栈空间</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> Integer <span class="title function_">allocateStackForValue</span><span class="params">(IrValue value)</span> {</span><br><span class="line">    <span class="type">Integer</span> <span class="variable">offset</span> <span class="operator">=</span> valueToStackMap.get(value);</span><br><span class="line">    <span class="keyword">if</span> (offset == <span class="literal">null</span>) {</span><br><span class="line">        offset = allocateStack(<span class="number">4</span>);</span><br><span class="line">        valueToStackMap.put(value， offset);</span><br><span class="line">    }</span><br><span class="line">    <span class="keyword">return</span> offset;</span><br><span class="line">}</span><br></pre></td></tr></table></figure></div></p>
<h4 id="8-2-2-指令翻译策略"><a href="#8-2-2-指令翻译策略" class="headerlink" title="8.2.2 指令翻译策略"></a>8.2.2 指令翻译策略</h4><p>每个IR指令实现 <code>toMips()</code> 方法，完成到MIPS的翻译：</p>
<p><strong>指令翻译示例</strong>：<br><div class="code-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">AluInstr: %3 = add i32 %1， %2</span><br><span class="line">→ add $t2， $t0， $t1  (假设%1在$t0， %2在$t1， %3分配到$t2)</span><br><span class="line"></span><br><span class="line">BranchInstr: br i1 %cond， label %then， label %else</span><br><span class="line">→ beq $t0， $zero， else_label  (假设%cond在$t0)</span><br><span class="line">   j then_label</span><br><span class="line"></span><br><span class="line">CallInstr: 保存现场 → 准备参数 → jal → 恢复现场 → 获取返回值</span><br><span class="line"></span><br><span class="line">Load/Store: lw/sw $t， offset($base)</span><br></pre></td></tr></table></figure></div></p>
<h4 id="8-2-3-栈帧布局"><a href="#8-2-3-栈帧布局" class="headerlink" title="8.2.3 栈帧布局"></a>8.2.3 栈帧布局</h4><p>每个函数的栈帧结构(从高地址到低地址)：<br>每个函数的栈帧结构(从高地址到低地址)：<br><div class="code-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">+------------------+  ← 调用前$sp</span><br><span class="line">|   参数5，6，...   |  (超过4个的参数)</span><br><span class="line">|   返回地址$ra     |</span><br><span class="line">|  saved $s寄存器   |</span><br><span class="line">|  局部变量/临时值   |</span><br><span class="line">+------------------+  ← 当前$sp</span><br></pre></td></tr></table></figure></div></p>
<p>函数进入/返回流程：</p>
<ul>
<li>进入： 分配栈帧 → 保存<mjx-container class="MathJax" jax="SVG"><svg style="vertical-align: -0.452ex;" xmlns="http://www.w3.org/2000/svg" width="17.049ex" height="2.149ex" role="img" focusable="false" viewBox="0 -750 7535.6 950"><g stroke="currentColor" fill="currentColor" stroke-width="0" transform="scale(1,-1)"><g data-mml-node="math"><g data-mml-node="mi"><path data-c="1D45F" d="M21 287Q22 290 23 295T28 317T38 348T53 381T73 411T99 433T132 442Q161 442 183 430T214 408T225 388Q227 382 228 382T236 389Q284 441 347 441H350Q398 441 422 400Q430 381 430 363Q430 333 417 315T391 292T366 288Q346 288 334 299T322 328Q322 376 378 392Q356 405 342 405Q286 405 239 331Q229 315 224 298T190 165Q156 25 151 16Q138 -11 108 -11Q95 -11 87 -5T76 7T74 17Q74 30 114 189T154 366Q154 405 128 405Q107 405 92 377T68 316T57 280Q55 278 41 278H27Q21 284 21 287Z"></path></g><g data-mml-node="mi" transform="translate(451,0)"><path data-c="1D44E" d="M33 157Q33 258 109 349T280 441Q331 441 370 392Q386 422 416 422Q429 422 439 414T449 394Q449 381 412 234T374 68Q374 43 381 35T402 26Q411 27 422 35Q443 55 463 131Q469 151 473 152Q475 153 483 153H487Q506 153 506 144Q506 138 501 117T481 63T449 13Q436 0 417 -8Q409 -10 393 -10Q359 -10 336 5T306 36L300 51Q299 52 296 50Q294 48 292 46Q233 -10 172 -10Q117 -10 75 30T33 157ZM351 328Q351 334 346 350T323 385T277 405Q242 405 210 374T160 293Q131 214 119 129Q119 126 119 118T118 106Q118 61 136 44T179 26Q217 26 254 59T298 110Q300 114 325 217T351 328Z"></path></g><g data-mml-node="mo" transform="translate(1257.8,0)"><path data-c="2192" d="M56 237T56 250T70 270H835Q719 357 692 493Q692 494 692 496T691 499Q691 511 708 511H711Q720 511 723 510T729 506T732 497T735 481T743 456Q765 389 816 336T935 261Q944 258 944 250Q944 244 939 241T915 231T877 212Q836 186 806 152T761 85T740 35T732 4Q730 -6 727 -8T711 -11Q691 -11 691 0Q691 7 696 25Q728 151 835 230H70Q56 237 56 250Z"></path></g><g data-mml-node="mi" transform="translate(2535.6,0)"><text data-variant="normal" transform="scale(1,-1)" font-size="884px" font-family="serif">保</text></g><g data-mml-node="mi" transform="translate(3535.6,0)"><text data-variant="normal" transform="scale(1,-1)" font-size="884px" font-family="serif">存</text></g><g data-mml-node="mi" transform="translate(4535.6,0)"><text data-variant="normal" transform="scale(1,-1)" font-size="884px" font-family="serif">需</text></g><g data-mml-node="mi" transform="translate(5535.6,0)"><text data-variant="normal" transform="scale(1,-1)" font-size="884px" font-family="serif">要</text></g><g data-mml-node="mi" transform="translate(6535.6,0)"><text data-variant="normal" transform="scale(1,-1)" font-size="884px" font-family="serif">的</text></g></g></g></svg></mjx-container>s寄存器</li>
<li>返回： 恢复<mjx-container class="MathJax" jax="SVG"><svg style="vertical-align: -0.452ex;" xmlns="http://www.w3.org/2000/svg" width="15.893ex" height="2.149ex" role="img" focusable="false" viewBox="0 -750 7024.6 950"><g stroke="currentColor" fill="currentColor" stroke-width="0" transform="scale(1,-1)"><g data-mml-node="math"><g data-mml-node="mi"><path data-c="1D460" d="M131 289Q131 321 147 354T203 415T300 442Q362 442 390 415T419 355Q419 323 402 308T364 292Q351 292 340 300T328 326Q328 342 337 354T354 372T367 378Q368 378 368 379Q368 382 361 388T336 399T297 405Q249 405 227 379T204 326Q204 301 223 291T278 274T330 259Q396 230 396 163Q396 135 385 107T352 51T289 7T195 -10Q118 -10 86 19T53 87Q53 126 74 143T118 160Q133 160 146 151T160 120Q160 94 142 76T111 58Q109 57 108 57T107 55Q108 52 115 47T146 34T201 27Q237 27 263 38T301 66T318 97T323 122Q323 150 302 164T254 181T195 196T148 231Q131 256 131 289Z"></path></g><g data-mml-node="mi" transform="translate(469,0)"><text data-variant="normal" transform="scale(1,-1)" font-size="884px" font-family="serif">寄</text></g><g data-mml-node="mi" transform="translate(1469,0)"><text data-variant="normal" transform="scale(1,-1)" font-size="884px" font-family="serif">存</text></g><g data-mml-node="mi" transform="translate(2469,0)"><text data-variant="normal" transform="scale(1,-1)" font-size="884px" font-family="serif">器</text></g><g data-mml-node="mo" transform="translate(3746.8,0)"><path data-c="2192" d="M56 237T56 250T70 270H835Q719 357 692 493Q692 494 692 496T691 499Q691 511 708 511H711Q720 511 723 510T729 506T732 497T735 481T743 456Q765 389 816 336T935 261Q944 258 944 250Q944 244 939 241T915 231T877 212Q836 186 806 152T761 85T740 35T732 4Q730 -6 727 -8T711 -11Q691 -11 691 0Q691 7 696 25Q728 151 835 230H70Q56 237 56 250Z"></path></g><g data-mml-node="mi" transform="translate(5024.6,0)"><text data-variant="normal" transform="scale(1,-1)" font-size="884px" font-family="serif">恢</text></g><g data-mml-node="mi" transform="translate(6024.6,0)"><text data-variant="normal" transform="scale(1,-1)" font-size="884px" font-family="serif">复</text></g></g></g></svg></mjx-container>ra → 释放栈帧 → jr $ra</li>
</ul>
<h4 id="8-2-4-全局变量和字符串常量"><a href="#8-2-4-全局变量和字符串常量" class="headerlink" title="8.2.4 全局变量和字符串常量"></a>8.2.4 全局变量和字符串常量</h4><div class="code-container" data-rel="Java"><figure class="iseeu highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="variable">a</span> <span class="operator">=</span> <span class="number">10</span>;    → a: .word <span class="number">10</span></span><br><span class="line"><span class="type">int</span> arr[<span class="number">3</span>];    → arr: .word <span class="number">0</span>， <span class="number">0</span>， <span class="number">0</span></span><br><span class="line"><span class="string">"hello"</span>        → str_0: .asciiz <span class="string">"hello"</span></span><br></pre></td></tr></table></figure></div>
<p>程序结构：<br><div class="code-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">jal main</span><br><span class="line">j end</span><br><span class="line">...(函数定义)</span><br><span class="line">end: li $v0， 10; syscall</span><br></pre></td></tr></table></figure></div></p>
]]></content>
      <tags>
        <tag>编译</tag>
      </tags>
  </entry>
</search>
